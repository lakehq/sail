coverage:
  status:
    patch: false
    project:
      default:
        only_pulls: true
        paths:
          - "crates/"
        target: 0
        informational: true

codecov:
  notify:
    after_n_builds: 1

comment:
  layout: "files"

ignore:
  - "crates/sail-gold-test"
