# generated by datamodel-codegen:
#   filename:  https://api.typesafe.ai/openapi.json

from __future__ import annotations

from typing import Any, Literal

from pydantic import BaseModel, Field, RootModel


class ChoiceAnswer(BaseModel):
    type: Literal["choice"] = Field(..., description="Identifies a selection from the requested choices.", examples=["choice"], title="Type")
    choice: str = Field(
        ...,
        description="The name of the choice with the highest probability among the question's criteria.",
        examples=["angry"],
        title="Choice",
    )
    confidence: float = Field(
        ...,
        description="Confidence in the selected choice, from 0 to 1. Higher values indicate greater certainty; use lower values to flag uncertain selections for review.",
        examples=[0.9],
        title="Confidence",
    )
    probabilities: dict[str, float] = Field(
        ...,
        description="Probability of each choice in criteria, keyed by choice name, from 0 to 1. Shows how likely the alternatives are; values sum to approximately 1.",
        examples=[{"angry": 0.8, "calm": 0.1, "excited": 0.1}],
        title="Probabilities",
    )


class ChoiceQuestion(BaseModel):
    type: Literal["choice"] = Field(
        ..., description="Identifies a question that selects one of the choices in criteria.", examples=["choice"], title="Type"
    )
    instructions: str | dict[str, Any] | list[Any] | None = Field(
        None,
        description="What the model should decide when choosing an option.",
        examples=["What is the tone of this message?"],
        title="Instructions",
    )
    criteria: dict[str, str | dict[str, Any] | list[Any] | None] = Field(
        ...,
        description="Choice names and descriptions of when each applies. A choice without a description is interpreted by its name alone.",
        examples=[
            {"angry": "An upset or hostile message", "calm": "A neutral or polite message", "excited": "An enthusiastic or eager message"}
        ],
        title="Criteria",
    )


class ModelMetadata(BaseModel):
    name: str = Field(..., description="Model name or alias accepted by the request's model field.", examples=["jev-latest"], title="Name")
    description: str = Field(
        ...,
        description="Human-readable description of the model and its capabilities.",
        examples=["General-purpose system one model."],
        title="Description",
    )
    release_date: str = Field(..., description="Model release date, formatted as YYYY-MM-DD.", examples=["2026-09-15"], title="Release Date")


class ModelMetadataList(BaseModel):
    models: list[ModelMetadata] = Field(
        ...,
        description="Available models and aliases. Use a model's name in POST /v1/systemone requests.",
        examples=[[{"description": "General-purpose system one model.", "name": "jev-latest", "release_date": "2026-09-15"}]],
        title="Models",
    )


class NoulAnswer(BaseModel):
    type: Literal["noul"] = Field(..., description="Identifies a yes/no answer.", examples=["noul"], title="Type")
    noul: float = Field(
        ...,
        description="Probability of a yes answer or a true statement, from 0 to 1. Values near 1 favor yes or true, values near 0 favor no or false, and values near 0.5 indicate uncertainty.",
        examples=[0.98],
        title="Noul",
    )


class NoulCriteria(BaseModel):
    true: str | dict[str, Any] | list[Any] | None = Field(
        None, description="What counts as a yes answer.", examples=["The message is unsolicited advertising."], title="True"
    )
    false: str | dict[str, Any] | list[Any] | None = Field(
        None, description="What counts as a no answer.", examples=["The message is a legitimate conversation."], title="False"
    )


class NoulQuestion(BaseModel):
    type: Literal["noul"] = Field(..., description="Identifies a yes/no question or statement.", examples=["noul"], title="Type")
    instructions: str | dict[str, Any] | list[Any] | None = Field(
        None,
        description="The yes/no question or statement to evaluate.",
        examples=["Is this message spam?", "This message contains unsolicited advertising.", {"task": "Identify unsolicited advertising."}],
        title="Instructions",
    )
    criteria: NoulCriteria | None = Field(
        None,
        description="Criteria clarifying what counts as a yes or no answer.",
        examples=[{"false": "A legitimate conversation", "true": "Unsolicited advertising"}],
    )


class ScoreAnswer(BaseModel):
    type: Literal["score"] = Field(..., description="Identifies a rating against the requested score levels.", examples=["score"], title="Type")
    score: float = Field(
        ...,
        description="Expected score: the probability-weighted average of the rubric levels. May fall between integer levels.",
        examples=[1.7],
        title="Score",
    )
    confidence: float = Field(
        ...,
        description="Confidence in the score, from 0 to 1. Higher values indicate greater certainty; use lower values to flag uncertain ratings for review.",
        examples=[0.9],
        title="Confidence",
    )
    legend: dict[str, str | dict[str, Any] | list[Any]] = Field(
        ...,
        description="The requested criteria mapped to their score levels, so you can interpret the score.",
        examples=[{"0": "Can wait", "1": "Needs attention this week", "2": "Needs attention today"}],
        title="Legend",
    )
    probabilities: dict[str, float] = Field(
        ...,
        description="Probability of each score level, from 0 to 1, using the same keys as legend. Shows how likely the alternatives are; values sum to approximately 1.",
        examples=[{"0": 0.1, "1": 0.1, "2": 0.8}],
        title="Probabilities",
    )


class ScoreQuestion(BaseModel):
    type: Literal["score"] = Field(
        ..., description="Identifies a question that rates the content using the levels in criteria.", examples=["score"], title="Type"
    )
    instructions: str | dict[str, Any] | list[Any] | None = Field(
        None, description="What the model should rate.", examples=["How urgent is this message?"], title="Instructions"
    )
    criteria: list[str | dict[str, Any] | list[Any]] = Field(
        ...,
        description="Ordered descriptions of the score levels. Each description's position determines its score, starting at zero.",
        examples=[["Can wait", "Needs attention this week", "Needs attention today"]],
        min_length=1,
        title="Criteria",
    )


class Usage(BaseModel):
    input_tokens: int = Field(
        ..., description="Number of billable input tokens used to evaluate the request.", examples=[120], title="Input Tokens"
    )
    output_tokens: int = Field(
        ...,
        description="Number of output tokens used to answer the questions. Output tokens are currently free of charge.",
        examples=[12],
        title="Output Tokens",
    )


class ValidationError(BaseModel):
    loc: list[str | int] = Field(
        ...,
        description="Path to the invalid value: the request location followed by field names and array indices.",
        examples=[["body", "questions", "urgency", "score", "criteria"]],
        title="Location",
    )
    msg: str = Field(..., description="Human-readable explanation of the validation failure.", examples=["Field required"], title="Message")
    type: str = Field(..., description="Machine-readable validation error code.", examples=["missing"], title="Error Type")
    input: Any | None = Field(None, description="The input value that failed validation.", examples=[{"type": "score"}], title="Input")
    ctx: dict[str, Any] | None = Field(
        None, description="Additional context used to explain the validation failure.", examples=[{"min_length": 1}], title="Context"
    )


class Answer(RootModel[NoulAnswer | ScoreAnswer | ChoiceAnswer]):
    root: NoulAnswer | ScoreAnswer | ChoiceAnswer = Field(
        ..., description="An answer whose type matches the corresponding question.", discriminator="type"
    )


class HTTPValidationError(BaseModel):
    detail: list[ValidationError] | None = Field(
        None,
        description="Validation errors describing which request values are missing or invalid.",
        examples=[[{"loc": ["body", "state"], "msg": "Field required", "type": "missing"}]],
        title="Detail",
    )


class Question(RootModel[NoulQuestion | ChoiceQuestion | ScoreQuestion]):
    root: NoulQuestion | ChoiceQuestion | ScoreQuestion = Field(..., description="A question about the supplied content.", discriminator="type")


class SystemOneRequest(BaseModel):
    state: str | dict[str, Any] | list[Any] = Field(
        ...,
        description="The content all questions in this request refer to.",
        examples=["I was charged twice. Please help.", {"message": "Please help.", "subject": "Duplicate charge"}],
        title="State",
    )
    model: str = Field(
        ...,
        description="Name or alias of the model to use. Available names are returned by GET /v1/models.",
        examples=["jev-latest"],
        title="Model",
    )
    questions: dict[str, Question] = Field(
        ...,
        description="Questions to ask about the content, each with a name you choose. The response uses those names to identify the answers.",
        examples=[{"billing": {"instructions": "Is this message about billing?", "type": "noul"}}],
        min_length=1,
        title="Questions",
    )


class SystemOneResponse(BaseModel):
    model: str = Field(
        ...,
        description="Name of the model that answered the questions. May differ from the alias supplied in the request.",
        examples=["jev-latest"],
        title="Model",
    )
    answers: dict[str, Answer] = Field(
        ...,
        description="Answers keyed by the question names supplied in the request. Each answer's type matches its question's type.",
        examples=[{"billing": {"noul": 0.98, "type": "noul"}}],
        min_length=1,
        title="Answers",
    )
    usage: Usage = Field(
        ..., description="Input and output token counts for this evaluation.", examples=[{"input_tokens": 120, "output_tokens": 12}]
    )
