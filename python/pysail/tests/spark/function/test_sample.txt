>>> df = spark.range(10)
>>> df.sample(0.0000000001, 3).show()
+---+
| id|
+---+
+---+

>>> df = spark.range(10)
>>> df.sample(True, 0.1, 1).show()
+---+
| id|
+---+
|  0|
|  4|
+---+
>>> df.sample(True, 0.5, 1).show()
+---+
| id|
+---+
|  0|
|  0|
|  3|
|  4|
|  8|
+---+

>>> from pyspark.sql.functions import col
>>> dataset = spark.range(0, 100).select((col("id") % 3).alias("key"))
>>> dataset.sampleBy("key", fractions={0: 0.1, 1: 0.2}, seed=0).show()
+---+
|key|
+---+
|  0|
|  1|
|  0|
|  1|
|  1|
|  1|
|  1|
|  1|
|  1|
+---+
