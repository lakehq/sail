# serializer version: 1
---
name: "test_the_plan_for_a_literal_unit"
data:
  |-
    == Physical Plan ==
    ProjectionExec: expr=[#2@0 as result]
      ProjectionExec: expr=[date_trunc(YEAR, column1@0) as #2]
        DataSourceExec: partitions=1, partition_sizes=[1]
---
name: "test_the_plan_for_a_unit_coming_from_a_column"
data:
  |-
    == Physical Plan ==
    ProjectionExec: expr=[#4@0 as result]
      ProjectionExec: expr=[date_trunc(CASE WHEN column2@1 ILIKE mon OR column2@1 ILIKE mm THEN month WHEN column2@1 ILIKE yy OR column2@1 ILIKE yyyy THEN year WHEN column2@1 ILIKE dd THEN day ELSE column2@1 END, column1@0) as #4]
        DataSourceExec: partitions=1, partition_sizes=[1]
