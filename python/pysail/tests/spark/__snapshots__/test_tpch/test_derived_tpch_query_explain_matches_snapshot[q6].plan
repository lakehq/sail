== Codegen ==
Whole-stage codegen is not supported; showing physical plan instead.

== Plan Steps ==
initial_logical_plan:
Projection: <exprs>
  Aggregate: groupBy=[[]], aggr=[[sum(lineitem.#<col> * lineitem.#<col>)]]
    Filter: lineitem.#<col> >= Date32("1994-01-01") AND lineitem.#<col> < Date32("1994-01-01") + IntervalYearMonth("12") AND lineitem.#<col> >= Decimal128(Some(4),2,2) - Decimal128(Some(1),2,2) AND lineitem.#<col> <= Decimal128(Some(4),2,2) + Decimal128(Some(1),2,2) AND lineitem.#<col> < Int32(24)
      Projection: <exprs>
        Projection: <exprs>
          SubqueryAlias: lineitem
            Projection: <exprs>
              Projection: <exprs>
                TableScan: ?table?

logical_plan after resolve_grouping_function:
SAME TEXT AS ABOVE

logical_plan after type_coercion:
Projection: <exprs>
  Aggregate: groupBy=[[]], aggr=[[sum(lineitem.#<col> * lineitem.#<col>)]]
    Filter: lineitem.#<col> >= Date32("1994-01-01") AND lineitem.#<col> < Date32("1994-01-01") + IntervalYearMonth("12") AND CAST(lineitem.#<col> AS Decimal128(3, 2)) >= Decimal128(Some(4),2,2) - Decimal128(Some(1),2,2) AND CAST(lineitem.#<col> AS Decimal128(3, 2)) <= Decimal128(Some(4),2,2) + Decimal128(Some(1),2,2) AND CAST(lineitem.#<col> AS Decimal128(12, 2)) < CAST(Int32(24) AS Decimal128(12, 2))
      Projection: <exprs>
        Projection: <exprs>
          SubqueryAlias: lineitem
            Projection: <exprs>
              Projection: <exprs>
                TableScan: ?table?

analyzed_logical_plan:
SAME TEXT AS ABOVE

logical_plan after eliminate_nested_union:
SAME TEXT AS ABOVE

logical_plan after simplify_expressions:
Projection: <exprs>
  Aggregate: groupBy=[[]], aggr=[[sum(lineitem.#<col> * lineitem.#<col>)]]
    Filter: lineitem.#<col> >= Date32("1994-01-01") AND lineitem.#<col> < Date32("1995-01-01") AND lineitem.#<col> >= Decimal128(Some(3),2,2) AND lineitem.#<col> <= Decimal128(Some(5),2,2) AND lineitem.#<col> < Decimal128(Some(2400),4,2)
      Projection: <exprs>
        Projection: <exprs>
          SubqueryAlias: lineitem
            Projection: <exprs>
              Projection: <exprs>
                TableScan: ?table?

logical_plan after replace_distinct_aggregate:
SAME TEXT AS ABOVE

logical_plan after eliminate_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_predicate_subquery:
SAME TEXT AS ABOVE

logical_plan after scalar_subquery_to_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_lateral_join:
SAME TEXT AS ABOVE

logical_plan after extract_equijoin_predicate:
SAME TEXT AS ABOVE

logical_plan after eliminate_duplicated_expr:
SAME TEXT AS ABOVE

logical_plan after eliminate_filter:
SAME TEXT AS ABOVE

logical_plan after eliminate_cross_join:
SAME TEXT AS ABOVE

logical_plan after eliminate_limit:
SAME TEXT AS ABOVE

logical_plan after propagate_empty_relation:
SAME TEXT AS ABOVE

logical_plan after eliminate_one_union:
SAME TEXT AS ABOVE

logical_plan after filter_null_join_keys:
SAME TEXT AS ABOVE

logical_plan after eliminate_outer_join:
SAME TEXT AS ABOVE

logical_plan after push_down_limit:
SAME TEXT AS ABOVE

logical_plan after push_down_filter:
Projection: <exprs>
  Aggregate: groupBy=[[]], aggr=[[sum(lineitem.#<col> * lineitem.#<col>)]]
    Projection: <exprs>
      Projection: <exprs>
        SubqueryAlias: lineitem
          Projection: <exprs>
            Projection: <exprs>
              Filter: ?table?._10 >= Date32("1994-01-01") AND ?table?._10 < Date32("1995-01-01") AND ?table?._6 >= Decimal128(Some(3),2,2) AND ?table?._6 <= Decimal128(Some(5),2,2) AND ?table?._4 < Decimal128(Some(2400),4,2)
                TableScan: ?table?

logical_plan after single_distinct_aggregation_to_group_by:
SAME TEXT AS ABOVE

logical_plan after eliminate_group_by_constant:
SAME TEXT AS ABOVE

logical_plan after common_sub_expression_eliminate:
SAME TEXT AS ABOVE

logical_plan after optimize_projections:
Projection: <exprs>
  Aggregate: groupBy=[[]], aggr=[[sum(lineitem.#<col> * lineitem.#<col>)]]
    Projection: <exprs>
      SubqueryAlias: lineitem
        Projection: <exprs>
          Filter: ?table?._10 >= Date32("1994-01-01") AND ?table?._10 < Date32("1995-01-01") AND ?table?._6 >= Decimal128(Some(3),2,2) AND ?table?._6 <= Decimal128(Some(5),2,2) AND ?table?._4 < Decimal128(Some(2400),4,2)
            TableScan: ?table? projection=[_4, _5, _6, _10]

logical_plan after eliminate_nested_union:
SAME TEXT AS ABOVE

logical_plan after simplify_expressions:
SAME TEXT AS ABOVE

logical_plan after replace_distinct_aggregate:
SAME TEXT AS ABOVE

logical_plan after eliminate_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_predicate_subquery:
SAME TEXT AS ABOVE

logical_plan after scalar_subquery_to_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_lateral_join:
SAME TEXT AS ABOVE

logical_plan after extract_equijoin_predicate:
SAME TEXT AS ABOVE

logical_plan after eliminate_duplicated_expr:
SAME TEXT AS ABOVE

logical_plan after eliminate_filter:
SAME TEXT AS ABOVE

logical_plan after eliminate_cross_join:
SAME TEXT AS ABOVE

logical_plan after eliminate_limit:
SAME TEXT AS ABOVE

logical_plan after propagate_empty_relation:
SAME TEXT AS ABOVE

logical_plan after eliminate_one_union:
SAME TEXT AS ABOVE

logical_plan after filter_null_join_keys:
SAME TEXT AS ABOVE

logical_plan after eliminate_outer_join:
SAME TEXT AS ABOVE

logical_plan after push_down_limit:
SAME TEXT AS ABOVE

logical_plan after push_down_filter:
SAME TEXT AS ABOVE

logical_plan after single_distinct_aggregation_to_group_by:
SAME TEXT AS ABOVE

logical_plan after eliminate_group_by_constant:
SAME TEXT AS ABOVE

logical_plan after common_sub_expression_eliminate:
SAME TEXT AS ABOVE

logical_plan after optimize_projections:
SAME TEXT AS ABOVE

logical_plan:
Projection: <exprs>
  Aggregate: groupBy=[[]], aggr=[[sum(lineitem.#<col> * lineitem.#<col>)]]
    Projection: <exprs>
      SubqueryAlias: lineitem
        Projection: <exprs>
          Filter: ?table?._10 >= Date32("1994-01-01") AND ?table?._10 < Date32("1995-01-01") AND ?table?._6 >= Decimal128(Some(3),2,2) AND ?table?._6 <= Decimal128(Some(5),2,2) AND ?table?._4 < Decimal128(Some(2400),4,2)
            TableScan: ?table? projection=[_4, _5, _6, _10]

initial_physical_plan:
ProjectionExec: expr=[<exprs>]
  AggregateExec: mode=Final, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
    CoalescePartitionsExec
      AggregateExec: mode=Partial, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
        RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
          ProjectionExec: expr=[<exprs>]
            CoalesceBatchesExec: target_batch_size=8192
              FilterExec: _10@<id> >= 1994-01-01 AND _10@<id> < 1995-01-01 AND _6@<id> >= Some(3),2,2 AND _6@<id> <= Some(5),2,2 AND _4@<id> < Some(2400),4,2, projection=[_5@<id>, _6@<id>]
                DataSourceExec: partitions=1, partition_sizes=[<sizes>]


initial_physical_plan_with_stats:
ProjectionExec: expr=[<exprs>]
  AggregateExec: mode=Final, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
    CoalescePartitionsExec, statistics=[Rows=Inexact(1201), Bytes=Absent, [(Col[0]:)]]
      AggregateExec: mode=Partial, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)], statistics=[Rows=Inexact(1201), Bytes=Absent, [(Col[0]:)]]
        RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>, statistics=[Rows=Inexact(1201), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0)),(Col[1]: Null=Inexact(0))]]
          ProjectionExec: expr=[<exprs>]
            CoalesceBatchesExec: target_batch_size=8192, statistics=[Rows=Inexact(1201), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0)),(Col[1]: Null=Inexact(0))]]
              FilterExec: _10@<id> >= 1994-01-01 AND _10@<id> < 1995-01-01 AND _6@<id> >= Some(3),2,2 AND _6@<id> <= Some(5),2,2 AND _4@<id> < Some(2400),4,2, projection=[_5@<id>, _6@<id>], statistics=[Rows=Inexact(1201), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0)),(Col[1]: Null=Inexact(0))]]
                DataSourceExec: partitions=1, partition_sizes=[<sizes>], statistics=[Rows=Exact(6005), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]: Null=Exact(0)),(Col[3]: Null=Exact(0))]]


initial_physical_plan_with_schema:
ProjectionExec: expr=[<exprs>]
  AggregateExec: mode=Final, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)], schema=[sum(lineitem.#<col> * lineitem.#<col>):Decimal128(20, 4);N]
    CoalescePartitionsExec, schema=[sum(lineitem.#<col> * lineitem.#<col>)[sum]:Decimal128(20, 4);N]
      AggregateExec: mode=Partial, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)], schema=[sum(lineitem.#<col> * lineitem.#<col>)[sum]:Decimal128(20, 4);N]
        RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>, schema=[#<col>:Decimal128(7, 2);N, #<col>:Decimal128(2, 2);N]
          ProjectionExec: expr=[<exprs>]
            CoalesceBatchesExec: target_batch_size=8192, schema=[_5:Decimal128(7, 2);N, _6:Decimal128(2, 2);N]
              FilterExec: _10@<id> >= 1994-01-01 AND _10@<id> < 1995-01-01 AND _6@<id> >= Some(3),2,2 AND _6@<id> <= Some(5),2,2 AND _4@<id> < Some(2400),4,2, projection=[_5@<id>, _6@<id>], schema=[_5:Decimal128(7, 2);N, _6:Decimal128(2, 2);N]
                DataSourceExec: partitions=1, partition_sizes=[<sizes>], schema=[_4:Decimal128(4, 2);N, _5:Decimal128(7, 2);N, _6:Decimal128(2, 2);N, _10:Date32;N]


physical_plan after OutputRequirements:
OutputRequirementExec: order_by=[], dist_by=Unspecified
  ProjectionExec: expr=[<exprs>]
    AggregateExec: mode=Final, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
      CoalescePartitionsExec
        AggregateExec: mode=Partial, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
          RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
            ProjectionExec: expr=[<exprs>]
              CoalesceBatchesExec: target_batch_size=8192
                FilterExec: _10@<id> >= 1994-01-01 AND _10@<id> < 1995-01-01 AND _6@<id> >= Some(3),2,2 AND _6@<id> <= Some(5),2,2 AND _4@<id> < Some(2400),4,2, projection=[_5@<id>, _6@<id>]
                  DataSourceExec: partitions=1, partition_sizes=[<sizes>]


physical_plan after aggregate_statistics:
SAME TEXT AS ABOVE

physical_plan after join_selection:
SAME TEXT AS ABOVE

physical_plan after LimitedDistinctAggregation:
SAME TEXT AS ABOVE

physical_plan after FilterPushdown:
SAME TEXT AS ABOVE

physical_plan after EnforceDistribution:
SAME TEXT AS ABOVE

physical_plan after CombinePartialFinalAggregate:
SAME TEXT AS ABOVE

physical_plan after EnforceSorting:
SAME TEXT AS ABOVE

physical_plan after OptimizeAggregateOrder:
SAME TEXT AS ABOVE

physical_plan after ProjectionPushdown:
SAME TEXT AS ABOVE

physical_plan after coalesce_batches:
OutputRequirementExec: order_by=[], dist_by=Unspecified
  ProjectionExec: expr=[<exprs>]
    AggregateExec: mode=Final, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
      CoalescePartitionsExec
        AggregateExec: mode=Partial, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
          RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
            ProjectionExec: expr=[<exprs>]
              CoalesceBatchesExec: target_batch_size=8192
                CoalesceBatchesExec: target_batch_size=8192
                  FilterExec: _10@<id> >= 1994-01-01 AND _10@<id> < 1995-01-01 AND _6@<id> >= Some(3),2,2 AND _6@<id> <= Some(5),2,2 AND _4@<id> < Some(2400),4,2, projection=[_5@<id>, _6@<id>]
                    DataSourceExec: partitions=1, partition_sizes=[<sizes>]


physical_plan after coalesce_async_exec_input:
SAME TEXT AS ABOVE

physical_plan after OutputRequirements:
ProjectionExec: expr=[<exprs>]
  AggregateExec: mode=Final, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
    CoalescePartitionsExec
      AggregateExec: mode=Partial, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
        RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
          ProjectionExec: expr=[<exprs>]
            CoalesceBatchesExec: target_batch_size=8192
              CoalesceBatchesExec: target_batch_size=8192
                FilterExec: _10@<id> >= 1994-01-01 AND _10@<id> < 1995-01-01 AND _6@<id> >= Some(3),2,2 AND _6@<id> <= Some(5),2,2 AND _4@<id> < Some(2400),4,2, projection=[_5@<id>, _6@<id>]
                  DataSourceExec: partitions=1, partition_sizes=[<sizes>]


physical_plan after LimitAggregation:
SAME TEXT AS ABOVE

physical_plan after LimitPushPastWindows:
SAME TEXT AS ABOVE

physical_plan after LimitPushdown:
SAME TEXT AS ABOVE

physical_plan after ProjectionPushdown:
SAME TEXT AS ABOVE

physical_plan after EnsureCooperative:
SAME TEXT AS ABOVE

physical_plan after FilterPushdown(Post):
SAME TEXT AS ABOVE

physical_plan after RewriteExplicitRepartition:
SAME TEXT AS ABOVE

physical_plan after SanityCheckPlan:
SAME TEXT AS ABOVE

physical_plan:
ProjectionExec: expr=[<exprs>]
  ProjectionExec: expr=[<exprs>]
    AggregateExec: mode=Final, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
      CoalescePartitionsExec
        AggregateExec: mode=Partial, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
          RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
            ProjectionExec: expr=[<exprs>]
              CoalesceBatchesExec: target_batch_size=8192
                CoalesceBatchesExec: target_batch_size=8192
                  FilterExec: _10@<id> >= 1994-01-01 AND _10@<id> < 1995-01-01 AND _6@<id> >= Some(3),2,2 AND _6@<id> <= Some(5),2,2 AND _4@<id> < Some(2400),4,2, projection=[_5@<id>, _6@<id>]
                    DataSourceExec: partitions=1, partition_sizes=[<sizes>]


== Physical Plan ==
ProjectionExec: expr=[<exprs>]
  ProjectionExec: expr=[<exprs>]
    AggregateExec: mode=Final, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
      CoalescePartitionsExec
        AggregateExec: mode=Partial, gby=[], aggr=[sum(lineitem.#<col> * lineitem.#<col>)]
          RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
            ProjectionExec: expr=[<exprs>]
              CoalesceBatchesExec: target_batch_size=8192
                CoalesceBatchesExec: target_batch_size=8192
                  FilterExec: _10@<id> >= 1994-01-01 AND _10@<id> < 1995-01-01 AND _6@<id> >= Some(3),2,2 AND _6@<id> <= Some(5),2,2 AND _4@<id> < Some(2400),4,2, projection=[_5@<id>, _6@<id>]
                    DataSourceExec: partitions=1, partition_sizes=[<sizes>]