== Codegen ==
Whole-stage codegen is not supported; showing physical plan instead.

== Plan Steps ==
initial_logical_plan:
Limit: skip=0, fetch=Int32(100)
  Projection: <exprs>
    Sort: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>), grouping(item.#<col>), grouping(item.#<col>)]]
            Filter: d1.#<col> >= Int32(1205) AND d1.#<col> <= Int32(1205) + Int32(11) AND d1.#<col> = web_sales.#<col> AND item.#<col> = web_sales.#<col>
              Cross Join: 
                Cross Join: 
                  Projection: <exprs>
                    Projection: <exprs>
                      SubqueryAlias: web_sales
                        Projection: <exprs>
                          Projection: <exprs>
                            TableScan: ?table?
                  SubqueryAlias: d1
                    Projection: <exprs>
                      Projection: <exprs>
                        SubqueryAlias: date_dim
                          Projection: <exprs>
                            Projection: <exprs>
                              TableScan: ?table?
                Projection: <exprs>
                  Projection: <exprs>
                    SubqueryAlias: item
                      Projection: <exprs>
                        Projection: <exprs>
                          TableScan: ?table?

logical_plan after resolve_grouping_function:
Limit: skip=0, fetch=Int32(100)
  Projection: <exprs>
    Sort: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Filter: d1.#<col> >= Int32(1205) AND d1.#<col> <= Int32(1205) + Int32(11) AND d1.#<col> = web_sales.#<col> AND item.#<col> = web_sales.#<col>
                Cross Join: 
                  Cross Join: 
                    Projection: <exprs>
                      Projection: <exprs>
                        SubqueryAlias: web_sales
                          Projection: <exprs>
                            Projection: <exprs>
                              TableScan: ?table?
                    SubqueryAlias: d1
                      Projection: <exprs>
                        Projection: <exprs>
                          SubqueryAlias: date_dim
                            Projection: <exprs>
                              Projection: <exprs>
                                TableScan: ?table?
                  Projection: <exprs>
                    Projection: <exprs>
                      SubqueryAlias: item
                        Projection: <exprs>
                          Projection: <exprs>
                            TableScan: ?table?

logical_plan after type_coercion:
Limit: skip=0, fetch=CAST(Int32(100) AS Int64)
  Projection: <exprs>
    Sort: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Filter: d1.#<col> >= CAST(Int32(1205) AS Int64) AND d1.#<col> <= CAST(Int32(1205) + Int32(11) AS Int64) AND CAST(d1.#<col> AS Float64) = web_sales.#<col> AND item.#<col> = web_sales.#<col>
                Cross Join: 
                  Cross Join: 
                    Projection: <exprs>
                      Projection: <exprs>
                        SubqueryAlias: web_sales
                          Projection: <exprs>
                            Projection: <exprs>
                              TableScan: ?table?
                    SubqueryAlias: d1
                      Projection: <exprs>
                        Projection: <exprs>
                          SubqueryAlias: date_dim
                            Projection: <exprs>
                              Projection: <exprs>
                                TableScan: ?table?
                  Projection: <exprs>
                    Projection: <exprs>
                      SubqueryAlias: item
                        Projection: <exprs>
                          Projection: <exprs>
                            TableScan: ?table?

analyzed_logical_plan:
SAME TEXT AS ABOVE

logical_plan after eliminate_nested_union:
SAME TEXT AS ABOVE

logical_plan after simplify_expressions:
Limit: skip=0, fetch=100
  Projection: <exprs>
    Sort: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Filter: d1.#<col> >= Int64(1205) AND d1.#<col> <= Int64(1216) AND CAST(d1.#<col> AS Float64) = web_sales.#<col> AND web_sales.#<col> = item.#<col>
                Cross Join: 
                  Cross Join: 
                    Projection: <exprs>
                      Projection: <exprs>
                        SubqueryAlias: web_sales
                          Projection: <exprs>
                            Projection: <exprs>
                              TableScan: ?table?
                    SubqueryAlias: d1
                      Projection: <exprs>
                        Projection: <exprs>
                          SubqueryAlias: date_dim
                            Projection: <exprs>
                              Projection: <exprs>
                                TableScan: ?table?
                  Projection: <exprs>
                    Projection: <exprs>
                      SubqueryAlias: item
                        Projection: <exprs>
                          Projection: <exprs>
                            TableScan: ?table?

logical_plan after replace_distinct_aggregate:
SAME TEXT AS ABOVE

logical_plan after eliminate_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_predicate_subquery:
SAME TEXT AS ABOVE

logical_plan after scalar_subquery_to_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_lateral_join:
SAME TEXT AS ABOVE

logical_plan after extract_equijoin_predicate:
SAME TEXT AS ABOVE

logical_plan after eliminate_duplicated_expr:
SAME TEXT AS ABOVE

logical_plan after eliminate_filter:
SAME TEXT AS ABOVE

logical_plan after eliminate_cross_join:
Limit: skip=0, fetch=100
  Projection: <exprs>
    Sort: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Filter: d1.#<col> >= Int64(1205) AND d1.#<col> <= Int64(1216)
                Inner Join: web_sales.#<col> = item.#<col>
                  Inner Join: web_sales.#<col> = CAST(d1.#<col> AS Float64)
                    Projection: <exprs>
                      Projection: <exprs>
                        SubqueryAlias: web_sales
                          Projection: <exprs>
                            Projection: <exprs>
                              TableScan: ?table?
                    SubqueryAlias: d1
                      Projection: <exprs>
                        Projection: <exprs>
                          SubqueryAlias: date_dim
                            Projection: <exprs>
                              Projection: <exprs>
                                TableScan: ?table?
                  Projection: <exprs>
                    Projection: <exprs>
                      SubqueryAlias: item
                        Projection: <exprs>
                          Projection: <exprs>
                            TableScan: ?table?

logical_plan after eliminate_limit:
SAME TEXT AS ABOVE

logical_plan after propagate_empty_relation:
SAME TEXT AS ABOVE

logical_plan after eliminate_one_union:
SAME TEXT AS ABOVE

logical_plan after filter_null_join_keys:
SAME TEXT AS ABOVE

logical_plan after eliminate_outer_join:
SAME TEXT AS ABOVE

logical_plan after push_down_limit:
Projection: <exprs>
  Limit: skip=0, fetch=100
    Sort: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Filter: d1.#<col> >= Int64(1205) AND d1.#<col> <= Int64(1216)
                Inner Join: web_sales.#<col> = item.#<col>
                  Inner Join: web_sales.#<col> = CAST(d1.#<col> AS Float64)
                    Projection: <exprs>
                      Projection: <exprs>
                        SubqueryAlias: web_sales
                          Projection: <exprs>
                            Projection: <exprs>
                              TableScan: ?table?
                    SubqueryAlias: d1
                      Projection: <exprs>
                        Projection: <exprs>
                          SubqueryAlias: date_dim
                            Projection: <exprs>
                              Projection: <exprs>
                                TableScan: ?table?
                  Projection: <exprs>
                    Projection: <exprs>
                      SubqueryAlias: item
                        Projection: <exprs>
                          Projection: <exprs>
                            TableScan: ?table?

logical_plan after push_down_filter:
Projection: <exprs>
  Limit: skip=0, fetch=100
    Sort: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Inner Join: web_sales.#<col> = item.#<col>
                Inner Join: web_sales.#<col> = CAST(d1.#<col> AS Float64)
                  Projection: <exprs>
                    Projection: <exprs>
                      SubqueryAlias: web_sales
                        Projection: <exprs>
                          Projection: <exprs>
                            TableScan: ?table?
                  SubqueryAlias: d1
                    Projection: <exprs>
                      Projection: <exprs>
                        SubqueryAlias: date_dim
                          Projection: <exprs>
                            Projection: <exprs>
                              Filter: ?table?._3 >= Int64(1205) AND ?table?._3 <= Int64(1216)
                                TableScan: ?table?
                Projection: <exprs>
                  Projection: <exprs>
                    SubqueryAlias: item
                      Projection: <exprs>
                        Projection: <exprs>
                          TableScan: ?table?

logical_plan after single_distinct_aggregation_to_group_by:
SAME TEXT AS ABOVE

logical_plan after eliminate_group_by_constant:
SAME TEXT AS ABOVE

logical_plan after common_sub_expression_eliminate:
Projection: <exprs>
  Limit: skip=0, fetch=100
    Projection: <exprs>
      Sort: <exprs>
        Projection: <exprs>
          Projection: <exprs>
            WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
              Projection: <exprs>
                Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
                  Inner Join: web_sales.#<col> = item.#<col>
                    Inner Join: web_sales.#<col> = CAST(d1.#<col> AS Float64)
                      Projection: <exprs>
                        Projection: <exprs>
                          SubqueryAlias: web_sales
                            Projection: <exprs>
                              Projection: <exprs>
                                TableScan: ?table?
                      SubqueryAlias: d1
                        Projection: <exprs>
                          Projection: <exprs>
                            SubqueryAlias: date_dim
                              Projection: <exprs>
                                Projection: <exprs>
                                  Filter: ?table?._3 >= Int64(1205) AND ?table?._3 <= Int64(1216)
                                    TableScan: ?table?
                    Projection: <exprs>
                      Projection: <exprs>
                        SubqueryAlias: item
                          Projection: <exprs>
                            Projection: <exprs>
                              TableScan: ?table?

logical_plan after optimize_projections:
Limit: skip=0, fetch=100
  Projection: <exprs>
    Sort: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Projection: <exprs>
                Inner Join: web_sales.#<col> = item.#<col>
                  Projection: <exprs>
                    Inner Join: web_sales.#<col> = CAST(d1.#<col> AS Float64)
                      Projection: <exprs>
                        SubqueryAlias: web_sales
                          Projection: <exprs>
                            TableScan: ?table? projection=[_0, _3, _29]
                      SubqueryAlias: d1
                        Projection: <exprs>
                          SubqueryAlias: date_dim
                            Projection: <exprs>
                              Filter: ?table?._3 >= Int64(1205) AND ?table?._3 <= Int64(1216)
                                TableScan: ?table? projection=[_0, _3]
                  Projection: <exprs>
                    SubqueryAlias: item
                      Projection: <exprs>
                        TableScan: ?table? projection=[_0, _10, _12]

logical_plan after eliminate_nested_union:
SAME TEXT AS ABOVE

logical_plan after simplify_expressions:
SAME TEXT AS ABOVE

logical_plan after replace_distinct_aggregate:
SAME TEXT AS ABOVE

logical_plan after eliminate_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_predicate_subquery:
SAME TEXT AS ABOVE

logical_plan after scalar_subquery_to_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_lateral_join:
SAME TEXT AS ABOVE

logical_plan after extract_equijoin_predicate:
SAME TEXT AS ABOVE

logical_plan after eliminate_duplicated_expr:
SAME TEXT AS ABOVE

logical_plan after eliminate_filter:
SAME TEXT AS ABOVE

logical_plan after eliminate_cross_join:
SAME TEXT AS ABOVE

logical_plan after eliminate_limit:
SAME TEXT AS ABOVE

logical_plan after propagate_empty_relation:
SAME TEXT AS ABOVE

logical_plan after eliminate_one_union:
SAME TEXT AS ABOVE

logical_plan after filter_null_join_keys:
SAME TEXT AS ABOVE

logical_plan after eliminate_outer_join:
SAME TEXT AS ABOVE

logical_plan after push_down_limit:
Projection: <exprs>
  Sort: <exprs>
    Projection: <exprs>
      WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
        Projection: <exprs>
          Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
            Projection: <exprs>
              Inner Join: web_sales.#<col> = item.#<col>
                Projection: <exprs>
                  Inner Join: web_sales.#<col> = CAST(d1.#<col> AS Float64)
                    Projection: <exprs>
                      SubqueryAlias: web_sales
                        Projection: <exprs>
                          TableScan: ?table? projection=[_0, _3, _29]
                    SubqueryAlias: d1
                      Projection: <exprs>
                        SubqueryAlias: date_dim
                          Projection: <exprs>
                            Filter: ?table?._3 >= Int64(1205) AND ?table?._3 <= Int64(1216)
                              TableScan: ?table? projection=[_0, _3]
                Projection: <exprs>
                  SubqueryAlias: item
                    Projection: <exprs>
                      TableScan: ?table? projection=[_0, _10, _12]

logical_plan after push_down_filter:
SAME TEXT AS ABOVE

logical_plan after single_distinct_aggregation_to_group_by:
SAME TEXT AS ABOVE

logical_plan after eliminate_group_by_constant:
SAME TEXT AS ABOVE

logical_plan after common_sub_expression_eliminate:
Projection: <exprs>
  Sort: <exprs>
    Projection: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Projection: <exprs>
                Inner Join: web_sales.#<col> = item.#<col>
                  Projection: <exprs>
                    Inner Join: web_sales.#<col> = CAST(d1.#<col> AS Float64)
                      Projection: <exprs>
                        SubqueryAlias: web_sales
                          Projection: <exprs>
                            TableScan: ?table? projection=[_0, _3, _29]
                      SubqueryAlias: d1
                        Projection: <exprs>
                          SubqueryAlias: date_dim
                            Projection: <exprs>
                              Filter: ?table?._3 >= Int64(1205) AND ?table?._3 <= Int64(1216)
                                TableScan: ?table? projection=[_0, _3]
                  Projection: <exprs>
                    SubqueryAlias: item
                      Projection: <exprs>
                        TableScan: ?table? projection=[_0, _10, _12]

logical_plan after optimize_projections:
Projection: <exprs>
  Sort: <exprs>
    Projection: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Projection: <exprs>
                Inner Join: web_sales.#<col> = item.#<col>
                  Projection: <exprs>
                    Inner Join: web_sales.#<col> = CAST(d1.#<col> AS Float64)
                      Projection: <exprs>
                        SubqueryAlias: web_sales
                          Projection: <exprs>
                            TableScan: ?table? projection=[_0, _3, _29]
                      SubqueryAlias: d1
                        Projection: <exprs>
                          SubqueryAlias: date_dim
                            Projection: <exprs>
                              Filter: ?table?._3 >= Int64(1205) AND ?table?._3 <= Int64(1216)
                                TableScan: ?table? projection=[_0, _3]
                  Projection: <exprs>
                    SubqueryAlias: item
                      Projection: <exprs>
                        TableScan: ?table? projection=[_0, _10, _12]

logical_plan after eliminate_nested_union:
SAME TEXT AS ABOVE

logical_plan after simplify_expressions:
SAME TEXT AS ABOVE

logical_plan after replace_distinct_aggregate:
SAME TEXT AS ABOVE

logical_plan after eliminate_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_predicate_subquery:
SAME TEXT AS ABOVE

logical_plan after scalar_subquery_to_join:
SAME TEXT AS ABOVE

logical_plan after decorrelate_lateral_join:
SAME TEXT AS ABOVE

logical_plan after extract_equijoin_predicate:
SAME TEXT AS ABOVE

logical_plan after eliminate_duplicated_expr:
SAME TEXT AS ABOVE

logical_plan after eliminate_filter:
SAME TEXT AS ABOVE

logical_plan after eliminate_cross_join:
SAME TEXT AS ABOVE

logical_plan after eliminate_limit:
SAME TEXT AS ABOVE

logical_plan after propagate_empty_relation:
SAME TEXT AS ABOVE

logical_plan after eliminate_one_union:
SAME TEXT AS ABOVE

logical_plan after filter_null_join_keys:
SAME TEXT AS ABOVE

logical_plan after eliminate_outer_join:
SAME TEXT AS ABOVE

logical_plan after push_down_limit:
SAME TEXT AS ABOVE

logical_plan after push_down_filter:
SAME TEXT AS ABOVE

logical_plan after single_distinct_aggregation_to_group_by:
SAME TEXT AS ABOVE

logical_plan after eliminate_group_by_constant:
SAME TEXT AS ABOVE

logical_plan after common_sub_expression_eliminate:
SAME TEXT AS ABOVE

logical_plan after optimize_projections:
SAME TEXT AS ABOVE

logical_plan:
Projection: <exprs>
  Sort: <exprs>
    Projection: <exprs>
      Projection: <exprs>
        WindowAggr: windowExpr=[[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Projection: <exprs>
            Aggregate: groupBy=[[ROLLUP (item.#<col>, item.#<col>)]], aggr=[[sum(web_sales.#<col>)]]
              Projection: <exprs>
                Inner Join: web_sales.#<col> = item.#<col>
                  Projection: <exprs>
                    Inner Join: web_sales.#<col> = CAST(d1.#<col> AS Float64)
                      Projection: <exprs>
                        SubqueryAlias: web_sales
                          Projection: <exprs>
                            TableScan: ?table? projection=[_0, _3, _29]
                      SubqueryAlias: d1
                        Projection: <exprs>
                          SubqueryAlias: date_dim
                            Projection: <exprs>
                              Filter: ?table?._3 >= Int64(1205) AND ?table?._3 <= Int64(1216)
                                TableScan: ?table? projection=[_0, _3]
                  Projection: <exprs>
                    SubqueryAlias: item
                      Projection: <exprs>
                        TableScan: ?table? projection=[_0, _10, _12]

initial_physical_plan:
ProjectionExec: expr=[<exprs>]
  SortExec: [<sort_exprs>], fetch=100
    SortExec: expr=[<exprs>]
      ProjectionExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
            SortExec: expr=[<exprs>]
              CoalesceBatchesExec: target_batch_size=8192
                RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>
                  ProjectionExec: expr=[<exprs>]
                    AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)]
                      CoalesceBatchesExec: target_batch_size=8192
                        RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>
                          AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)]
                            RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                              CoalesceBatchesExec: target_batch_size=8192
                                HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>]
                                  CoalescePartitionsExec
                                    CoalesceBatchesExec: target_batch_size=8192
                                      HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>]
                                        ProjectionExec: expr=[<exprs>]
                                          DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                        ProjectionExec: expr=[<exprs>]
                                          CoalesceBatchesExec: target_batch_size=8192
                                            FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>]
                                              RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                                DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                  ProjectionExec: expr=[<exprs>]
                                    DataSourceExec: partitions=1, partition_sizes=[<sizes>]


initial_physical_plan_with_stats:
ProjectionExec: expr=[<exprs>]
  SortExec: [<sort_exprs>]
    SortExec: expr=[<exprs>]
      ProjectionExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted], statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:),(Col[4]:),(Col[5]:)]]
            SortExec: expr=[<exprs>]
              CoalesceBatchesExec: target_batch_size=8192, statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:),(Col[4]:)]]
                RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>, statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:),(Col[4]:)]]
                  ProjectionExec: expr=[<exprs>]
                    AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)], statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:)]]
                      CoalesceBatchesExec: target_batch_size=8192, statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:)]]
                        RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>, statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:)]]
                          AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)], statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:)]]
                            RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>, statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]: Null=Exact(4)),(Col[1]: Null=Exact(0)),(Col[2]: Null=Exact(0))]]
                              CoalesceBatchesExec: target_batch_size=8192, statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]: Null=Exact(4)),(Col[1]: Null=Exact(0)),(Col[2]: Null=Exact(0))]]
                                HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>], statistics=[Rows=Inexact(180), Bytes=Absent, [(Col[0]: Null=Exact(4)),(Col[1]: Null=Exact(0)),(Col[2]: Null=Exact(0))]]
                                  CoalescePartitionsExec, statistics=[Rows=Inexact(7212), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(4))]]
                                    CoalesceBatchesExec: target_batch_size=8192, statistics=[Rows=Inexact(7212), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(4))]]
                                      HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>], statistics=[Rows=Inexact(7212), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(4))]]
                                        ProjectionExec: expr=[<exprs>]
                                          DataSourceExec: partitions=1, partition_sizes=[<sizes>], statistics=[Rows=Exact(7212), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(4)),(Col[1]: Null=Exact(0)),(Col[2]: Null=Exact(4))]]
                                        ProjectionExec: expr=[<exprs>]
                                          CoalesceBatchesExec: target_batch_size=8192, statistics=[Rows=Inexact(14610), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0))]]
                                            FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>], statistics=[Rows=Inexact(14610), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0))]]
                                              RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>, statistics=[Rows=Exact(73049), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                                                DataSourceExec: partitions=1, partition_sizes=[<sizes>], statistics=[Rows=Exact(73049), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                                  ProjectionExec: expr=[<exprs>]
                                    DataSourceExec: partitions=1, partition_sizes=[<sizes>], statistics=[Rows=Exact(180), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]: Null=Exact(0))]]


initial_physical_plan_with_schema:
ProjectionExec: expr=[<exprs>]
  SortExec: [<sort_exprs>]
    SortExec: expr=[<exprs>]
      ProjectionExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted], schema=[#<col>:Utf8;N, #<col>:Utf8;N, sum(web_sales.#<col>):Decimal128(17, 2);N, grouping(item.#<col>):Int32, grouping(item.#<col>):Int32, rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW:UInt64]
            SortExec: expr=[<exprs>]
              CoalesceBatchesExec: target_batch_size=8192, schema=[#<col>:Utf8;N, #<col>:Utf8;N, sum(web_sales.#<col>):Decimal128(17, 2);N, grouping(item.#<col>):Int32, grouping(item.#<col>):Int32]
                RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>, schema=[#<col>:Utf8;N, #<col>:Utf8;N, sum(web_sales.#<col>):Decimal128(17, 2);N, grouping(item.#<col>):Int32, grouping(item.#<col>):Int32]
                  ProjectionExec: expr=[<exprs>]
                    AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)], schema=[#<col>:Utf8;N, #<col>:Utf8;N, __grouping_id:UInt8, sum(web_sales.#<col>):Decimal128(17, 2);N]
                      CoalesceBatchesExec: target_batch_size=8192, schema=[#<col>:Utf8;N, #<col>:Utf8;N, __grouping_id:UInt8, sum(web_sales.#<col>)[sum]:Decimal128(17, 2);N]
                        RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>, schema=[#<col>:Utf8;N, #<col>:Utf8;N, __grouping_id:UInt8, sum(web_sales.#<col>)[sum]:Decimal128(17, 2);N]
                          AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)], schema=[#<col>:Utf8;N, #<col>:Utf8;N, __grouping_id:UInt8, sum(web_sales.#<col>)[sum]:Decimal128(17, 2);N]
                            RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>, schema=[#<col>:Decimal128(7, 2);N, #<col>:Utf8;N, #<col>:Utf8;N]
                              CoalesceBatchesExec: target_batch_size=8192, schema=[#<col>:Decimal128(7, 2);N, #<col>:Utf8;N, #<col>:Utf8;N]
                                HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>], schema=[#<col>:Decimal128(7, 2);N, #<col>:Utf8;N, #<col>:Utf8;N]
                                  CoalescePartitionsExec, schema=[#<col>:Int64;N, #<col>:Decimal128(7, 2);N]
                                    CoalesceBatchesExec: target_batch_size=8192, schema=[#<col>:Int64;N, #<col>:Decimal128(7, 2);N]
                                      HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>], schema=[#<col>:Int64;N, #<col>:Decimal128(7, 2);N]
                                        ProjectionExec: expr=[<exprs>]
                                          DataSourceExec: partitions=1, partition_sizes=[<sizes>], schema=[_0:Float64;N, _3:Int64;N, _29:Decimal128(7, 2);N]
                                        ProjectionExec: expr=[<exprs>]
                                          CoalesceBatchesExec: target_batch_size=8192, schema=[_0:Int64;N]
                                            FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>], schema=[_0:Int64;N]
                                              RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>, schema=[_0:Int64;N, _3:Int64;N]
                                                DataSourceExec: partitions=1, partition_sizes=[<sizes>], schema=[_0:Int64;N, _3:Int64;N]
                                  ProjectionExec: expr=[<exprs>]
                                    DataSourceExec: partitions=1, partition_sizes=[<sizes>], schema=[_0:Int64;N, _10:Utf8;N, _12:Utf8;N]


physical_plan after OutputRequirements:
ProjectionExec: expr=[<exprs>]
  OutputRequirementExec: order_by=[(__common_expr_1@<id>, desc), (CASE WHEN __common_expr_1@<id> = 0 THEN #<col> END, asc), (CAST(rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@<id> AS Int32), asc)], dist_by=SinglePartition
    SortExec: [<sort_exprs>], fetch=100
      SortExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          ProjectionExec: expr=[<exprs>]
            BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
              SortExec: expr=[<exprs>]
                CoalesceBatchesExec: target_batch_size=8192
                  RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>
                    ProjectionExec: expr=[<exprs>]
                      AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)]
                        CoalesceBatchesExec: target_batch_size=8192
                          RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>
                            AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)]
                              RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                CoalesceBatchesExec: target_batch_size=8192
                                  HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>]
                                    CoalescePartitionsExec
                                      CoalesceBatchesExec: target_batch_size=8192
                                        HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>]
                                          ProjectionExec: expr=[<exprs>]
                                            DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                          ProjectionExec: expr=[<exprs>]
                                            CoalesceBatchesExec: target_batch_size=8192
                                              FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>]
                                                RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                                  DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                    ProjectionExec: expr=[<exprs>]
                                      DataSourceExec: partitions=1, partition_sizes=[<sizes>]


physical_plan after aggregate_statistics:
SAME TEXT AS ABOVE

physical_plan after join_selection:
ProjectionExec: expr=[<exprs>]
  OutputRequirementExec: order_by=[(__common_expr_1@<id>, desc), (CASE WHEN __common_expr_1@<id> = 0 THEN #<col> END, asc), (CAST(rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@<id> AS Int32), asc)], dist_by=SinglePartition
    SortExec: [<sort_exprs>], fetch=100
      SortExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          ProjectionExec: expr=[<exprs>]
            BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
              SortExec: expr=[<exprs>]
                CoalesceBatchesExec: target_batch_size=8192
                  RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>
                    ProjectionExec: expr=[<exprs>]
                      AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)]
                        CoalesceBatchesExec: target_batch_size=8192
                          RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>
                            AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)]
                              RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                CoalesceBatchesExec: target_batch_size=8192
                                  HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>]
                                    ProjectionExec: expr=[<exprs>]
                                      DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                    CoalescePartitionsExec
                                      CoalesceBatchesExec: target_batch_size=8192
                                        HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>]
                                          ProjectionExec: expr=[<exprs>]
                                            DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                          ProjectionExec: expr=[<exprs>]
                                            CoalesceBatchesExec: target_batch_size=8192
                                              FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>]
                                                RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                                  DataSourceExec: partitions=1, partition_sizes=[<sizes>]


physical_plan after LimitedDistinctAggregation:
SAME TEXT AS ABOVE

physical_plan after FilterPushdown:
SAME TEXT AS ABOVE

physical_plan after EnforceDistribution:
ProjectionExec: expr=[<exprs>]
  OutputRequirementExec: order_by=[(__common_expr_1@<id>, desc), (CASE WHEN __common_expr_1@<id> = 0 THEN #<col> END, asc), (CAST(rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@<id> AS Int32), asc)], dist_by=SinglePartition
    SortExec: expr=[<exprs>]
      CoalescePartitionsExec
        SortExec: expr=[<exprs>]
          ProjectionExec: expr=[<exprs>]
            ProjectionExec: expr=[<exprs>]
              BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
                SortExec: expr=[<exprs>]
                  RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>
                    SortExec: expr=[<exprs>]
                      CoalesceBatchesExec: target_batch_size=8192
                        ProjectionExec: expr=[<exprs>]
                          AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)]
                            RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>
                              CoalesceBatchesExec: target_batch_size=8192
                                AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)]
                                  CoalesceBatchesExec: target_batch_size=8192
                                    HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>]
                                      ProjectionExec: expr=[<exprs>]
                                        DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                      CoalesceBatchesExec: target_batch_size=8192
                                        HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>]
                                          ProjectionExec: expr=[<exprs>]
                                            DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                          ProjectionExec: expr=[<exprs>]
                                            CoalesceBatchesExec: target_batch_size=8192
                                              FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>]
                                                RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                                  DataSourceExec: partitions=1, partition_sizes=[<sizes>]


physical_plan after CombinePartialFinalAggregate:
SAME TEXT AS ABOVE

physical_plan after EnforceSorting:
ProjectionExec: expr=[<exprs>]
  OutputRequirementExec: order_by=[(__common_expr_1@<id>, desc), (CASE WHEN __common_expr_1@<id> = 0 THEN #<col> END, asc), (CAST(rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@<id> AS Int32), asc)], dist_by=SinglePartition
    SortExec: [<sort_exprs>], fetch=100
      SortExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          ProjectionExec: expr=[<exprs>]
            BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
              SortExec: expr=[<exprs>]
                RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>
                  CoalesceBatchesExec: target_batch_size=8192
                    ProjectionExec: expr=[<exprs>]
                      AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)]
                        RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>
                          CoalesceBatchesExec: target_batch_size=8192
                            AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)]
                              CoalesceBatchesExec: target_batch_size=8192
                                HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>]
                                  ProjectionExec: expr=[<exprs>]
                                    DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                  CoalesceBatchesExec: target_batch_size=8192
                                    HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>]
                                      ProjectionExec: expr=[<exprs>]
                                        DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                      ProjectionExec: expr=[<exprs>]
                                        CoalesceBatchesExec: target_batch_size=8192
                                          FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>]
                                            RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                              DataSourceExec: partitions=1, partition_sizes=[<sizes>]


physical_plan after OptimizeAggregateOrder:
SAME TEXT AS ABOVE

physical_plan after ProjectionPushdown:
SAME TEXT AS ABOVE

physical_plan after coalesce_batches:
ProjectionExec: expr=[<exprs>]
  OutputRequirementExec: order_by=[(__common_expr_1@<id>, desc), (CASE WHEN __common_expr_1@<id> = 0 THEN #<col> END, asc), (CAST(rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@<id> AS Int32), asc)], dist_by=SinglePartition
    SortExec: [<sort_exprs>], fetch=100
      SortExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          ProjectionExec: expr=[<exprs>]
            BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
              SortExec: expr=[<exprs>]
                CoalesceBatchesExec: target_batch_size=8192
                  RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>
                    CoalesceBatchesExec: target_batch_size=8192
                      ProjectionExec: expr=[<exprs>]
                        AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)]
                          CoalesceBatchesExec: target_batch_size=8192
                            RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>
                              CoalesceBatchesExec: target_batch_size=8192
                                AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)]
                                  CoalesceBatchesExec: target_batch_size=8192
                                    CoalesceBatchesExec: target_batch_size=8192
                                      HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>]
                                        ProjectionExec: expr=[<exprs>]
                                          DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                        CoalesceBatchesExec: target_batch_size=8192
                                          CoalesceBatchesExec: target_batch_size=8192
                                            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>]
                                              ProjectionExec: expr=[<exprs>]
                                                DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                              ProjectionExec: expr=[<exprs>]
                                                CoalesceBatchesExec: target_batch_size=8192
                                                  CoalesceBatchesExec: target_batch_size=8192
                                                    FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>]
                                                      RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                                        DataSourceExec: partitions=1, partition_sizes=[<sizes>]


physical_plan after coalesce_async_exec_input:
SAME TEXT AS ABOVE

physical_plan after OutputRequirements:
ProjectionExec: expr=[<exprs>]
  SortExec: [<sort_exprs>], fetch=100
    SortExec: expr=[<exprs>]
      ProjectionExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
            SortExec: expr=[<exprs>]
              CoalesceBatchesExec: target_batch_size=8192
                RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>
                  CoalesceBatchesExec: target_batch_size=8192
                    ProjectionExec: expr=[<exprs>]
                      AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)]
                        CoalesceBatchesExec: target_batch_size=8192
                          RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>
                            CoalesceBatchesExec: target_batch_size=8192
                              AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)]
                                CoalesceBatchesExec: target_batch_size=8192
                                  CoalesceBatchesExec: target_batch_size=8192
                                    HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>]
                                      ProjectionExec: expr=[<exprs>]
                                        DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                      CoalesceBatchesExec: target_batch_size=8192
                                        CoalesceBatchesExec: target_batch_size=8192
                                          HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>]
                                            ProjectionExec: expr=[<exprs>]
                                              DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                            ProjectionExec: expr=[<exprs>]
                                              CoalesceBatchesExec: target_batch_size=8192
                                                CoalesceBatchesExec: target_batch_size=8192
                                                  FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>]
                                                    RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                                      DataSourceExec: partitions=1, partition_sizes=[<sizes>]


physical_plan after LimitAggregation:
SAME TEXT AS ABOVE

physical_plan after LimitPushPastWindows:
SAME TEXT AS ABOVE

physical_plan after LimitPushdown:
SAME TEXT AS ABOVE

physical_plan after ProjectionPushdown:
SAME TEXT AS ABOVE

physical_plan after EnsureCooperative:
SAME TEXT AS ABOVE

physical_plan after FilterPushdown(Post):
SAME TEXT AS ABOVE

physical_plan after RewriteExplicitRepartition:
SAME TEXT AS ABOVE

physical_plan after SanityCheckPlan:
SAME TEXT AS ABOVE

physical_plan:
ProjectionExec: expr=[<exprs>]
  ProjectionExec: expr=[<exprs>]
    SortExec: [<sort_exprs>], fetch=100
      SortExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          ProjectionExec: expr=[<exprs>]
            BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
              SortExec: expr=[<exprs>]
                CoalesceBatchesExec: target_batch_size=8192
                  RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>
                    CoalesceBatchesExec: target_batch_size=8192
                      ProjectionExec: expr=[<exprs>]
                        AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)]
                          CoalesceBatchesExec: target_batch_size=8192
                            RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>
                              CoalesceBatchesExec: target_batch_size=8192
                                AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)]
                                  CoalesceBatchesExec: target_batch_size=8192
                                    CoalesceBatchesExec: target_batch_size=8192
                                      HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>]
                                        ProjectionExec: expr=[<exprs>]
                                          DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                        CoalesceBatchesExec: target_batch_size=8192
                                          CoalesceBatchesExec: target_batch_size=8192
                                            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>]
                                              ProjectionExec: expr=[<exprs>]
                                                DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                              ProjectionExec: expr=[<exprs>]
                                                CoalesceBatchesExec: target_batch_size=8192
                                                  CoalesceBatchesExec: target_batch_size=8192
                                                    FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>]
                                                      RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                                        DataSourceExec: partitions=1, partition_sizes=[<sizes>]


== Physical Plan ==
ProjectionExec: expr=[<exprs>]
  ProjectionExec: expr=[<exprs>]
    SortExec: [<sort_exprs>], fetch=100
      SortExec: expr=[<exprs>]
        ProjectionExec: expr=[<exprs>]
          ProjectionExec: expr=[<exprs>]
            BoundedWindowAggExec: wdw=[rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "rank() PARTITION BY [<cols>] ORDER BY [<cols>] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
              SortExec: expr=[<exprs>]
                CoalesceBatchesExec: target_batch_size=8192
                  RepartitionExec: partitioning=Hash([grouping(item.#<col>)@<id> + grouping(item.#<col>)@<id>, CASE WHEN grouping(item.#<col>)@<id> = 0 THEN #<col> END], <partitions>), input_partitions=<partitions>
                    CoalesceBatchesExec: target_batch_size=8192
                      ProjectionExec: expr=[<exprs>]
                        AggregateExec: mode=FinalPartitioned, gby=[#<col> as #<col>, #<col> as #<col>, __grouping_id@<id> as __grouping_id], aggr=[sum(web_sales.#<col>)]
                          CoalesceBatchesExec: target_batch_size=8192
                            RepartitionExec: partitioning=Hash([#<col>, #<col>, __grouping_id@<id>], <partitions>), input_partitions=<partitions>
                              CoalesceBatchesExec: target_batch_size=8192
                                AggregateExec: mode=Partial, gby=[(NULL as #<col>, NULL as #<col>), (#<col> as #<col>, NULL as #<col>), (#<col> as #<col>, #<col> as #<col>)], aggr=[sum(web_sales.#<col>)]
                                  CoalesceBatchesExec: target_batch_size=8192
                                    CoalesceBatchesExec: target_batch_size=8192
                                      HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, #<col>)], projection=[#<col>, #<col>, #<col>]
                                        ProjectionExec: expr=[<exprs>]
                                          DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                        CoalesceBatchesExec: target_batch_size=8192
                                          CoalesceBatchesExec: target_batch_size=8192
                                            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#<col>, CAST(d1.#<col> AS Float64)@<id>)], projection=[#<col>, #<col>]
                                              ProjectionExec: expr=[<exprs>]
                                                DataSourceExec: partitions=1, partition_sizes=[<sizes>]
                                              ProjectionExec: expr=[<exprs>]
                                                CoalesceBatchesExec: target_batch_size=8192
                                                  CoalesceBatchesExec: target_batch_size=8192
                                                    FilterExec: _3@<id> >= 1205 AND _3@<id> <= 1216, projection=[_0@<id>]
                                                      RepartitionExec: partitioning=RoundRobinBatch(<partitions>), input_partitions=<partitions>
                                                        DataSourceExec: partitions=1, partition_sizes=[<sizes>]