>>> import pyarrow as pa
>>> from pyspark.sql.functions import udf
>>> from pyspark.sql.types import IntegerType, LongType
>>>
>>> @udf(LongType(), useArrow=True)
... def arrow_add_one(x):
...     if x is None:
...         return None
...     return x + 1
...
>>> spark.range(3).select(arrow_add_one("id")).show()
+----------------+
|arrow_add_one(id)|
+-----------------+
|                1|
|                2|
|                3|
+-----------------+

