# serializer version: 1
---
name: "test_correlated_scalar_subquery_in_filter"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10
        Filter: outer_t.#3 = (<subquery>)
          Subquery:
            Projection: max(inner_t.#7) AS #8
              Aggregate: groupBy=[[]], aggr=[[max(inner_t.#7)]]
                Filter: inner_t.#6 = outer_ref(outer_t.#2)
                  SubqueryAlias: inner_t
                    Projection: #4 AS #6, #5 AS #7
                      Projection: column1 AS #4, column2 AS #5
                        Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10
        Projection: outer_t.#2, outer_t.#3
          Filter: outer_t.#3 = __scalar_sq_1.#8
            Left Join:  Filter: __scalar_sq_1.#6 = outer_t.#2
              SubqueryAlias: outer_t
                Projection: #0 AS #2, #1 AS #3
                  Projection: column1 AS #0, column2 AS #1
                    Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
              SubqueryAlias: __scalar_sq_1
                Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
                  Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                    SubqueryAlias: inner_t
                      Projection: #4 AS #6, #5 AS #7
                        Projection: column1 AS #4, column2 AS #5
                          Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10
        Projection: outer_t.#2, outer_t.#3
          Filter: outer_t.#3 = __scalar_sq_1.#8
            Left Join: outer_t.#2 = __scalar_sq_1.#6
              SubqueryAlias: outer_t
                Projection: #0 AS #2, #1 AS #3
                  Projection: column1 AS #0, column2 AS #1
                    Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
              SubqueryAlias: __scalar_sq_1
                Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
                  Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                    SubqueryAlias: inner_t
                      Projection: #4 AS #6, #5 AS #7
                        Projection: column1 AS #4, column2 AS #5
                          Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10
        Projection: outer_t.#2, outer_t.#3
          Filter: outer_t.#3 = __scalar_sq_1.#8
            Inner Join: outer_t.#2 = __scalar_sq_1.#6
              SubqueryAlias: outer_t
                Projection: #0 AS #2, #1 AS #3
                  Projection: column1 AS #0, column2 AS #1
                    Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
              SubqueryAlias: __scalar_sq_1
                Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
                  Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                    SubqueryAlias: inner_t
                      Projection: #4 AS #6, #5 AS #7
                        Projection: column1 AS #4, column2 AS #5
                          Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10
        Projection: outer_t.#2, outer_t.#3
          Inner Join: outer_t.#2 = __scalar_sq_1.#6 Filter: outer_t.#3 = __scalar_sq_1.#8
            SubqueryAlias: outer_t
              Projection: #0 AS #2, #1 AS #3
                Projection: column1 AS #0, column2 AS #1
                  Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
            SubqueryAlias: __scalar_sq_1
              Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
                Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                  SubqueryAlias: inner_t
                    Projection: #4 AS #6, #5 AS #7
                      Projection: column1 AS #4, column2 AS #5
                        Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10
        Projection: outer_t.#2, outer_t.#3
          Inner Join: outer_t.#2 = __scalar_sq_1.#6 Filter: outer_t.#3 = __scalar_sq_1.#8
            SubqueryAlias: outer_t
              Projection: #0 AS #2, #1 AS #3
                Projection: column1 AS #0, column2 AS #1
                  Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
            SubqueryAlias: __scalar_sq_1
              Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
                Projection: inner_t.#6, Boolean(true) AS __always_true, max(inner_t.#7)
                  Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                    SubqueryAlias: inner_t
                      Projection: #4 AS #6, #5 AS #7
                        Projection: column1 AS #4, column2 AS #5
                          Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10
        Inner Join: outer_t.#2 = __scalar_sq_1.#6 Filter: outer_t.#3 = __scalar_sq_1.#8
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10
        Inner Join: outer_t.#2 = __scalar_sq_1.#6, outer_t.#3 = __scalar_sq_1.#8
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10
        Inner Join: outer_t.#2 = __scalar_sq_1.#6, outer_t.#3 = __scalar_sq_1.#8
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    initial_physical_plan:
    SortExec: expr=[#9@0 ASC], preserve_partitioning=[false]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
        HashJoinExec: mode=Auto, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
              AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                  DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    SortExec: expr=[#9@0 ASC], preserve_partitioning=[false], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
        HashJoinExec: mode=Auto, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)], statistics=[Rows=Inexact(4), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:),(Col[3]:)]]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
            DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
              AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
                ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                  DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    SortExec: expr=[#9@0 ASC], preserve_partitioning=[false], schema=[#9:Int32;N, #10:Int32;N]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10], schema=[#9:Int32;N, #10:Int32;N]
        HashJoinExec: mode=Auto, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)], schema=[#2:Int32;N, #3:Int32;N, #8:Int32;N, #6:Int32;N]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
            DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6], schema=[#8:Int32;N, #6:Int32;N]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7):Int32;N]
              AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7)[value]:Int32;N]
                ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], schema=[#6:Int32;N, #7:Int32;N]
                  DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortExec: expr=[#9@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
          HashJoinExec: mode=Auto, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                    DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortExec: expr=[#9@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
          HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                    DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortExec: expr=[#9@0 ASC], preserve_partitioning=[false]
        CoalescePartitionsExec
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceSorting:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortPreservingMergeExec: [#9@0 ASC]
        SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortPreservingMergeExec: [#9@0 ASC]
        SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)], projection=[#2@0, #3@1]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    SortPreservingMergeExec: [#9@0 ASC]
      SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
        ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
          HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)], projection=[#2@0, #3@1]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                  AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                      DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#9@0 as k, #10@1 as v]
      SortPreservingMergeExec: [#9@0 ASC]
        SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)], projection=[#2@0, #3@1]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#9@0 as k, #10@1 as v]
      SortPreservingMergeExec: [#9@0 ASC]
        SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)], projection=[#2@0, #3@1]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#6@0], channels=4)
    placement=Worker
    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
        DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10]
        HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #8@0)], projection=[#2@0, #3@1]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
              StageInputExec: input=0, partitioning=Hash([#6@0], 4)
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Merge)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#9@0 as k, #10@1 as v]
      SortPreservingMergeExec: [#9@0 ASC]
        StageInputExec: input=0, partitioning=Hash([#9@0], 4)
---
name: "test_correlated_scalar_subquery_in_projection"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #9 ASC NULLS FIRST, #10 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, (<subquery>) AS mx AS #11
        Subquery:
          Projection: max(inner_t.#7) AS #8
            Aggregate: groupBy=[[]], aggr=[[max(inner_t.#7)]]
              Filter: inner_t.#6 = outer_ref(outer_t.#2)
                SubqueryAlias: inner_t
                  Projection: #4 AS #6, #5 AS #7
                    Projection: column1 AS #4, column2 AS #5
                      Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
        SubqueryAlias: outer_t
          Projection: #0 AS #2, #1 AS #3
            Projection: column1 AS #0, column2 AS #1
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    Sort: #9 ASC NULLS FIRST, #10 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join:  Filter: __scalar_sq_1.#6 = outer_t.#2
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
              Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: #4 AS #6, #5 AS #7
                    Projection: column1 AS #4, column2 AS #5
                      Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    Sort: #9 ASC NULLS FIRST, #10 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
              Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: #4 AS #6, #5 AS #7
                    Projection: column1 AS #4, column2 AS #5
                      Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    Sort: #9 ASC NULLS FIRST, #10 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
              Projection: inner_t.#6, Boolean(true) AS __always_true, max(inner_t.#7)
                Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                  SubqueryAlias: inner_t
                    Projection: #4 AS #6, #5 AS #7
                      Projection: column1 AS #4, column2 AS #5
                        Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #9 ASC NULLS FIRST, #10 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #9 ASC NULLS FIRST, #10 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    initial_physical_plan:
    SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[false]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
        HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
              AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                  DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[false], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:)]]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:)]]
        HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)], statistics=[Rows=Inexact(4), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:),(Col[3]:)]]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
            DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
              AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
                ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                  DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[false], schema=[#9:Int32;N, #10:Int32;N, #11:Int32;N]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11], schema=[#9:Int32;N, #10:Int32;N, #11:Int32;N]
        HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)], schema=[#2:Int32;N, #3:Int32;N, #8:Int32;N, #6:Int32;N]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
            DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6], schema=[#8:Int32;N, #6:Int32;N]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7):Int32;N]
              AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7)[value]:Int32;N]
                ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], schema=[#6:Int32;N, #7:Int32;N]
                  DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[(#9@0, asc), (#10@1, asc)], dist_by=SinglePartition
      SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
          HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                    DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    OutputRequirementExec: order_by=[(#9@0, asc), (#10@1, asc)], dist_by=SinglePartition
      SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
          HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                    DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[(#9@0, asc), (#10@1, asc)], dist_by=SinglePartition
      SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[false]
        CoalescePartitionsExec
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceSorting:
    OutputRequirementExec: order_by=[(#9@0, asc), (#10@1, asc)], dist_by=SinglePartition
      SortPreservingMergeExec: [#9@0 ASC, #10@1 ASC]
        SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[(#9@0, asc), (#10@1, asc)], dist_by=SinglePartition
      SortPreservingMergeExec: [#9@0 ASC, #10@1 ASC]
        SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    SortPreservingMergeExec: [#9@0 ASC, #10@1 ASC]
      SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[true]
        ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
          HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                  AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                      DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#9@0 as k, #10@1 as v, #11@2 as mx]
      SortPreservingMergeExec: [#9@0 ASC, #10@1 ASC]
        SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#9@0 as k, #10@1 as v, #11@2 as mx]
      SortPreservingMergeExec: [#9@0 ASC, #10@1 ASC]
        SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#2@0], channels=4)
    placement=Worker
    ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
      DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#6@0], channels=4)
    placement=Worker
    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
        DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=Hash(keys=[#6@1], channels=4)
    placement=Worker
    ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
      AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
        StageInputExec: input=0, partitioning=Hash([#6@0], 4)
  
    === stage 3 ===
    inputs=[StageInput(stage=0, mode=Shuffle), StageInput(stage=2, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    SortExec: expr=[#9@0 ASC, #10@1 ASC], preserve_partitioning=[true]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
        HashJoinExec: mode=Partitioned, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
          StageInputExec: input=0, partitioning=Hash([#2@0], 4)
          StageInputExec: input=1, partitioning=Hash([#6@1], 4)
  
    === stage 4 ===
    inputs=[StageInput(stage=3, mode=Merge)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#9@0 as k, #10@1 as v, #11@2 as mx]
      SortPreservingMergeExec: [#9@0 ASC, #10@1 ASC]
        StageInputExec: input=0, partitioning=Hash([#9@0], 4)
---
name: "test_correlated_scalar_subquery_nomatch_result"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, (<subquery>) AS mx AS #11
        Subquery:
          Projection: max(inner_t.#7) AS #8
            Aggregate: groupBy=[[]], aggr=[[max(inner_t.#7)]]
              Filter: inner_t.#6 = outer_ref(outer_t.#2)
                SubqueryAlias: inner_t
                  Projection: #4 AS #6, #5 AS #7
                    Projection: column1 AS #4, column2 AS #5
                      Values: (Int32(1), Int32(4)), (Int32(1), Int32(2))
        SubqueryAlias: outer_t
          Projection: #0 AS #2, #1 AS #3
            Projection: column1 AS #0, column2 AS #1
              Values: (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join:  Filter: __scalar_sq_1.#6 = outer_t.#2
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
              Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: #4 AS #6, #5 AS #7
                    Projection: column1 AS #4, column2 AS #5
                      Values: (Int32(1), Int32(4)), (Int32(1), Int32(2))
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
              Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: #4 AS #6, #5 AS #7
                    Projection: column1 AS #4, column2 AS #5
                      Values: (Int32(1), Int32(4)), (Int32(1), Int32(2))
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6, __always_true
              Projection: inner_t.#6, Boolean(true) AS __always_true, max(inner_t.#7)
                Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                  SubqueryAlias: inner_t
                    Projection: #4 AS #6, #5 AS #7
                      Projection: column1 AS #4, column2 AS #5
                        Values: (Int32(1), Int32(4)), (Int32(1), Int32(2))
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #9 ASC NULLS FIRST
      Projection: outer_t.#2 AS #9, outer_t.#3 AS #10, __scalar_sq_1.#8 AS mx AS #11
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) AS #8, inner_t.#6
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2))
  
    initial_physical_plan:
    SortExec: expr=[#9@0 ASC], preserve_partitioning=[false]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
        HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
              AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                  DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    SortExec: expr=[#9@0 ASC], preserve_partitioning=[false], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:)]]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:)]]
        HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)], statistics=[Rows=Inexact(2), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:),(Col[3]:)]]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
            DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
              AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
                ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                  DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    SortExec: expr=[#9@0 ASC], preserve_partitioning=[false], schema=[#9:Int32;N, #10:Int32;N, #11:Int32;N]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11], schema=[#9:Int32;N, #10:Int32;N, #11:Int32;N]
        HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)], schema=[#2:Int32;N, #3:Int32;N, #8:Int32;N, #6:Int32;N]
          ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
            DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
          ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6], schema=[#8:Int32;N, #6:Int32;N]
            AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7):Int32;N]
              AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7)[value]:Int32;N]
                ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], schema=[#6:Int32;N, #7:Int32;N]
                  DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortExec: expr=[#9@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
          HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                    DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortExec: expr=[#9@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
          HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                    DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortExec: expr=[#9@0 ASC], preserve_partitioning=[false]
        CoalescePartitionsExec
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceSorting:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortPreservingMergeExec: [#9@0 ASC]
        SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[(#9@0, asc)], dist_by=SinglePartition
      SortPreservingMergeExec: [#9@0 ASC]
        SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    SortPreservingMergeExec: [#9@0 ASC]
      SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
        ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
          HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                  AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                      DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#9@0 as k, #10@1 as v, #11@2 as mx]
      SortPreservingMergeExec: [#9@0 ASC]
        SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#9@0 as k, #10@1 as v, #11@2 as mx]
      SortPreservingMergeExec: [#9@0 ASC]
        SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#2@0], channels=4)
    placement=Worker
    ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
      DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#6@0], channels=4)
    placement=Worker
    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
        DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=Hash(keys=[#6@1], channels=4)
    placement=Worker
    ProjectionExec: expr=[max(inner_t.#7)@1 as #8, #6@0 as #6]
      AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
        StageInputExec: input=0, partitioning=Hash([#6@0], 4)
  
    === stage 3 ===
    inputs=[StageInput(stage=0, mode=Shuffle), StageInput(stage=2, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    SortExec: expr=[#9@0 ASC], preserve_partitioning=[true]
      ProjectionExec: expr=[#2@0 as #9, #3@1 as #10, #8@2 as #11]
        HashJoinExec: mode=Partitioned, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #8@2]
          StageInputExec: input=0, partitioning=Hash([#2@0], 4)
          StageInputExec: input=1, partitioning=Hash([#6@1], 4)
  
    === stage 4 ===
    inputs=[StageInput(stage=3, mode=Merge)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#9@0 as k, #10@1 as v, #11@2 as mx]
      SortPreservingMergeExec: [#9@0 ASC]
        StageInputExec: input=0, partitioning=Hash([#9@0], 4)
---
name: "test_correlated_scalar_subquery_with_nested_scalar_in_filter"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #12 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13
        Filter: outer_t.#3 = (<subquery>)
          Subquery:
            Projection: max(inner_t.#7) + (<subquery>) AS #11
              Subquery:
                Projection: min(u.#9) AS #10
                  Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                    SubqueryAlias: u
                      Projection: #8 AS #9
                        Projection: column1 AS #8
                          Values: (Int32(1))
              Aggregate: groupBy=[[]], aggr=[[max(inner_t.#7)]]
                Filter: inner_t.#6 = outer_ref(outer_t.#2)
                  SubqueryAlias: inner_t
                    Projection: #4 AS #6, #5 AS #7
                      Projection: column1 AS #4, column2 AS #5
                        Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(5)), (Int32(2), Int32(1)), (Int32(2), Int32(4))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    Sort: #12 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13
        Projection: outer_t.#2, outer_t.#3
          Filter: outer_t.#3 = __scalar_sq_1.#11
            Left Join:  Filter: __scalar_sq_1.#6 = outer_t.#2
              SubqueryAlias: outer_t
                Projection: #0 AS #2, #1 AS #3
                  Projection: column1 AS #0, column2 AS #1
                    Values: (Int32(1), Int32(2)), (Int32(1), Int32(5)), (Int32(2), Int32(1)), (Int32(2), Int32(4))
              SubqueryAlias: __scalar_sq_1
                Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6, __always_true
                  Subquery:
                    Projection: min(u.#9) AS #10
                      Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                        SubqueryAlias: u
                          Projection: #8 AS #9
                            Projection: column1 AS #8
                              Values: (Int32(1))
                  Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                    SubqueryAlias: inner_t
                      Projection: #4 AS #6, #5 AS #7
                        Projection: column1 AS #4, column2 AS #5
                          Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    Sort: #12 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13
        Projection: outer_t.#2, outer_t.#3
          Filter: outer_t.#3 = __scalar_sq_1.#11
            Left Join: outer_t.#2 = __scalar_sq_1.#6
              SubqueryAlias: outer_t
                Projection: #0 AS #2, #1 AS #3
                  Projection: column1 AS #0, column2 AS #1
                    Values: (Int32(1), Int32(2)), (Int32(1), Int32(5)), (Int32(2), Int32(1)), (Int32(2), Int32(4))
              SubqueryAlias: __scalar_sq_1
                Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6, __always_true
                  Subquery:
                    Projection: min(u.#9) AS #10
                      Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                        SubqueryAlias: u
                          Projection: #8 AS #9
                            Projection: column1 AS #8
                              Values: (Int32(1))
                  Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                    SubqueryAlias: inner_t
                      Projection: #4 AS #6, #5 AS #7
                        Projection: column1 AS #4, column2 AS #5
                          Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    Sort: #12 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13
        Projection: outer_t.#2, outer_t.#3
          Filter: outer_t.#3 = __scalar_sq_1.#11
            Inner Join: outer_t.#2 = __scalar_sq_1.#6
              SubqueryAlias: outer_t
                Projection: #0 AS #2, #1 AS #3
                  Projection: column1 AS #0, column2 AS #1
                    Values: (Int32(1), Int32(2)), (Int32(1), Int32(5)), (Int32(2), Int32(1)), (Int32(2), Int32(4))
              SubqueryAlias: __scalar_sq_1
                Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6, __always_true
                  Subquery:
                    Projection: min(u.#9) AS #10
                      Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                        SubqueryAlias: u
                          Projection: #8 AS #9
                            Projection: column1 AS #8
                              Values: (Int32(1))
                  Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                    SubqueryAlias: inner_t
                      Projection: #4 AS #6, #5 AS #7
                        Projection: column1 AS #4, column2 AS #5
                          Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    Sort: #12 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13
        Projection: outer_t.#2, outer_t.#3
          Inner Join: outer_t.#2 = __scalar_sq_1.#6 Filter: outer_t.#3 = __scalar_sq_1.#11
            SubqueryAlias: outer_t
              Projection: #0 AS #2, #1 AS #3
                Projection: column1 AS #0, column2 AS #1
                  Values: (Int32(1), Int32(2)), (Int32(1), Int32(5)), (Int32(2), Int32(1)), (Int32(2), Int32(4))
            SubqueryAlias: __scalar_sq_1
              Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6, __always_true
                Subquery:
                  Projection: min(u.#9) AS #10
                    Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                      SubqueryAlias: u
                        Projection: #8 AS #9
                          Projection: column1 AS #8
                            Values: (Int32(1))
                Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                  SubqueryAlias: inner_t
                    Projection: #4 AS #6, #5 AS #7
                      Projection: column1 AS #4, column2 AS #5
                        Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    Sort: #12 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13
        Projection: outer_t.#2, outer_t.#3
          Inner Join: outer_t.#2 = __scalar_sq_1.#6 Filter: outer_t.#3 = __scalar_sq_1.#11
            SubqueryAlias: outer_t
              Projection: #0 AS #2, #1 AS #3
                Projection: column1 AS #0, column2 AS #1
                  Values: (Int32(1), Int32(2)), (Int32(1), Int32(5)), (Int32(2), Int32(1)), (Int32(2), Int32(4))
            SubqueryAlias: __scalar_sq_1
              Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6, __always_true
                Subquery:
                  Projection: min(u.#9) AS #10
                    Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                      SubqueryAlias: u
                        Projection: #8 AS #9
                          Projection: column1 AS #8
                            Values: (Int32(1))
                Projection: inner_t.#6, Boolean(true) AS __always_true, max(inner_t.#7)
                  Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                    SubqueryAlias: inner_t
                      Projection: #4 AS #6, #5 AS #7
                        Projection: column1 AS #4, column2 AS #5
                          Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #12 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13
        Inner Join: outer_t.#2 = __scalar_sq_1.#6 Filter: outer_t.#3 = __scalar_sq_1.#11
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(5)), (Int32(2), Int32(1)), (Int32(2), Int32(4))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6
              Subquery:
                Projection: min(u.#9) AS #10
                  Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                    SubqueryAlias: u
                      Projection: column1 AS #9
                        Values: (Int32(1))
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    Sort: #12 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13
        Inner Join: outer_t.#2 = __scalar_sq_1.#6, outer_t.#3 = __scalar_sq_1.#11
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(5)), (Int32(2), Int32(1)), (Int32(2), Int32(4))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6
              Subquery:
                Projection: min(u.#9) AS #10
                  Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                    SubqueryAlias: u
                      Projection: column1 AS #9
                        Values: (Int32(1))
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #12 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13
        Inner Join: outer_t.#2 = __scalar_sq_1.#6, outer_t.#3 = __scalar_sq_1.#11
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(5)), (Int32(2), Int32(1)), (Int32(2), Int32(4))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6
              Subquery:
                Projection: min(u.#9) AS #10
                  Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                    SubqueryAlias: u
                      Projection: column1 AS #9
                        Values: (Int32(1))
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#12@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
          HashJoinExec: mode=Auto, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                    DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(u.#9)@0 as #10]
        AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      SortExec: expr=[#12@0 ASC], preserve_partitioning=[false], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
        ProjectionExec: expr=[#2@0 as #12, #3@1 as #13], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
          HashJoinExec: mode=Auto, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)], statistics=[Rows=Inexact(4), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:),(Col[3]:)]]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
            ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                    DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      ProjectionExec: expr=[min(u.#9)@0 as #10], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #9], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#12:Int32;N, #13:Int32;N]
      SortExec: expr=[#12@0 ASC], preserve_partitioning=[false], schema=[#12:Int32;N, #13:Int32;N]
        ProjectionExec: expr=[#2@0 as #12, #3@1 as #13], schema=[#12:Int32;N, #13:Int32;N]
          HashJoinExec: mode=Auto, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)], schema=[#2:Int32;N, #3:Int32;N, #11:Int32;N, #6:Int32;N]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
            ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6], schema=[#11:Int32;N, #6:Int32;N]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7):Int32;N]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7)[value]:Int32;N]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], schema=[#6:Int32;N, #7:Int32;N]
                    DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
      ProjectionExec: expr=[min(u.#9)@0 as #10], schema=[#10:Int32;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)], schema=[min(u.#9):Int32;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)], schema=[min(u.#9)[value]:Int32;N]
            ProjectionExec: expr=[column1@0 as #9], schema=[#9:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#12@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
            HashJoinExec: mode=Auto, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                      DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)]
              ProjectionExec: expr=[column1@0 as #9]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#12@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                      DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)]
              ProjectionExec: expr=[column1@0 as #9]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#12@0 ASC], preserve_partitioning=[false]
          CoalescePartitionsExec
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
              HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)]
              ProjectionExec: expr=[column1@0 as #9]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#12@0 ASC], preserve_partitioning=[false]
          CoalescePartitionsExec
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
              HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortPreservingMergeExec: [#12@0 ASC]
          SortExec: expr=[#12@0 ASC], preserve_partitioning=[true]
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
              HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortPreservingMergeExec: [#12@0 ASC]
          SortExec: expr=[#12@0 ASC], preserve_partitioning=[true]
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
              HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)], projection=[#2@0, #3@1]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortPreservingMergeExec: [#12@0 ASC]
        SortExec: expr=[#12@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)], projection=[#2@0, #3@1]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(u.#9)@0 as #10]
        AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
          ProjectionExec: expr=[column1@0 as #9]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#12@0 as k, #13@1 as v]
      ScalarSubqueryExec: subqueries=1
        SortPreservingMergeExec: [#12@0 ASC]
          SortExec: expr=[#12@0 ASC], preserve_partitioning=[true]
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
              HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)], projection=[#2@0, #3@1]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#12@0 as k, #13@1 as v]
      ScalarSubqueryExec: subqueries=1
        SortPreservingMergeExec: [#12@0 ASC]
          SortExec: expr=[#12@0 ASC], preserve_partitioning=[true]
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
              HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)], projection=[#2@0, #3@1]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(u.#9)@0 as #10]
      AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
        ProjectionExec: expr=[column1@0 as #9]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#6@0], channels=4)
    placement=Worker
    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
        DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle), StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#12@0 ASC], preserve_partitioning=[true]
        ProjectionExec: expr=[#2@0 as #12, #3@1 as #13]
          HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(#2@0, #6@1), (#3@1, #11@0)], projection=[#2@0, #3@1]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                StageInputExec: input=0, partitioning=Hash([#6@0], 4)
      StageInputExec: input=1, partitioning=UnknownPartitioning(1)
  
    === stage 3 ===
    inputs=[StageInput(stage=2, mode=Merge)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#12@0 as k, #13@1 as v]
      SortPreservingMergeExec: [#12@0 ASC]
        StageInputExec: input=0, partitioning=Hash([#12@0], 4)
---
name: "test_correlated_scalar_subquery_with_nested_scalar_in_projection"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #12 ASC NULLS FIRST, #13 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13, (<subquery>) AS shifted AS #14
        Subquery:
          Projection: max(inner_t.#7) + (<subquery>) AS #11
            Subquery:
              Projection: min(u.#9) AS #10
                Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                  SubqueryAlias: u
                    Projection: #8 AS #9
                      Projection: column1 AS #8
                        Values: (Int32(1))
            Aggregate: groupBy=[[]], aggr=[[max(inner_t.#7)]]
              Filter: inner_t.#6 = outer_ref(outer_t.#2)
                SubqueryAlias: inner_t
                  Projection: #4 AS #6, #5 AS #7
                    Projection: column1 AS #4, column2 AS #5
                      Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
        SubqueryAlias: outer_t
          Projection: #0 AS #2, #1 AS #3
            Projection: column1 AS #0, column2 AS #1
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    Sort: #12 ASC NULLS FIRST, #13 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13, __scalar_sq_1.#11 AS shifted AS #14
        Left Join:  Filter: __scalar_sq_1.#6 = outer_t.#2
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6, __always_true
              Subquery:
                Projection: min(u.#9) AS #10
                  Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                    SubqueryAlias: u
                      Projection: #8 AS #9
                        Projection: column1 AS #8
                          Values: (Int32(1))
              Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: #4 AS #6, #5 AS #7
                    Projection: column1 AS #4, column2 AS #5
                      Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    Sort: #12 ASC NULLS FIRST, #13 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13, __scalar_sq_1.#11 AS shifted AS #14
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6, __always_true
              Subquery:
                Projection: min(u.#9) AS #10
                  Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                    SubqueryAlias: u
                      Projection: #8 AS #9
                        Projection: column1 AS #8
                          Values: (Int32(1))
              Aggregate: groupBy=[[inner_t.#6, Boolean(true) AS __always_true]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: #4 AS #6, #5 AS #7
                    Projection: column1 AS #4, column2 AS #5
                      Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    Sort: #12 ASC NULLS FIRST, #13 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13, __scalar_sq_1.#11 AS shifted AS #14
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6, __always_true
              Subquery:
                Projection: min(u.#9) AS #10
                  Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                    SubqueryAlias: u
                      Projection: #8 AS #9
                        Projection: column1 AS #8
                          Values: (Int32(1))
              Projection: inner_t.#6, Boolean(true) AS __always_true, max(inner_t.#7)
                Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                  SubqueryAlias: inner_t
                    Projection: #4 AS #6, #5 AS #7
                      Projection: column1 AS #4, column2 AS #5
                        Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #12 ASC NULLS FIRST, #13 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13, __scalar_sq_1.#11 AS shifted AS #14
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6
              Subquery:
                Projection: min(u.#9) AS #10
                  Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                    SubqueryAlias: u
                      Projection: column1 AS #9
                        Values: (Int32(1))
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #12 ASC NULLS FIRST, #13 ASC NULLS FIRST
      Projection: outer_t.#2 AS #12, outer_t.#3 AS #13, __scalar_sq_1.#11 AS shifted AS #14
        Left Join: outer_t.#2 = __scalar_sq_1.#6
          SubqueryAlias: outer_t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(4)), (Int32(2), Int32(1)), (Int32(2), Int32(3))
          SubqueryAlias: __scalar_sq_1
            Projection: max(inner_t.#7) + (<subquery>) AS #11, inner_t.#6
              Subquery:
                Projection: min(u.#9) AS #10
                  Aggregate: groupBy=[[]], aggr=[[min(u.#9)]]
                    SubqueryAlias: u
                      Projection: column1 AS #9
                        Values: (Int32(1))
              Aggregate: groupBy=[[inner_t.#6]], aggr=[[max(inner_t.#7)]]
                SubqueryAlias: inner_t
                  Projection: column1 AS #6, column2 AS #7
                    Values: (Int32(1), Int32(4)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(1))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
          HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                    DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(u.#9)@0 as #10]
        AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:)]]
      SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[false], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:)]]
        ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:)]]
          HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)], statistics=[Rows=Inexact(4), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0)),(Col[2]:),(Col[3]:)]]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
            ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                    DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      ProjectionExec: expr=[min(u.#9)@0 as #10], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #9], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#12:Int32;N, #13:Int32;N, #14:Int32;N]
      SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[false], schema=[#12:Int32;N, #13:Int32;N, #14:Int32;N]
        ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14], schema=[#12:Int32;N, #13:Int32;N, #14:Int32;N]
          HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)], schema=[#2:Int32;N, #3:Int32;N, #11:Int32;N, #6:Int32;N]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
            ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6], schema=[#11:Int32;N, #6:Int32;N]
              AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7):Int32;N]
                AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)], schema=[#6:Int32;N, max(inner_t.#7)[value]:Int32;N]
                  ProjectionExec: expr=[column1@0 as #6, column2@1 as #7], schema=[#6:Int32;N, #7:Int32;N]
                    DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
      ProjectionExec: expr=[min(u.#9)@0 as #10], schema=[#10:Int32;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)], schema=[min(u.#9):Int32;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)], schema=[min(u.#9)[value]:Int32;N]
            ProjectionExec: expr=[column1@0 as #9], schema=[#9:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
            HashJoinExec: mode=Auto, join_type=Left, on=[(#2@0, #6@1)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                      DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)]
              ProjectionExec: expr=[column1@0 as #9]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                      DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)]
              ProjectionExec: expr=[column1@0 as #9]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[false]
          CoalescePartitionsExec
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
              HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Final, gby=[], aggr=[min(u.#9)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(u.#9)]
              ProjectionExec: expr=[column1@0 as #9]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[false]
          CoalescePartitionsExec
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
              HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortPreservingMergeExec: [#12@0 ASC, #13@1 ASC]
          SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[true]
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
              HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortPreservingMergeExec: [#12@0 ASC, #13@1 ASC]
          SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[true]
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
              HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #11@2]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortPreservingMergeExec: [#12@0 ASC, #13@1 ASC]
        SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
            HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #11@2]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                  RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                        DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(u.#9)@0 as #10]
        AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
          ProjectionExec: expr=[column1@0 as #9]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#12@0 as k, #13@1 as v, #14@2 as shifted]
      ScalarSubqueryExec: subqueries=1
        SortPreservingMergeExec: [#12@0 ASC, #13@1 ASC]
          SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[true]
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
              HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #11@2]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#12@0 as k, #13@1 as v, #14@2 as shifted]
      ScalarSubqueryExec: subqueries=1
        SortPreservingMergeExec: [#12@0 ASC, #13@1 ASC]
          SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[true]
            ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
              HashJoinExec: mode=CollectLeft, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #11@2]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
                  AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                    RepartitionExec: partitioning=Hash([#6@0], 4), input_partitions=1
                      AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
                        ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
                          DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#9)@0 as #10]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
            ProjectionExec: expr=[column1@0 as #9]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(u.#9)@0 as #10]
      AggregateExec: mode=Single, gby=[], aggr=[min(u.#9)]
        ProjectionExec: expr=[column1@0 as #9]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#2@0], channels=4)
    placement=Worker
    ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
      DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 2 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#6@0], channels=4)
    placement=Worker
    AggregateExec: mode=Partial, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
      ProjectionExec: expr=[column1@0 as #6, column2@1 as #7]
        DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 3 ===
    inputs=[StageInput(stage=2, mode=Shuffle), StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=4
    distribution=Hash(keys=[#6@1], channels=4)
    placement=Worker
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[max(inner_t.#7)@1 + scalar_subquery(<pending>) as #11, #6@0 as #6]
        AggregateExec: mode=FinalPartitioned, gby=[#6@0 as #6], aggr=[max(inner_t.#7)]
          StageInputExec: input=0, partitioning=Hash([#6@0], 4)
      StageInputExec: input=1, partitioning=UnknownPartitioning(1)
  
    === stage 4 ===
    inputs=[StageInput(stage=1, mode=Shuffle), StageInput(stage=3, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    SortExec: expr=[#12@0 ASC, #13@1 ASC], preserve_partitioning=[true]
      ProjectionExec: expr=[#2@0 as #12, #3@1 as #13, #11@2 as #14]
        HashJoinExec: mode=Partitioned, join_type=Left, on=[(#2@0, #6@1)], projection=[#2@0, #3@1, #11@2]
          StageInputExec: input=0, partitioning=Hash([#2@0], 4)
          StageInputExec: input=1, partitioning=Hash([#6@1], 4)
  
    === stage 5 ===
    inputs=[StageInput(stage=4, mode=Merge)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#12@0 as k, #13@1 as v, #14@2 as shifted]
      SortPreservingMergeExec: [#12@0 ASC, #13@1 ASC]
        StageInputExec: input=0, partitioning=Hash([#12@0], 4)
---
name: "test_empty_scalar_subquery_result"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #5 ASC NULLS FIRST
      Projection: t.#1 AS #5, CAST(t.#1 AS Int64) + (<subquery>) AS shifted AS #6
        Subquery:
          Projection: CAST(s.#3 AS Int64) AS #4
            Filter: s.#3 > Int32(100)
              SubqueryAlias: s
                Projection: #2 AS #3
                  Projection: column1 AS #2
                    Values: (Int32(10))
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    Sort: #5 ASC NULLS FIRST
      Projection: t.#1 AS #5, CAST(t.#1 AS Int64) + (<subquery>) AS shifted AS #6
        Subquery:
          Projection: CAST(s.#3 AS Int64) AS #4
            SubqueryAlias: s
              Projection: #2 AS #3
                Projection: column1 AS #2
                  Filter: column1 > Int32(100)
                    Values: (Int32(10))
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2))
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #5 ASC NULLS FIRST
      Projection: t.#1 AS #5, CAST(t.#1 AS Int64) + (<subquery>) AS shifted AS #6
        Subquery:
          Projection: CAST(s.#3 AS Int64) AS #4
            SubqueryAlias: s
              Projection: column1 AS #3
                Filter: column1 > Int32(100)
                  Values: (Int32(10))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #5 ASC NULLS FIRST
      Projection: t.#1 AS #5, CAST(t.#1 AS Int64) + (<subquery>) AS shifted AS #6
        Subquery:
          Projection: CAST(s.#3 AS Int64) AS #4
            SubqueryAlias: s
              Projection: column1 AS #3
                Filter: column1 > Int32(100)
                  Values: (Int32(10))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6]
          ProjectionExec: expr=[column1@0 as #1]
            DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4]
        ProjectionExec: expr=[column1@0 as #3]
          FilterExec: column1@0 > 100
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
      SortExec: expr=[#5@0 ASC], preserve_partitioning=[false], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
        ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
          ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
            DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4], statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]: Min=Inexact(Int64(101)) Null=Inexact(0))]]
        ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]: Min=Inexact(Int32(101)) Null=Inexact(0))]]
          FilterExec: column1@0 > 100, statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]: Min=Inexact(Int32(101)) Null=Inexact(0))]]
            DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#5:Int32;N, #6:Int64;N]
      SortExec: expr=[#5@0 ASC], preserve_partitioning=[false], schema=[#5:Int32;N, #6:Int64;N]
        ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6], schema=[#5:Int32;N, #6:Int64;N]
          ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
            DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4], schema=[#4:Int64;N]
        ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
          FilterExec: column1@0 > 100, schema=[column1:Int32;N]
            DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4]
          ProjectionExec: expr=[column1@0 as #3]
            FilterExec: column1@0 > 100
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4]
          RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
            ProjectionExec: expr=[column1@0 as #3]
              FilterExec: column1@0 > 100
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4]
          RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
            ProjectionExec: expr=[column1@0 as #3]
              FilterExec: column1@0 > 100
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
          DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4]
        RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
          ProjectionExec: expr=[column1@0 as #3]
            FilterExec: column1@0 > 100
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#5@0 as v, #6@1 as shifted]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4]
          RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
            ProjectionExec: expr=[column1@0 as #3]
              FilterExec: column1@0 > 100
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#5@0 as v, #6@1 as shifted]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4]
          RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
            ProjectionExec: expr=[column1@0 as #3]
              FilterExec: column1@0 > 100
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=4)
    placement=Worker
    ProjectionExec: expr=[column1@0 as #3]
      FilterExec: column1@0 > 100
        DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[CAST(#3@0 AS Int64) as #4]
      StageInputExec: input=0, partitioning=RoundRobinBatch(4)
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#5@0 as v, #6@1 as shifted]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_multiple_scalar_subqueries_in_one_expression"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Projection: sum(t.#1 + #4 + #7) AS total AS #8
      Aggregate: groupBy=[[]], aggr=[[sum(CAST(t.#1 AS Int64) + (<subquery>) + (<subquery>))]]
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: #2 AS #3
                  Projection: column1 AS #2
                    Values: (Int32(10)), (Int32(20))
        Subquery:
          Projection: max(u.#6) AS #7
            Aggregate: groupBy=[[]], aggr=[[max(CAST(u.#6 AS Int64))]]
              SubqueryAlias: u
                Projection: #5 AS #6
                  Projection: column1 AS #5
                    Values: (Int32(1)), (Int32(2))
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Projection: sum(t.#1 + #4 + #7) AS total AS #8
      Aggregate: groupBy=[[]], aggr=[[sum(CAST(t.#1 AS Int64) + (<subquery>) + (<subquery>))]]
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(10)), (Int32(20))
        Subquery:
          Projection: max(u.#6) AS #7
            Aggregate: groupBy=[[]], aggr=[[max(CAST(u.#6 AS Int64))]]
              SubqueryAlias: u
                Projection: column1 AS #6
                  Values: (Int32(1)), (Int32(2))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Projection: sum(t.#1 + #4 + #7) AS total AS #8
      Aggregate: groupBy=[[]], aggr=[[sum(CAST(t.#1 AS Int64) + (<subquery>) + (<subquery>))]]
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(10)), (Int32(20))
        Subquery:
          Projection: max(u.#6) AS #7
            Aggregate: groupBy=[[]], aggr=[[max(CAST(u.#6 AS Int64))]]
              SubqueryAlias: u
                Projection: column1 AS #6
                  Values: (Int32(1)), (Int32(2))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=2
      ProjectionExec: expr=[sum(t.#1 + #4 + #7)@0 as #8]
        AggregateExec: mode=Final, gby=[], aggr=[sum(t.#1 + #4 + #7)]
          AggregateExec: mode=Partial, gby=[], aggr=[sum(t.#1 + #4 + #7)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[max(u.#6)@0 as #7]
        AggregateExec: mode=Final, gby=[], aggr=[max(u.#6)]
          AggregateExec: mode=Partial, gby=[], aggr=[max(u.#6)]
            ProjectionExec: expr=[column1@0 as #6]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=2, statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
      ProjectionExec: expr=[sum(t.#1 + #4 + #7)@0 as #8], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[sum(t.#1 + #4 + #7)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[sum(t.#1 + #4 + #7)], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#3)@0 as #4], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[max(u.#6)@0 as #7], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[max(u.#6)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[max(u.#6)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #6], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=2, schema=[#8:Int64;N]
      ProjectionExec: expr=[sum(t.#1 + #4 + #7)@0 as #8], schema=[#8:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[sum(t.#1 + #4 + #7)], schema=[sum(t.#1 + #4 + #7):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[sum(t.#1 + #4 + #7)], schema=[sum(t.#1 + #4 + #7)[sum]:Int64;N]
            ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#3)@0 as #4], schema=[#4:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], schema=[min(s.#3):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], schema=[min(s.#3)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[max(u.#6)@0 as #7], schema=[#7:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[max(u.#6)], schema=[max(u.#6):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[max(u.#6)], schema=[max(u.#6)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #6], schema=[#6:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=2
        ProjectionExec: expr=[sum(t.#1 + #4 + #7)@0 as #8]
          AggregateExec: mode=Final, gby=[], aggr=[sum(t.#1 + #4 + #7)]
            AggregateExec: mode=Partial, gby=[], aggr=[sum(t.#1 + #4 + #7)]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[max(u.#6)@0 as #7]
          AggregateExec: mode=Final, gby=[], aggr=[max(u.#6)]
            AggregateExec: mode=Partial, gby=[], aggr=[max(u.#6)]
              ProjectionExec: expr=[column1@0 as #6]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=2
        ProjectionExec: expr=[sum(t.#1 + #4 + #7)@0 as #8]
          AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4 + #7)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[max(u.#6)@0 as #7]
          AggregateExec: mode=Single, gby=[], aggr=[max(u.#6)]
            ProjectionExec: expr=[column1@0 as #6]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=2
      ProjectionExec: expr=[sum(t.#1 + #4 + #7)@0 as #8]
        AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4 + #7)]
          ProjectionExec: expr=[column1@0 as #1]
            DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
          ProjectionExec: expr=[column1@0 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[max(u.#6)@0 as #7]
        AggregateExec: mode=Single, gby=[], aggr=[max(u.#6)]
          ProjectionExec: expr=[column1@0 as #6]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#8@0 as total]
      ScalarSubqueryExec: subqueries=2
        ProjectionExec: expr=[sum(t.#1 + #4 + #7)@0 as #8]
          AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4 + #7)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[max(u.#6)@0 as #7]
          AggregateExec: mode=Single, gby=[], aggr=[max(u.#6)]
            ProjectionExec: expr=[column1@0 as #6]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#8@0 as total]
      ScalarSubqueryExec: subqueries=2
        ProjectionExec: expr=[sum(t.#1 + #4 + #7)@0 as #8]
          AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4 + #7)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[max(u.#6)@0 as #7]
          AggregateExec: mode=Single, gby=[], aggr=[max(u.#6)]
            ProjectionExec: expr=[column1@0 as #6]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#3)@0 as #4]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
        ProjectionExec: expr=[column1@0 as #3]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[max(u.#6)@0 as #7]
      AggregateExec: mode=Single, gby=[], aggr=[max(u.#6)]
        ProjectionExec: expr=[column1@0 as #6]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 2 ===
    inputs=[StageInput(stage=0, mode=Broadcast), StageInput(stage=1, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#8@0 as total]
      ScalarSubqueryExec: subqueries=2
        ProjectionExec: expr=[sum(t.#1 + #4 + #7)@0 as #8]
          AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4 + #7)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
        StageInputExec: input=1, partitioning=UnknownPartitioning(1)
---
name: "test_nested_scalar_subquery"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #8 ASC NULLS FIRST
      Projection: t.#1 AS #8, CAST(t.#1 AS Int64) + (<subquery>) AS shifted AS #9
        Subquery:
          Projection: min(s.#3) + (<subquery>) AS #7
            Subquery:
              Projection: min(u.#5) AS #6
                Aggregate: groupBy=[[]], aggr=[[min(u.#5)]]
                  SubqueryAlias: u
                    Projection: #4 AS #5
                      Projection: column1 AS #4
                        Values: (Int32(1))
            Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
              SubqueryAlias: s
                Projection: #2 AS #3
                  Projection: column1 AS #2
                    Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    Sort: #8 ASC NULLS FIRST
      Projection: t.#1 AS #8, CAST(t.#1 AS Int64) + (<subquery>) AS shifted AS #9
        Subquery:
          Projection: CAST(min(s.#3) + (<subquery>) AS #7 AS Int64)
            Subquery:
              Projection: min(u.#5) AS #6
                Aggregate: groupBy=[[]], aggr=[[min(u.#5)]]
                  SubqueryAlias: u
                    Projection: #4 AS #5
                      Projection: column1 AS #4
                        Values: (Int32(1))
            Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
              SubqueryAlias: s
                Projection: #2 AS #3
                  Projection: column1 AS #2
                    Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2))
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #8 ASC NULLS FIRST
      Projection: t.#1 AS #8, CAST(t.#1 AS Int64) + (<subquery>) AS shifted AS #9
        Subquery:
          Projection: CAST(min(s.#3) + (<subquery>) AS #7 AS Int64)
            Subquery:
              Projection: min(u.#5) AS #6
                Aggregate: groupBy=[[]], aggr=[[min(u.#5)]]
                  SubqueryAlias: u
                    Projection: column1 AS #5
                      Values: (Int32(1))
            Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #8 ASC NULLS FIRST
      Projection: t.#1 AS #8, CAST(t.#1 AS Int64) + (<subquery>) AS shifted AS #9
        Subquery:
          Projection: CAST(min(s.#3) + (<subquery>) AS #7 AS Int64)
            Subquery:
              Projection: min(u.#5) AS #6
                Aggregate: groupBy=[[]], aggr=[[min(u.#5)]]
                  SubqueryAlias: u
                    Projection: column1 AS #5
                      Values: (Int32(1))
            Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#8@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #8, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #9]
          ProjectionExec: expr=[column1@0 as #1]
            DataSourceExec: partitions=1, partition_sizes=[1]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(u.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(u.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
      SortExec: expr=[#8@0 ASC], preserve_partitioning=[false], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
        ProjectionExec: expr=[#1@0 as #8, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #9], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
          ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
            DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
              ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
                DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
        ProjectionExec: expr=[min(u.#5)@0 as #6], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Final, gby=[], aggr=[min(u.#5)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
            AggregateExec: mode=Partial, gby=[], aggr=[min(u.#5)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
              ProjectionExec: expr=[column1@0 as #5], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
                DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#8:Int32;N, #9:Int64;N]
      SortExec: expr=[#8@0 ASC], preserve_partitioning=[false], schema=[#8:Int32;N, #9:Int64;N]
        ProjectionExec: expr=[#1@0 as #8, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #9], schema=[#8:Int32;N, #9:Int64;N]
          ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
            DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ScalarSubqueryExec: subqueries=1, schema=[#7:Int64;N]
        ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7], schema=[#7:Int64;N]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], schema=[min(s.#3):Int32;N]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], schema=[min(s.#3)[value]:Int32;N]
              ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
                DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
        ProjectionExec: expr=[min(u.#5)@0 as #6], schema=[#6:Int32;N]
          AggregateExec: mode=Final, gby=[], aggr=[min(u.#5)], schema=[min(u.#5):Int32;N]
            AggregateExec: mode=Partial, gby=[], aggr=[min(u.#5)], schema=[min(u.#5)[value]:Int32;N]
              ProjectionExec: expr=[column1@0 as #5], schema=[#5:Int32;N]
                DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#8@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #8, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #9]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ScalarSubqueryExec: subqueries=1
          ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7]
            AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
              AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
                ProjectionExec: expr=[column1@0 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
          ProjectionExec: expr=[min(u.#5)@0 as #6]
            AggregateExec: mode=Final, gby=[], aggr=[min(u.#5)]
              AggregateExec: mode=Partial, gby=[], aggr=[min(u.#5)]
                ProjectionExec: expr=[column1@0 as #5]
                  DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#8@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #8, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #9]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ScalarSubqueryExec: subqueries=1
          ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7]
            AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
          ProjectionExec: expr=[min(u.#5)@0 as #6]
            AggregateExec: mode=Single, gby=[], aggr=[min(u.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#8@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #8, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #9]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ScalarSubqueryExec: subqueries=1
          ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7]
            AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
          ProjectionExec: expr=[min(u.#5)@0 as #6]
            AggregateExec: mode=Single, gby=[], aggr=[min(u.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#8@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[column1@0 as #8, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #9]
          DataSourceExec: partitions=1, partition_sizes=[1]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(u.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(u.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#8@0 as v, #9@1 as shifted]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#8@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #8, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #9]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ScalarSubqueryExec: subqueries=1
          ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7]
            AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
          ProjectionExec: expr=[min(u.#5)@0 as #6]
            AggregateExec: mode=Single, gby=[], aggr=[min(u.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#8@0 as v, #9@1 as shifted]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#8@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #8, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #9]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ScalarSubqueryExec: subqueries=1
          ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7]
            AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
          ProjectionExec: expr=[min(u.#5)@0 as #6]
            AggregateExec: mode=Single, gby=[], aggr=[min(u.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(u.#5)@0 as #6]
      AggregateExec: mode=Single, gby=[], aggr=[min(u.#5)]
        ProjectionExec: expr=[column1@0 as #5]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[CAST(min(s.#3)@0 + scalar_subquery(<pending>) AS Int64) as #7]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
          ProjectionExec: expr=[column1@0 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
      StageInputExec: input=0, partitioning=UnknownPartitioning(1)
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#8@0 as v, #9@1 as shifted]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#8@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #8, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #9]
            DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_null_scalar_subquery_result"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #5 ASC NULLS FIRST
      Projection: t.#1 AS #5, CAST(t.#1 AS Int64) + (<subquery>) AS maybe_shifted AS #6
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: #2 AS #3
                  Projection: column1 AS #2
                    Values: (CAST(NULL AS Int32))
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    Sort: #5 ASC NULLS FIRST
      Projection: t.#1 AS #5, CAST(t.#1 AS Int64) + (<subquery>) AS maybe_shifted AS #6
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: #2 AS #3
                  Projection: column1 AS #2
                    Values: (Int32(NULL) AS NULL)
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2))
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #5 ASC NULLS FIRST
      Projection: t.#1 AS #5, CAST(t.#1 AS Int64) + (<subquery>) AS maybe_shifted AS #6
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(NULL) AS NULL)
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #5 ASC NULLS FIRST
      Projection: t.#1 AS #5, CAST(t.#1 AS Int64) + (<subquery>) AS maybe_shifted AS #6
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(NULL) AS NULL)
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6]
          ProjectionExec: expr=[column1@0 as #1]
            DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
      SortExec: expr=[#5@0 ASC], preserve_partitioning=[false], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
        ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
          ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
            DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#3)@0 as #4], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(1))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(1))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#5:Int32;N, #6:Int64;N]
      SortExec: expr=[#5@0 ASC], preserve_partitioning=[false], schema=[#5:Int32;N, #6:Int64;N]
        ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6], schema=[#5:Int32;N, #6:Int64;N]
          ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
            DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#3)@0 as #4], schema=[#4:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], schema=[min(s.#3):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], schema=[min(s.#3)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #5, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
          DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
          ProjectionExec: expr=[column1@0 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#5@0 as v, #6@1 as maybe_shifted]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#5@0 as v, #6@1 as maybe_shifted]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#3)@0 as #4]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
        ProjectionExec: expr=[column1@0 as #3]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#5@0 as v, #6@1 as maybe_shifted]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#5@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #5, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as #6]
            DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_aggregate_filter"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Projection: t.#2 AS #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>)) AS n AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]]
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: #4 AS #5
                  Projection: column1 AS #4
                    Values: (Int32(1))
        SubqueryAlias: t
          Projection: #0 AS #2, #1 AS #3
            Projection: column1 AS #0, column2 AS #1
              Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Projection: t.#2 AS #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>)) AS n AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]]
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: column1 AS #5
                  Values: (Int32(1))
        SubqueryAlias: t
          Projection: column1 AS #2, column2 AS #3
            Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Projection: t.#2 AS #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>)) AS n AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]]
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: column1 AS #5
                  Values: (Int32(1))
        SubqueryAlias: t
          Projection: column1 AS #2, column2 AS #3
            Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
          AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
      ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
          AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#5)@0 as #6], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #5], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#7:Int32;N, #8:Int64]
      ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8], schema=[#7:Int32;N, #8:Int64]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))], schema=[#2:Int32;N, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>)):Int64]
          AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))], schema=[#2:Int32;N, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))[count]:Int64]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
      ProjectionExec: expr=[min(s.#5)@0 as #6], schema=[#6:Int32;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], schema=[min(s.#5):Int32;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], schema=[min(s.#5)[value]:Int32;N]
            ProjectionExec: expr=[column1@0 as #5], schema=[#5:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
            AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
          RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
            AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
          ProjectionExec: expr=[column1@0 as #5]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#7@0 as k, #8@1 as n]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#7@0 as k, #8@1 as n]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#5)@0 as #6]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
        ProjectionExec: expr=[column1@0 as #5]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#2@0], channels=4)
    placement=Worker
    ScalarSubqueryExec: subqueries=1
      AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
        ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
          DataSourceExec: partitions=1, partition_sizes=[1]
      StageInputExec: input=0, partitioning=UnknownPartitioning(1)
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle), StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#7@0 as k, #8@1 as n]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[count(Int64(1)) FILTER (WHERE t.#3 > (<subquery>))]
            StageInputExec: input=0, partitioning=Hash([#2@0], 4)
        StageInputExec: input=1, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_aggregate_order_by"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Projection: CASE WHEN array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST], Utf8(",")) IS NOT NULL THEN array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST], Utf8(",")) WHEN Boolean(true) THEN NULL END AS joined AS #7
      Aggregate: groupBy=[[]], aggr=[[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]]
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: #4 AS #5
                  Projection: column1 AS #4
                    Values: (Int32(1))
        SubqueryAlias: t
          Projection: #0 AS #2, #1 AS #3
            Projection: column1 AS #0, column2 AS #1
              Values: (Utf8("a"), Int32(2)), (Utf8("b"), Int32(1)), (Utf8("c"), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    Projection: CASE WHEN array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST], Utf8(",")) IS NOT NULL THEN array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST], Utf8(",")) WHEN Boolean(true) THEN CAST(NULL AS Utf8) END AS joined AS #7
      Aggregate: groupBy=[[]], aggr=[[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]]
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: #4 AS #5
                  Projection: column1 AS #4
                    Values: (Int32(1))
        SubqueryAlias: t
          Projection: #0 AS #2, #1 AS #3
            Projection: column1 AS #0, column2 AS #1
              Values: (Utf8("a"), Int32(2)), (Utf8("b"), Int32(1)), (Utf8("c"), Int32(3))
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    Projection: CASE WHEN array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST], Utf8(",")) IS NOT NULL THEN array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST], Utf8(",")) ELSE Utf8(NULL) END AS joined AS #7
      Aggregate: groupBy=[[]], aggr=[[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] AS array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]]
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: #4 AS #5
                  Projection: column1 AS #4
                    Values: (Int32(1))
        SubqueryAlias: t
          Projection: #0 AS #2, #1 AS #3
            Projection: column1 AS #0, column2 AS #1
              Values: (Utf8("a"), Int32(2)), (Utf8("b"), Int32(1)), (Utf8("c"), Int32(3))
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    Projection: CASE WHEN __common_expr_1 IS NOT NULL THEN __common_expr_1 ELSE Utf8(NULL) END AS joined AS #7
      Projection: array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST], Utf8(",")) AS __common_expr_1, array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]
        Aggregate: groupBy=[[]], aggr=[[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] AS array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]]
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
                SubqueryAlias: s
                  Projection: #4 AS #5
                    Projection: column1 AS #4
                      Values: (Int32(1))
          SubqueryAlias: t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Utf8("a"), Int32(2)), (Utf8("b"), Int32(1)), (Utf8("c"), Int32(3))
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Projection: CASE WHEN __common_expr_1 IS NOT NULL THEN __common_expr_1 ELSE Utf8(NULL) END AS joined AS #7
      Projection: array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST], Utf8(",")) AS __common_expr_1
        Aggregate: groupBy=[[]], aggr=[[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] AS array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]]
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
                SubqueryAlias: s
                  Projection: column1 AS #5
                    Values: (Int32(1))
          SubqueryAlias: t
            Projection: column1 AS #2, column2 AS #3
              Values: (Utf8("a"), Int32(2)), (Utf8("b"), Int32(1)), (Utf8("c"), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Projection: CASE WHEN __common_expr_1 IS NOT NULL THEN __common_expr_1 ELSE Utf8(NULL) END AS joined AS #7
      Projection: array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST], Utf8(",")) AS __common_expr_1
        Aggregate: groupBy=[[]], aggr=[[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] AS array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]]
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
                SubqueryAlias: s
                  Projection: column1 AS #5
                    Values: (Int32(1))
          SubqueryAlias: t
            Projection: column1 AS #2, column2 AS #3
              Values: (Utf8("a"), Int32(2)), (Utf8("b"), Int32(1)), (Utf8("c"), Int32(3))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7]
        ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1]
          AggregateExec: mode=Final, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
            AggregateExec: mode=Partial, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
      ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
        ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Final, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            AggregateExec: mode=Partial, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(3), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#5)@0 as #6], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #5], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#7:Utf8;N]
      ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7], schema=[#7:Utf8;N]
        ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1], schema=[__common_expr_1:Utf8;N]
          AggregateExec: mode=Final, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]], schema=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]:List(Utf8);N]
            AggregateExec: mode=Partial, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]], schema=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST][array_agg]:List(Utf8);N, array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST][array_agg_orderings]:List(Struct("CASE WHEN scalar_subquery(<pending>) = 1 THEN #3@1 ELSE negative(#3@1) END": Int32))]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Utf8;N, #3:Int32;N]
                DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Utf8;N, column2:Int32;N]
      ProjectionExec: expr=[min(s.#5)@0 as #6], schema=[#6:Int32;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], schema=[min(s.#5):Int32;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], schema=[min(s.#5)[value]:Int32;N]
            ProjectionExec: expr=[column1@0 as #5], schema=[#5:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7]
          ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1]
            AggregateExec: mode=Final, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
              AggregateExec: mode=Partial, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7]
          ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1]
            AggregateExec: mode=Single, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7]
          ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1]
            AggregateExec: mode=Single, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
              SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #3@1 ELSE negative(#3@1) END ASC], preserve_partitioning=[false]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7]
        ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1]
          AggregateExec: mode=Single, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
            SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #3@1 ELSE negative(#3@1) END ASC], preserve_partitioning=[false]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
          ProjectionExec: expr=[column1@0 as #5]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#7@0 as joined]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7]
          ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1]
            AggregateExec: mode=Single, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
              SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #3@1 ELSE negative(#3@1) END ASC], preserve_partitioning=[false]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#7@0 as joined]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7]
          ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1]
            AggregateExec: mode=Single, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
              SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #3@1 ELSE negative(#3@1) END ASC], preserve_partitioning=[false]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#5)@0 as #6]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
        ProjectionExec: expr=[column1@0 as #5]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#7@0 as joined]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[CASE WHEN __common_expr_1@0 IS NOT NULL THEN __common_expr_1@0 END as #7]
          ProjectionExec: expr=[array_to_string(array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]@0, ,) as __common_expr_1]
            AggregateExec: mode=Single, gby=[], aggr=[array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 ELSE negative(t.#3) END ASC NULLS FIRST] as array_agg(t.#2) IGNORE NULLS ORDER BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#3 WHEN Boolean(true) THEN negative(t.#3) END ASC NULLS FIRST]]
              SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #3@1 ELSE negative(#3@1) END ASC], preserve_partitioning=[false]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_filter_before_aggregate"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Projection: t.#2 AS #7, sum(t.#3) AS total AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[sum(t.#3)]]
        Filter: t.#3 > (<subquery>)
          Subquery:
            Projection: max(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[max(s.#5)]]
                SubqueryAlias: s
                  Projection: #4 AS #5
                    Projection: column1 AS #4
                      Values: (Int32(1)), (Int32(2))
          SubqueryAlias: t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(3)), (Int32(2), Int32(10)), (Int32(3), Int32(1))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    Projection: t.#2 AS #7, sum(t.#3) AS total AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64))]]
        Filter: t.#3 > (<subquery>)
          Subquery:
            Projection: max(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[max(s.#5)]]
                SubqueryAlias: s
                  Projection: #4 AS #5
                    Projection: column1 AS #4
                      Values: (Int32(1)), (Int32(2))
          SubqueryAlias: t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(3)), (Int32(2), Int32(10)), (Int32(3), Int32(1))
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    Projection: t.#2 AS #7, sum(t.#3) AS total AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64))]]
        SubqueryAlias: t
          Projection: #0 AS #2, #1 AS #3
            Projection: column1 AS #0, column2 AS #1
              Filter: column2 > (<subquery>)
                Subquery:
                  Projection: max(s.#5) AS #6
                    Aggregate: groupBy=[[]], aggr=[[max(s.#5)]]
                      SubqueryAlias: s
                        Projection: #4 AS #5
                          Projection: column1 AS #4
                            Values: (Int32(1)), (Int32(2))
                Values: (Int32(1), Int32(2)), (Int32(1), Int32(3)), (Int32(2), Int32(10)), (Int32(3), Int32(1))
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Projection: t.#2 AS #7, sum(t.#3) AS total AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64))]]
        SubqueryAlias: t
          Projection: column1 AS #2, column2 AS #3
            Filter: column2 > (<subquery>)
              Subquery:
                Projection: max(s.#5) AS #6
                  Aggregate: groupBy=[[]], aggr=[[max(s.#5)]]
                    SubqueryAlias: s
                      Projection: column1 AS #5
                        Values: (Int32(1)), (Int32(2))
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(3)), (Int32(2), Int32(10)), (Int32(3), Int32(1))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Projection: t.#2 AS #7, sum(t.#3) AS total AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64))]]
        SubqueryAlias: t
          Projection: column1 AS #2, column2 AS #3
            Filter: column2 > (<subquery>)
              Subquery:
                Projection: max(s.#5) AS #6
                  Aggregate: groupBy=[[]], aggr=[[max(s.#5)]]
                    SubqueryAlias: s
                      Projection: column1 AS #5
                        Values: (Int32(1)), (Int32(2))
              Values: (Int32(1), Int32(2)), (Int32(1), Int32(3)), (Int32(2), Int32(10)), (Int32(3), Int32(1))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
          AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              FilterExec: column2@1 > scalar_subquery(<pending>)
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[max(s.#5)@0 as #6]
        AggregateExec: mode=Final, gby=[], aggr=[max(s.#5)]
          AggregateExec: mode=Partial, gby=[], aggr=[max(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8], statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)], statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
          AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)], statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0)),(Col[1]: Null=Inexact(0))]]
              FilterExec: column2@1 > scalar_subquery(<pending>), statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0)),(Col[1]: Null=Inexact(0))]]
                DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      ProjectionExec: expr=[max(s.#5)@0 as #6], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[max(s.#5)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[max(s.#5)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #5], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#7:Int32;N, #8:Int64;N]
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8], schema=[#7:Int32;N, #8:Int64;N]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)], schema=[#2:Int32;N, sum(t.#3):Int64;N]
          AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)], schema=[#2:Int32;N, sum(t.#3)[sum]:Int64;N]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
              FilterExec: column2@1 > scalar_subquery(<pending>), schema=[column1:Int32;N, column2:Int32;N]
                DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
      ProjectionExec: expr=[max(s.#5)@0 as #6], schema=[#6:Int32;N]
        AggregateExec: mode=Final, gby=[], aggr=[max(s.#5)], schema=[max(s.#5):Int32;N]
          AggregateExec: mode=Partial, gby=[], aggr=[max(s.#5)], schema=[max(s.#5)[value]:Int32;N]
            ProjectionExec: expr=[column1@0 as #5], schema=[#5:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
            AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                FilterExec: column2@1 > scalar_subquery(<pending>)
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[max(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[max(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[max(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=4
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    FilterExec: column2@1 > scalar_subquery(<pending>)
                      DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[max(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[max(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[max(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=4
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    FilterExec: column2@1 > scalar_subquery(<pending>)
                      DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[max(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[max(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
          RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=4
            AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
              RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  FilterExec: column2@1 > scalar_subquery(<pending>)
                    DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[max(s.#5)@0 as #6]
        AggregateExec: mode=Single, gby=[], aggr=[max(s.#5)]
          ProjectionExec: expr=[column1@0 as #5]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#7@0 as k, #8@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=4
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    FilterExec: column2@1 > scalar_subquery(<pending>)
                      DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[max(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[max(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#7@0 as k, #8@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=4
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    FilterExec: column2@1 > scalar_subquery(<pending>)
                      DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[max(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[max(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[max(s.#5)@0 as #6]
      AggregateExec: mode=Single, gby=[], aggr=[max(s.#5)]
        ProjectionExec: expr=[column1@0 as #5]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=4)
    placement=Worker
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
        FilterExec: column2@1 > scalar_subquery(<pending>)
          DataSourceExec: partitions=1, partition_sizes=[1]
      StageInputExec: input=0, partitioning=UnknownPartitioning(1)
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=Hash(keys=[#2@0], channels=4)
    placement=Worker
    AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
      StageInputExec: input=0, partitioning=RoundRobinBatch(4)
  
    === stage 3 ===
    inputs=[StageInput(stage=2, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#7@0 as k, #8@1 as total]
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
          StageInputExec: input=0, partitioning=Hash([#2@0], 4)
---
name: "test_scalar_subquery_in_group_by_expression"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Projection: g AS #5, count(Int64(1)) AS n AS #6
      Aggregate: groupBy=[[CAST(t.#1 AS Int64) + (<subquery>) AS g]], aggr=[[count(Int64(1))]]
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: #2 AS #3
                  Projection: column1 AS #2
                    Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2)), (Int32(1))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Projection: g AS #5, count(Int64(1)) AS n AS #6
      Aggregate: groupBy=[[CAST(t.#1 AS Int64) + (<subquery>) AS g]], aggr=[[count(Int64(1))]]
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2)), (Int32(1))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Projection: g AS #5, count(Int64(1)) AS n AS #6
      Aggregate: groupBy=[[CAST(t.#1 AS Int64) + (<subquery>) AS g]], aggr=[[count(Int64(1))]]
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2)), (Int32(1))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6]
        AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))]
          AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
      ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
        AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
          AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
            ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#3)@0 as #4], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#5:Int64;N, #6:Int64]
      ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6], schema=[#5:Int64;N, #6:Int64]
        AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))], schema=[g:Int64;N, count(Int64(1)):Int64]
          AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))], schema=[g:Int64;N, count(Int64(1))[count]:Int64]
            ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#3)@0 as #4], schema=[#4:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], schema=[min(s.#3):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], schema=[min(s.#3)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6]
          AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))]
            AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6]
          AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))]
            RepartitionExec: partitioning=Hash([g@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6]
          AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))]
            RepartitionExec: partitioning=Hash([g@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6]
        AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))]
          RepartitionExec: partitioning=Hash([g@0], 4), input_partitions=1
            AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
          ProjectionExec: expr=[column1@0 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#5@0 as g, #6@1 as n]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6]
          AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))]
            RepartitionExec: partitioning=Hash([g@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#5@0 as g, #6@1 as n]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6]
          AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))]
            RepartitionExec: partitioning=Hash([g@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#3)@0 as #4]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
        ProjectionExec: expr=[column1@0 as #3]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[g@0], channels=4)
    placement=Worker
    ScalarSubqueryExec: subqueries=1
      AggregateExec: mode=Partial, gby=[CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as g], aggr=[count(Int64(1))]
        ProjectionExec: expr=[column1@0 as #1]
          DataSourceExec: partitions=1, partition_sizes=[1]
      StageInputExec: input=0, partitioning=UnknownPartitioning(1)
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#5@0 as g, #6@1 as n]
      ProjectionExec: expr=[g@0 as #5, count(Int64(1))@1 as #6]
        AggregateExec: mode=FinalPartitioned, gby=[g@0 as g], aggr=[count(Int64(1))]
          StageInputExec: input=0, partitioning=Hash([g@0], 4)
---
name: "test_scalar_subquery_in_grouped_aggregate_expression"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Projection: t.#2 AS #7, sum(t.#3 + #6) AS total AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64) + (<subquery>))]]
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
              SubqueryAlias: s
                Projection: #4 AS #5
                  Projection: column1 AS #4
                    Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: #0 AS #2, #1 AS #3
            Projection: column1 AS #0, column2 AS #1
              Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Projection: t.#2 AS #7, sum(t.#3 + #6) AS total AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64) + (<subquery>))]]
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #5
                  Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: column1 AS #2, column2 AS #3
            Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Projection: t.#2 AS #7, sum(t.#3 + #6) AS total AS #8
      Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64) + (<subquery>))]]
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #5
                  Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: column1 AS #2, column2 AS #3
            Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
          AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
          AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#5)@0 as #6], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #5], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#7:Int32;N, #8:Int64;N]
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8], schema=[#7:Int32;N, #8:Int64;N]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)], schema=[#2:Int32;N, sum(t.#3 + #6):Int64;N]
          AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)], schema=[#2:Int32;N, sum(t.#3 + #6)[sum]:Int64;N]
            ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
      ProjectionExec: expr=[min(s.#5)@0 as #6], schema=[#6:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], schema=[min(s.#5):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], schema=[min(s.#5)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #5], schema=[#5:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
            AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
          RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
            AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
          ProjectionExec: expr=[column1@0 as #5]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#7@0 as k, #8@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#7@0 as k, #8@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#5)@0 as #6]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
        ProjectionExec: expr=[column1@0 as #5]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#2@0], channels=4)
    placement=Worker
    ScalarSubqueryExec: subqueries=1
      AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
        ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
          DataSourceExec: partitions=1, partition_sizes=[1]
      StageInputExec: input=0, partitioning=UnknownPartitioning(1)
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle), StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#7@0 as k, #8@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3 + #6)@1 as #8]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3 + #6)]
            StageInputExec: input=0, partitioning=Hash([#2@0], 4)
        StageInputExec: input=1, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_having_filter"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Projection: t.#2 AS #7, sum(t.#3) AS total AS #8
      Filter: sum(t.#3) > (<subquery>)
        Subquery:
          Projection: min(s.#5) AS #6
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: #4 AS #5
                  Projection: column1 AS #4
                    Values: (Int32(3))
        Aggregate: groupBy=[[t.#2]], aggr=[[sum(t.#3)]]
          SubqueryAlias: t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(4))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    Projection: t.#2 AS #7, sum(t.#3) AS total AS #8
      Filter: sum(t.#3) > (<subquery>)
        Subquery:
          Projection: CAST(min(s.#5) AS #6 AS Int64)
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: #4 AS #5
                  Projection: column1 AS #4
                    Values: (Int32(3))
        Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64))]]
          SubqueryAlias: t
            Projection: #0 AS #2, #1 AS #3
              Projection: column1 AS #0, column2 AS #1
                Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(4))
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Projection: t.#2 AS #7, sum(t.#3) AS total AS #8
      Filter: sum(t.#3) > (<subquery>)
        Subquery:
          Projection: CAST(min(s.#5) AS #6 AS Int64)
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: column1 AS #5
                  Values: (Int32(3))
        Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64))]]
          SubqueryAlias: t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(4))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Projection: t.#2 AS #7, sum(t.#3) AS total AS #8
      Filter: sum(t.#3) > (<subquery>)
        Subquery:
          Projection: CAST(min(s.#5) AS #6 AS Int64)
            Aggregate: groupBy=[[]], aggr=[[min(s.#5)]]
              SubqueryAlias: s
                Projection: column1 AS #5
                  Values: (Int32(3))
        Aggregate: groupBy=[[t.#2]], aggr=[[sum(CAST(t.#3 AS Int64))]]
          SubqueryAlias: t
            Projection: column1 AS #2, column2 AS #3
              Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3)), (Int32(2), Int32(4))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
        FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>)
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
            AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8], statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
        FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>), statistics=[Rows=Inexact(1), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
            AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)], statistics=[Rows=Inexact(4), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #5], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#7:Int32;N, #8:Int64;N]
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8], schema=[#7:Int32;N, #8:Int64;N]
        FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>), schema=[#2:Int32;N, sum(t.#3):Int64;N]
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)], schema=[#2:Int32;N, sum(t.#3):Int64;N]
            AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)], schema=[#2:Int32;N, sum(t.#3)[sum]:Int64;N]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
                DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
      ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6], schema=[#6:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], schema=[min(s.#5):Int32;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], schema=[min(s.#5)[value]:Int32;N]
            ProjectionExec: expr=[column1@0 as #5], schema=[#5:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>)
            AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>)
            AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
              RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
                AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>)
            AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
              RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
                AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
        FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>)
          AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
            RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
          ProjectionExec: expr=[column1@0 as #5]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#7@0 as k, #8@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>)
            AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
              RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
                AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#7@0 as k, #8@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>)
            AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
              RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
                AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[CAST(min(s.#5)@0 AS Int64) as #6]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
        ProjectionExec: expr=[column1@0 as #5]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#2@0], channels=4)
    placement=Worker
    AggregateExec: mode=Partial, gby=[#2@0 as #2], aggr=[sum(t.#3)]
      ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
        DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle), StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#7@0 as k, #8@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#2@0 as #7, sum(t.#3)@1 as #8]
          FilterExec: sum(t.#3)@1 > scalar_subquery(<pending>)
            AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[sum(t.#3)]
              StageInputExec: input=0, partitioning=Hash([#2@0], 4)
        StageInputExec: input=1, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_join_condition"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #7 ASC NULLS FIRST
      Projection: l.#1 AS #7, r.#3 AS #8
        Inner Join:  Filter: CAST(l.#1 AS Int64) + (<subquery>) = CAST(r.#3 AS Int64)
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                SubqueryAlias: s
                  Projection: #4 AS #5
                    Projection: column1 AS #4
                      Values: (Int32(10)), (Int32(20))
          SubqueryAlias: l
            Projection: #0 AS #1
              Projection: column1 AS #0
                Values: (Int32(1)), (Int32(2)), (Int32(3))
          SubqueryAlias: r
            Projection: #2 AS #3
              Projection: column1 AS #2
                Values: (Int32(11)), (Int32(12)), (Int32(13))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    Sort: #7 ASC NULLS FIRST
      Projection: l.#1 AS #7, r.#3 AS #8
        Inner Join: CAST(l.#1 AS Int64) + (<subquery>) = CAST(r.#3 AS Int64)
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                SubqueryAlias: s
                  Projection: #4 AS #5
                    Projection: column1 AS #4
                      Values: (Int32(10)), (Int32(20))
          SubqueryAlias: l
            Projection: #0 AS #1
              Projection: column1 AS #0
                Values: (Int32(1)), (Int32(2)), (Int32(3))
          SubqueryAlias: r
            Projection: #2 AS #3
              Projection: column1 AS #2
                Values: (Int32(11)), (Int32(12)), (Int32(13))
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #7 ASC NULLS FIRST
      Projection: l.#1 AS #7, r.#3 AS #8
        Inner Join: CAST(l.#1 AS Int64) + (<subquery>) = CAST(r.#3 AS Int64)
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                SubqueryAlias: s
                  Projection: column1 AS #5
                    Values: (Int32(10)), (Int32(20))
          SubqueryAlias: l
            Projection: column1 AS #1
              Values: (Int32(1)), (Int32(2)), (Int32(3))
          SubqueryAlias: r
            Projection: column1 AS #3
              Values: (Int32(11)), (Int32(12)), (Int32(13))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #7 ASC NULLS FIRST
      Projection: l.#1 AS #7, r.#3 AS #8
        Inner Join: CAST(l.#1 AS Int64) + (<subquery>) = CAST(r.#3 AS Int64)
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                SubqueryAlias: s
                  Projection: column1 AS #5
                    Values: (Int32(10)), (Int32(20))
          SubqueryAlias: l
            Projection: column1 AS #1
              Values: (Int32(1)), (Int32(2)), (Int32(3))
          SubqueryAlias: r
            Projection: column1 AS #3
              Values: (Int32(11)), (Int32(12)), (Int32(13))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#7@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
          ProjectionExec: expr=[#1@0 as #1, #3@2 as #3]
            HashJoinExec: mode=Auto, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)]
              ProjectionExec: expr=[#1@0 as #1, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[#3@0 as #3, CAST(#3@0 AS Int64) as CAST(r.#3 AS Int64)]
                ProjectionExec: expr=[column1@0 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      SortExec: expr=[#7@0 ASC], preserve_partitioning=[false], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
        ProjectionExec: expr=[#1@0 as #7, #3@1 as #8], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
          ProjectionExec: expr=[#1@0 as #1, #3@2 as #3], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
            HashJoinExec: mode=Auto, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)], statistics=[Rows=Inexact(3), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]:),(Col[2]: Null=Exact(0)),(Col[3]:)]]
              ProjectionExec: expr=[#1@0 as #1, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
                ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
                  DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              ProjectionExec: expr=[#3@0 as #3, CAST(#3@0 AS Int64) as CAST(r.#3 AS Int64)], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
                ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
                  DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#5)@0 as #6], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #5], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#7:Int32;N, #8:Int32;N]
      SortExec: expr=[#7@0 ASC], preserve_partitioning=[false], schema=[#7:Int32;N, #8:Int32;N]
        ProjectionExec: expr=[#1@0 as #7, #3@1 as #8], schema=[#7:Int32;N, #8:Int32;N]
          ProjectionExec: expr=[#1@0 as #1, #3@2 as #3], schema=[#1:Int32;N, #3:Int32;N]
            HashJoinExec: mode=Auto, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)], schema=[#1:Int32;N, l.#1 + #6:Int64;N, #3:Int32;N, CAST(r.#3 AS Int64):Int64;N]
              ProjectionExec: expr=[#1@0 as #1, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6], schema=[#1:Int32;N, l.#1 + #6:Int64;N]
                ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
                  DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
              ProjectionExec: expr=[#3@0 as #3, CAST(#3@0 AS Int64) as CAST(r.#3 AS Int64)], schema=[#3:Int32;N, CAST(r.#3 AS Int64):Int64;N]
                ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
                  DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#5)@0 as #6], schema=[#6:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], schema=[min(s.#5):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], schema=[min(s.#5)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #5], schema=[#5:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            ProjectionExec: expr=[#1@0 as #1, #3@2 as #3]
              HashJoinExec: mode=Auto, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)]
                ProjectionExec: expr=[#1@0 as #1, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6]
                  ProjectionExec: expr=[column1@0 as #1]
                    DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[#3@0 as #3, CAST(#3@0 AS Int64) as CAST(r.#3 AS Int64)]
                  ProjectionExec: expr=[column1@0 as #3]
                    DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            ProjectionExec: expr=[#1@0 as #1, #3@2 as #3]
              HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)]
                ProjectionExec: expr=[#1@0 as #1, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6]
                  ProjectionExec: expr=[column1@0 as #1]
                    DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[#3@0 as #3, CAST(#3@0 AS Int64) as CAST(r.#3 AS Int64)]
                  ProjectionExec: expr=[column1@0 as #3]
                    DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            ProjectionExec: expr=[#1@0 as #1, #3@2 as #3]
              HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)]
                ProjectionExec: expr=[#1@0 as #1, CAST(#1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6]
                  ProjectionExec: expr=[column1@0 as #1]
                    DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[#3@0 as #3, CAST(#3@0 AS Int64) as CAST(r.#3 AS Int64)]
                  ProjectionExec: expr=[column1@0 as #3]
                    DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)], projection=[#1@0, #3@2]
              ProjectionExec: expr=[column1@0 as #1, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[column1@0 as #3, CAST(column1@0 AS Int64) as CAST(r.#3 AS Int64)]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#7@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
          HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)], projection=[#1@0, #3@2]
            ProjectionExec: expr=[column1@0 as #1, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6]
              DataSourceExec: partitions=1, partition_sizes=[1]
            ProjectionExec: expr=[column1@0 as #3, CAST(column1@0 AS Int64) as CAST(r.#3 AS Int64)]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
          ProjectionExec: expr=[column1@0 as #5]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#7@0 as v, #8@1 as w]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)], projection=[#1@0, #3@2]
              ProjectionExec: expr=[column1@0 as #1, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[column1@0 as #3, CAST(column1@0 AS Int64) as CAST(r.#3 AS Int64)]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#7@0 as v, #8@1 as w]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)], projection=[#1@0, #3@2]
              ProjectionExec: expr=[column1@0 as #1, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[column1@0 as #3, CAST(column1@0 AS Int64) as CAST(r.#3 AS Int64)]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#5)@0 as #6]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
        ProjectionExec: expr=[column1@0 as #5]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#7@0 as v, #8@1 as w]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            HashJoinExec: mode=CollectLeft, join_type=Inner, on=[(l.#1 + #6@1, CAST(r.#3 AS Int64)@1)], projection=[#1@0, #3@2]
              ProjectionExec: expr=[column1@0 as #1, CAST(column1@0 AS Int64) + scalar_subquery(<pending>) as l.#1 + #6]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[column1@0 as #3, CAST(column1@0 AS Int64) as CAST(r.#3 AS Int64)]
                DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_nonequi_join_condition"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #7 ASC NULLS FIRST, #8 ASC NULLS FIRST
      Projection: l.#1 AS #7, r.#3 AS #8
        Inner Join:  Filter: CAST(l.#1 AS Int64) + (<subquery>) < CAST(r.#3 AS Int64)
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                SubqueryAlias: s
                  Projection: #4 AS #5
                    Projection: column1 AS #4
                      Values: (Int32(10)), (Int32(20))
          SubqueryAlias: l
            Projection: #0 AS #1
              Projection: column1 AS #0
                Values: (Int32(1)), (Int32(2)), (Int32(3))
          SubqueryAlias: r
            Projection: #2 AS #3
              Projection: column1 AS #2
                Values: (Int32(11)), (Int32(12)), (Int32(13))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    Sort: #7 ASC NULLS FIRST, #8 ASC NULLS FIRST
      Projection: l.#1 AS #7, r.#3 AS #8
        Filter: CAST(l.#1 AS Int64) + (<subquery>) < CAST(r.#3 AS Int64)
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                SubqueryAlias: s
                  Projection: #4 AS #5
                    Projection: column1 AS #4
                      Values: (Int32(10)), (Int32(20))
          Cross Join:
            SubqueryAlias: l
              Projection: #0 AS #1
                Projection: column1 AS #0
                  Values: (Int32(1)), (Int32(2)), (Int32(3))
            SubqueryAlias: r
              Projection: #2 AS #3
                Projection: column1 AS #2
                  Values: (Int32(11)), (Int32(12)), (Int32(13))
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #7 ASC NULLS FIRST, #8 ASC NULLS FIRST
      Projection: l.#1 AS #7, r.#3 AS #8
        Filter: CAST(l.#1 AS Int64) + (<subquery>) < CAST(r.#3 AS Int64)
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                SubqueryAlias: s
                  Projection: column1 AS #5
                    Values: (Int32(10)), (Int32(20))
          Cross Join:
            SubqueryAlias: l
              Projection: column1 AS #1
                Values: (Int32(1)), (Int32(2)), (Int32(3))
            SubqueryAlias: r
              Projection: column1 AS #3
                Values: (Int32(11)), (Int32(12)), (Int32(13))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #7 ASC NULLS FIRST, #8 ASC NULLS FIRST
      Projection: l.#1 AS #7, r.#3 AS #8
        Filter: CAST(l.#1 AS Int64) + (<subquery>) < CAST(r.#3 AS Int64)
          Subquery:
            Projection: min(s.#5) AS #6
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                SubqueryAlias: s
                  Projection: column1 AS #5
                    Values: (Int32(10)), (Int32(20))
          Cross Join:
            SubqueryAlias: l
              Projection: column1 AS #1
                Values: (Int32(1)), (Int32(2)), (Int32(3))
            SubqueryAlias: r
              Projection: column1 AS #3
                Values: (Int32(11)), (Int32(12)), (Int32(13))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#7@0 ASC, #8@1 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
          FilterExec: CAST(#1@0 AS Int64) + scalar_subquery(<pending>) < CAST(#3@1 AS Int64)
            CrossJoinExec
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0)),(Col[1]: Null=Inexact(0))]]
      SortExec: expr=[#7@0 ASC, #8@1 ASC], preserve_partitioning=[false], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0)),(Col[1]: Null=Inexact(0))]]
        ProjectionExec: expr=[#1@0 as #7, #3@1 as #8], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0)),(Col[1]: Null=Inexact(0))]]
          FilterExec: CAST(#1@0 AS Int64) + scalar_subquery(<pending>) < CAST(#3@1 AS Int64), statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]: Null=Inexact(0)),(Col[1]: Null=Inexact(0))]]
            CrossJoinExec, statistics=[Rows=Exact(9), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
              ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
                DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
                DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#5)@0 as #6], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #5], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#7:Int32;N, #8:Int32;N]
      SortExec: expr=[#7@0 ASC, #8@1 ASC], preserve_partitioning=[false], schema=[#7:Int32;N, #8:Int32;N]
        ProjectionExec: expr=[#1@0 as #7, #3@1 as #8], schema=[#7:Int32;N, #8:Int32;N]
          FilterExec: CAST(#1@0 AS Int64) + scalar_subquery(<pending>) < CAST(#3@1 AS Int64), schema=[#1:Int32;N, #3:Int32;N]
            CrossJoinExec, schema=[#1:Int32;N, #3:Int32;N]
              ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
                DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
              ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
                DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#5)@0 as #6], schema=[#6:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], schema=[min(s.#5):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], schema=[min(s.#5)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #5], schema=[#5:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC, #8@1 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            FilterExec: CAST(#1@0 AS Int64) + scalar_subquery(<pending>) < CAST(#3@1 AS Int64)
              CrossJoinExec
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[column1@0 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC, #8@1 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            FilterExec: CAST(#1@0 AS Int64) + scalar_subquery(<pending>) < CAST(#3@1 AS Int64)
              CrossJoinExec
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[column1@0 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#7@0 ASC, #8@1 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
          FilterExec: CAST(#1@0 AS Int64) + scalar_subquery(<pending>) < CAST(#3@1 AS Int64)
            CrossJoinExec
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
          ProjectionExec: expr=[column1@0 as #5]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#7@0 as v, #8@1 as w]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC, #8@1 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            FilterExec: CAST(#1@0 AS Int64) + scalar_subquery(<pending>) < CAST(#3@1 AS Int64)
              CrossJoinExec
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[column1@0 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#7@0 as v, #8@1 as w]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC, #8@1 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            FilterExec: CAST(#1@0 AS Int64) + scalar_subquery(<pending>) < CAST(#3@1 AS Int64)
              CrossJoinExec
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[column1@0 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#5)@0 as #6]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
        ProjectionExec: expr=[column1@0 as #5]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#7@0 as v, #8@1 as w]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@0 ASC, #8@1 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #7, #3@1 as #8]
            FilterExec: CAST(#1@0 AS Int64) + scalar_subquery(<pending>) < CAST(#3@1 AS Int64)
              CrossJoinExec
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
                ProjectionExec: expr=[column1@0 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_projection_before_aggregate"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Projection: p.#7 AS #9, sum(p.#8) AS total AS #10
      Aggregate: groupBy=[[p.#7]], aggr=[[sum(p.#8)]]
        SubqueryAlias: p
          Projection: t.#2 AS #7, CAST(t.#3 AS Int64) + (<subquery>) AS shifted AS #8
            Subquery:
              Projection: min(s.#5) AS #6
                Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                  SubqueryAlias: s
                    Projection: #4 AS #5
                      Projection: column1 AS #4
                        Values: (Int32(10)), (Int32(20))
            SubqueryAlias: t
              Projection: #0 AS #2, #1 AS #3
                Projection: column1 AS #0, column2 AS #1
                  Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Projection: p.#7 AS #9, sum(p.#8) AS total AS #10
      Aggregate: groupBy=[[p.#7]], aggr=[[sum(p.#8)]]
        SubqueryAlias: p
          Projection: t.#2 AS #7, CAST(t.#3 AS Int64) + (<subquery>) AS shifted AS #8
            Subquery:
              Projection: min(s.#5) AS #6
                Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                  SubqueryAlias: s
                    Projection: column1 AS #5
                      Values: (Int32(10)), (Int32(20))
            SubqueryAlias: t
              Projection: column1 AS #2, column2 AS #3
                Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Projection: p.#7 AS #9, sum(p.#8) AS total AS #10
      Aggregate: groupBy=[[p.#7]], aggr=[[sum(p.#8)]]
        SubqueryAlias: p
          Projection: t.#2 AS #7, CAST(t.#3 AS Int64) + (<subquery>) AS shifted AS #8
            Subquery:
              Projection: min(s.#5) AS #6
                Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#5 AS Int64))]]
                  SubqueryAlias: s
                    Projection: column1 AS #5
                      Values: (Int32(10)), (Int32(20))
            SubqueryAlias: t
              Projection: column1 AS #2, column2 AS #3
                Values: (Int32(1), Int32(1)), (Int32(1), Int32(2)), (Int32(2), Int32(3))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10]
        AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)]
          AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)]
            ProjectionExec: expr=[#2@0 as #7, CAST(#3@1 AS Int64) + scalar_subquery(<pending>) as #8]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
      ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
        AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
          AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:),(Col[1]:)]]
            ProjectionExec: expr=[#2@0 as #7, CAST(#3@1 AS Int64) + scalar_subquery(<pending>) as #8], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
                DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#5)@0 as #6], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #5], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#9:Int32;N, #10:Int64;N]
      ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10], schema=[#9:Int32;N, #10:Int64;N]
        AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)], schema=[#7:Int32;N, sum(p.#8):Int64;N]
          AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)], schema=[#7:Int32;N, sum(p.#8)[sum]:Int64;N]
            ProjectionExec: expr=[#2@0 as #7, CAST(#3@1 AS Int64) + scalar_subquery(<pending>) as #8], schema=[#7:Int32;N, #8:Int64;N]
              ProjectionExec: expr=[column1@0 as #2, column2@1 as #3], schema=[#2:Int32;N, #3:Int32;N]
                DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N, column2:Int32;N]
      ProjectionExec: expr=[min(s.#5)@0 as #6], schema=[#6:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)], schema=[min(s.#5):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)], schema=[min(s.#5)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #5], schema=[#5:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10]
          AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)]
            AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)]
              ProjectionExec: expr=[#2@0 as #7, CAST(#3@1 AS Int64) + scalar_subquery(<pending>) as #8]
                ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10]
          AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)]
            RepartitionExec: partitioning=Hash([#7@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)]
                ProjectionExec: expr=[#2@0 as #7, CAST(#3@1 AS Int64) + scalar_subquery(<pending>) as #8]
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#5)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#5)]
              ProjectionExec: expr=[column1@0 as #5]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10]
          AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)]
            RepartitionExec: partitioning=Hash([#7@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)]
                ProjectionExec: expr=[#2@0 as #7, CAST(#3@1 AS Int64) + scalar_subquery(<pending>) as #8]
                  ProjectionExec: expr=[column1@0 as #2, column2@1 as #3]
                    DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10]
          AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)]
            RepartitionExec: partitioning=Hash([#7@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)]
                ProjectionExec: expr=[column1@0 as #7, CAST(column2@1 AS Int64) + scalar_subquery(<pending>) as #8]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10]
        AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)]
          RepartitionExec: partitioning=Hash([#7@0], 4), input_partitions=1
            AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)]
              ProjectionExec: expr=[column1@0 as #7, CAST(column2@1 AS Int64) + scalar_subquery(<pending>) as #8]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#5)@0 as #6]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
          ProjectionExec: expr=[column1@0 as #5]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#9@0 as k, #10@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10]
          AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)]
            RepartitionExec: partitioning=Hash([#7@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)]
                ProjectionExec: expr=[column1@0 as #7, CAST(column2@1 AS Int64) + scalar_subquery(<pending>) as #8]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#9@0 as k, #10@1 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10]
          AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)]
            RepartitionExec: partitioning=Hash([#7@0], 4), input_partitions=1
              AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)]
                ProjectionExec: expr=[column1@0 as #7, CAST(column2@1 AS Int64) + scalar_subquery(<pending>) as #8]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#5)@0 as #6]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
            ProjectionExec: expr=[column1@0 as #5]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#5)@0 as #6]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#5)]
        ProjectionExec: expr=[column1@0 as #5]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#7@0], channels=4)
    placement=Worker
    ScalarSubqueryExec: subqueries=1
      AggregateExec: mode=Partial, gby=[#7@0 as #7], aggr=[sum(p.#8)]
        ProjectionExec: expr=[column1@0 as #7, CAST(column2@1 AS Int64) + scalar_subquery(<pending>) as #8]
          DataSourceExec: partitions=1, partition_sizes=[1]
      StageInputExec: input=0, partitioning=UnknownPartitioning(1)
  
    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#9@0 as k, #10@1 as total]
      ProjectionExec: expr=[#7@0 as #9, sum(p.#8)@1 as #10]
        AggregateExec: mode=FinalPartitioned, gby=[#7@0 as #7], aggr=[sum(p.#8)]
          StageInputExec: input=0, partitioning=Hash([#7@0], 4)
---
name: "test_scalar_subquery_in_sort_expression"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: CASE WHEN (<subquery>) = Int32(2) THEN #2 WHEN Boolean(true) THEN negative(#2) END DESC NULLS LAST
      Subquery:
        Projection: min(s.#4) AS #5
          Aggregate: groupBy=[[]], aggr=[[min(s.#4)]]
            SubqueryAlias: s
              Projection: #3 AS #4
                Projection: column1 AS #3
                  Values: (Int32(2))
      Projection: t.#1 AS #2
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    Sort: CASE WHEN (<subquery>) = Int32(2) THEN #2 ELSE negative(#2) END AS CASE WHEN #5 = Int32(2) THEN #2 WHEN Boolean(true) THEN negative(#2) END DESC NULLS LAST
      Subquery:
        Projection: min(s.#4) AS #5
          Aggregate: groupBy=[[]], aggr=[[min(s.#4)]]
            SubqueryAlias: s
              Projection: #3 AS #4
                Projection: column1 AS #3
                  Values: (Int32(2))
      Projection: t.#1 AS #2
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: CASE WHEN (<subquery>) = Int32(2) THEN #2 ELSE negative(#2) END AS CASE WHEN #5 = Int32(2) THEN #2 WHEN Boolean(true) THEN negative(#2) END DESC NULLS LAST
      Subquery:
        Projection: min(s.#4) AS #5
          Aggregate: groupBy=[[]], aggr=[[min(s.#4)]]
            SubqueryAlias: s
              Projection: column1 AS #4
                Values: (Int32(2))
      Projection: t.#1 AS #2
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: CASE WHEN (<subquery>) = Int32(2) THEN #2 ELSE negative(#2) END AS CASE WHEN #5 = Int32(2) THEN #2 WHEN Boolean(true) THEN negative(#2) END DESC NULLS LAST
      Subquery:
        Projection: min(s.#4) AS #5
          Aggregate: groupBy=[[]], aggr=[[min(s.#4)]]
            SubqueryAlias: s
              Projection: column1 AS #4
                Values: (Int32(2))
      Projection: t.#1 AS #2
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #2]
          ProjectionExec: expr=[column1@0 as #1]
            DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#4)@0 as #5]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#4)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#4)]
            ProjectionExec: expr=[column1@0 as #4]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
        ProjectionExec: expr=[#1@0 as #2], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
          ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
            DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#4)@0 as #5], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#4)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#4)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #4], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#2:Int32;N]
      SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false], schema=[#2:Int32;N]
        ProjectionExec: expr=[#1@0 as #2], schema=[#2:Int32;N]
          ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
            DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#4)@0 as #5], schema=[#5:Int32;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#4)], schema=[min(s.#4):Int32;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#4)], schema=[min(s.#4)[value]:Int32;N]
            ProjectionExec: expr=[column1@0 as #4], schema=[#4:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #2]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#4)@0 as #5]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#4)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#4)]
              ProjectionExec: expr=[column1@0 as #4]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #2]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#4)@0 as #5]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#4)]
            ProjectionExec: expr=[column1@0 as #4]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #2]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#4)@0 as #5]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#4)]
            ProjectionExec: expr=[column1@0 as #4]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false]
        ProjectionExec: expr=[column1@0 as #2]
          DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#4)@0 as #5]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#4)]
          ProjectionExec: expr=[column1@0 as #4]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#2@0 as v]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #2]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#4)@0 as #5]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#4)]
            ProjectionExec: expr=[column1@0 as #4]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#2@0 as v]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #2]
            DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#4)@0 as #5]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#4)]
            ProjectionExec: expr=[column1@0 as #4]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#4)@0 as #5]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#4)]
        ProjectionExec: expr=[column1@0 as #4]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#2@0 as v]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #2@0 ELSE negative(#2@0) END DESC NULLS LAST], preserve_partitioning=[false]
          ProjectionExec: expr=[column1@0 as #2]
            DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_ungrouped_aggregate_expression"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Projection: sum(t.#1 + #4) AS total AS #5
      Aggregate: groupBy=[[]], aggr=[[sum(CAST(t.#1 AS Int64) + (<subquery>))]]
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: #2 AS #3
                  Projection: column1 AS #2
                    Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Projection: sum(t.#1 + #4) AS total AS #5
      Aggregate: groupBy=[[]], aggr=[[sum(CAST(t.#1 AS Int64) + (<subquery>))]]
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Projection: sum(t.#1 + #4) AS total AS #5
      Aggregate: groupBy=[[]], aggr=[[sum(CAST(t.#1 AS Int64) + (<subquery>))]]
        Subquery:
          Projection: min(s.#3) AS #4
            Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
              SubqueryAlias: s
                Projection: column1 AS #3
                  Values: (Int32(10)), (Int32(20))
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[sum(t.#1 + #4)@0 as #5]
        AggregateExec: mode=Final, gby=[], aggr=[sum(t.#1 + #4)]
          AggregateExec: mode=Partial, gby=[], aggr=[sum(t.#1 + #4)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
      ProjectionExec: expr=[sum(t.#1 + #4)@0 as #5], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[sum(t.#1 + #4)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[sum(t.#1 + #4)], statistics=[Rows=Inexact(3), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#3)@0 as #4], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#5:Int64;N]
      ProjectionExec: expr=[sum(t.#1 + #4)@0 as #5], schema=[#5:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[sum(t.#1 + #4)], schema=[sum(t.#1 + #4):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[sum(t.#1 + #4)], schema=[sum(t.#1 + #4)[sum]:Int64;N]
            ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#3)@0 as #4], schema=[#4:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], schema=[min(s.#3):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], schema=[min(s.#3)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[sum(t.#1 + #4)@0 as #5]
          AggregateExec: mode=Final, gby=[], aggr=[sum(t.#1 + #4)]
            AggregateExec: mode=Partial, gby=[], aggr=[sum(t.#1 + #4)]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[sum(t.#1 + #4)@0 as #5]
          AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[sum(t.#1 + #4)@0 as #5]
        AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4)]
          ProjectionExec: expr=[column1@0 as #1]
            DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
          ProjectionExec: expr=[column1@0 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#5@0 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[sum(t.#1 + #4)@0 as #5]
          AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#5@0 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[sum(t.#1 + #4)@0 as #5]
          AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#3)@0 as #4]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
        ProjectionExec: expr=[column1@0 as #3]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#5@0 as total]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[sum(t.#1 + #4)@0 as #5]
          AggregateExec: mode=Single, gby=[], aggr=[sum(t.#1 + #4)]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_window_aggregate_argument"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #6 ASC NULLS FIRST
      Projection: t.#1 AS #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS #5 AS total AS #7
        WindowAggr: windowExpr=[[sum(CAST(t.#1 AS Int64) + (<subquery>)) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
                SubqueryAlias: s
                  Projection: #2 AS #3
                    Projection: column1 AS #2
                      Values: (Int32(10)), (Int32(20))
          SubqueryAlias: t
            Projection: #0 AS #1
              Projection: column1 AS #0
                Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #6 ASC NULLS FIRST
      Projection: t.#1 AS #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS #5 AS total AS #7
        WindowAggr: windowExpr=[[sum(CAST(t.#1 AS Int64) + (<subquery>)) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
                SubqueryAlias: s
                  Projection: column1 AS #3
                    Values: (Int32(10)), (Int32(20))
          SubqueryAlias: t
            Projection: column1 AS #1
              Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #6 ASC NULLS FIRST
      Projection: t.#1 AS #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS #5 AS total AS #7
        WindowAggr: windowExpr=[[sum(CAST(t.#1 AS Int64) + (<subquery>)) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(CAST(s.#3 AS Int64))]]
                SubqueryAlias: s
                  Projection: column1 AS #3
                    Values: (Int32(10)), (Int32(20))
          SubqueryAlias: t
            Projection: column1 AS #1
              Values: (Int32(1)), (Int32(2)), (Int32(3))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
          WindowAggExec: wdw=[sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64, nullable: true }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
      SortExec: expr=[#6@0 ASC], preserve_partitioning=[false], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
        ProjectionExec: expr=[#1@0 as #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
          WindowAggExec: wdw=[sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64, nullable: true }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }], statistics=[Rows=Exact(3), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]:)]]
            ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#3)@0 as #4], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#6:Int32;N, #7:Int64;N]
      SortExec: expr=[#6@0 ASC], preserve_partitioning=[false], schema=[#6:Int32;N, #7:Int64;N]
        ProjectionExec: expr=[#1@0 as #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7], schema=[#6:Int32;N, #7:Int64;N]
          WindowAggExec: wdw=[sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64, nullable: true }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }], schema=[#1:Int32;N, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING:Int64;N]
            ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#3)@0 as #4], schema=[#4:Int64;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], schema=[min(s.#3):Int64;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], schema=[min(s.#3)[value]:Int64;N]
            ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64, nullable: true }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64, nullable: true }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    SAME TEXT AS ABOVE
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
          WindowAggExec: wdw=[sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64, nullable: true }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
          ProjectionExec: expr=[column1@0 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#6@0 as v, #7@1 as total]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64, nullable: true }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#6@0 as v, #7@1 as total]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64, nullable: true }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#3)@0 as #4]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
        ProjectionExec: expr=[column1@0 as #3]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#6@0 as v, #7@1 as total]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "sum(t.#1 + #4) ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64, nullable: true }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_window_order_expression"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #7 ASC NULLS FIRST
      Projection: t.#1 AS #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW AS #5 AS Int32) AS rn AS #7
        WindowAggr: windowExpr=[[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
                SubqueryAlias: s
                  Projection: #2 AS #3
                    Projection: column1 AS #2
                      Values: (Int32(2))
          SubqueryAlias: t
            Projection: #0 AS #1
              Projection: column1 AS #0
                Values: (Int32(3)), (Int32(1)), (Int32(2))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    SAME TEXT AS ABOVE
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    Sort: #7 ASC NULLS FIRST
      Projection: t.#1 AS #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW AS #5 AS Int32) AS rn AS #7
        WindowAggr: windowExpr=[[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 ELSE negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW AS row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
                SubqueryAlias: s
                  Projection: #2 AS #3
                    Projection: column1 AS #2
                      Values: (Int32(2))
          SubqueryAlias: t
            Projection: #0 AS #1
              Projection: column1 AS #0
                Values: (Int32(3)), (Int32(1)), (Int32(2))
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #7 ASC NULLS FIRST
      Projection: t.#1 AS #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW AS #5 AS Int32) AS rn AS #7
        WindowAggr: windowExpr=[[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 ELSE negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW AS row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
                SubqueryAlias: s
                  Projection: column1 AS #3
                    Values: (Int32(2))
          SubqueryAlias: t
            Projection: column1 AS #1
              Values: (Int32(3)), (Int32(1)), (Int32(2))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #7 ASC NULLS FIRST
      Projection: t.#1 AS #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW AS #5 AS Int32) AS rn AS #7
        WindowAggr: windowExpr=[[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 ELSE negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW AS row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
                SubqueryAlias: s
                  Projection: column1 AS #3
                    Values: (Int32(2))
          SubqueryAlias: t
            Projection: column1 AS #1
              Values: (Int32(3)), (Int32(1)), (Int32(2))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#7@1 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7]
          BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
      SortExec: expr=[#7@1 ASC], preserve_partitioning=[false], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
        ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
          BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted], statistics=[Rows=Exact(3), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]:)]]
            ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(3), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#3)@0 as #4], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#6:Int32;N, #7:Int32]
      SortExec: expr=[#7@1 ASC], preserve_partitioning=[false], schema=[#6:Int32;N, #7:Int32]
        ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7], schema=[#6:Int32;N, #7:Int32]
          BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted], schema=[#1:Int32;N, row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW:UInt64]
            ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#3)@0 as #4], schema=[#4:Int32;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], schema=[min(s.#3):Int32;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], schema=[min(s.#3)[value]:Int32;N]
            ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@1 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7]
            BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#7@1 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7]
            BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7]
          BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
            SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #1@0 ELSE negative(#1@0) END ASC], preserve_partitioning=[false]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7]
        BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
          SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #1@0 ELSE negative(#1@0) END ASC], preserve_partitioning=[false]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
          ProjectionExec: expr=[column1@0 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#6@0 as v, #7@1 as rn]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7]
          BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
            SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #1@0 ELSE negative(#1@0) END ASC], preserve_partitioning=[false]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#6@0 as v, #7@1 as rn]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7]
          BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
            SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #1@0 ELSE negative(#1@0) END ASC], preserve_partitioning=[false]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#3)@0 as #4]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
        ProjectionExec: expr=[column1@0 as #3]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#6@0 as v, #7@1 as rn]
      ScalarSubqueryExec: subqueries=1
        ProjectionExec: expr=[#1@0 as #6, CAST(row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW@1 AS Int32) as #7]
          BoundedWindowAggExec: wdw=[row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW: Field { "row_number() ORDER BY [CASE WHEN (<subquery>) = Int32(2) THEN t.#1 WHEN Boolean(true) THEN negative(t.#1) END ASC NULLS FIRST] RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW": UInt64 }, frame: RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW], mode=[Sorted]
            SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 2 THEN #1@0 ELSE negative(#1@0) END ASC], preserve_partitioning=[false]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_scalar_subquery_in_window_partition_expression"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #6 ASC NULLS FIRST
      Projection: t.#1 AS #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS #5 AS n AS #7
        WindowAggr: windowExpr=[[count(Int64(1)) PARTITION BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
                SubqueryAlias: s
                  Projection: #2 AS #3
                    Projection: column1 AS #2
                      Values: (Int32(1))
          SubqueryAlias: t
            Projection: #0 AS #1
              Projection: column1 AS #0
                Values: (Int32(1)), (Int32(2)), (Int32(3)), (Int32(4))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    Sort: #6 ASC NULLS FIRST
      Projection: t.#1 AS #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS #5 AS n AS #7
        WindowAggr: windowExpr=[[count(Int64(1)) PARTITION BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN CAST(raise_error(Utf8("Remainder by zero")) AS Int32) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
                SubqueryAlias: s
                  Projection: #2 AS #3
                    Projection: column1 AS #2
                      Values: (Int32(1))
          SubqueryAlias: t
            Projection: #0 AS #1
              Projection: column1 AS #0
                Values: (Int32(1)), (Int32(2)), (Int32(3)), (Int32(4))
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    Sort: #6 ASC NULLS FIRST
      Projection: t.#1 AS #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS #5 AS n AS #7
        WindowAggr: windowExpr=[[count(Int64(1)) PARTITION BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#1 % Int32(2) ELSE t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
                SubqueryAlias: s
                  Projection: #2 AS #3
                    Projection: column1 AS #2
                      Values: (Int32(1))
          SubqueryAlias: t
            Projection: #0 AS #1
              Projection: column1 AS #0
                Values: (Int32(1)), (Int32(2)), (Int32(3)), (Int32(4))
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #6 ASC NULLS FIRST
      Projection: t.#1 AS #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS #5 AS n AS #7
        WindowAggr: windowExpr=[[count(Int64(1)) PARTITION BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#1 % Int32(2) ELSE t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
                SubqueryAlias: s
                  Projection: column1 AS #3
                    Values: (Int32(1))
          SubqueryAlias: t
            Projection: column1 AS #1
              Values: (Int32(1)), (Int32(2)), (Int32(3)), (Int32(4))
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #6 ASC NULLS FIRST
      Projection: t.#1 AS #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS #5 AS n AS #7
        WindowAggr: windowExpr=[[count(Int64(1)) PARTITION BY [CASE WHEN (<subquery>) = Int32(1) THEN t.#1 % Int32(2) ELSE t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING AS count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING]]
          Subquery:
            Projection: min(s.#3) AS #4
              Aggregate: groupBy=[[]], aggr=[[min(s.#3)]]
                SubqueryAlias: s
                  Projection: column1 AS #3
                    Values: (Int32(1))
          SubqueryAlias: t
            Projection: column1 AS #1
              Values: (Int32(1)), (Int32(2)), (Int32(3)), (Int32(4))
  
    initial_physical_plan:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
          WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    initial_physical_plan_with_stats:
    ScalarSubqueryExec: subqueries=1, statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
      SortExec: expr=[#6@0 ASC], preserve_partitioning=[false], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
        ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0)),(Col[1]:)]]
          WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }], statistics=[Rows=Exact(4), Bytes=Absent, [(Col[0]: Null=Exact(0)),(Col[1]:)]]
            ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(4), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
      ProjectionExec: expr=[min(s.#3)@0 as #4], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(1), Bytes=Absent, [(Col[0]:)]]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], statistics=[Rows=Exact(0), Bytes=Inexact(<bytes>), [(Col[0]:)]]
            ProjectionExec: expr=[column1@0 as #3], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]
  
  
    initial_physical_plan_with_schema:
    ScalarSubqueryExec: subqueries=1, schema=[#6:Int32;N, #7:Int64]
      SortExec: expr=[#6@0 ASC], preserve_partitioning=[false], schema=[#6:Int32;N, #7:Int64]
        ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7], schema=[#6:Int32;N, #7:Int64]
          WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }], schema=[#1:Int32;N, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING:Int64]
            ProjectionExec: expr=[column1@0 as #1], schema=[#1:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
      ProjectionExec: expr=[min(s.#3)@0 as #4], schema=[#4:Int32;N]
        AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)], schema=[min(s.#3):Int32;N]
          AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)], schema=[min(s.#3)[value]:Int32;N]
            ProjectionExec: expr=[column1@0 as #3], schema=[#3:Int32;N]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Int32;N]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Final, gby=[], aggr=[min(s.#3)]
            AggregateExec: mode=Partial, gby=[], aggr=[min(s.#3)]
              ProjectionExec: expr=[column1@0 as #3]
                DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    SAME TEXT AS ABOVE
  
    physical_plan after CombinePartialFinalAggregate:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after EnforceSorting:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #1@0 % 2 ELSE #1@0 END ASC NULLS LAST], preserve_partitioning=[false]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after OutputRequirements:
    ScalarSubqueryExec: subqueries=1
      SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
          WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
            SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #1@0 % 2 ELSE #1@0 END ASC NULLS LAST], preserve_partitioning=[false]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]
      ProjectionExec: expr=[min(s.#3)@0 as #4]
        AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
          ProjectionExec: expr=[column1@0 as #3]
            DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#6@0 as v, #7@1 as n]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #1@0 % 2 ELSE #1@0 END ASC NULLS LAST], preserve_partitioning=[false]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#6@0 as v, #7@1 as n]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #1@0 % 2 ELSE #1@0 END ASC NULLS LAST], preserve_partitioning=[false]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        ProjectionExec: expr=[min(s.#3)@0 as #4]
          AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
            ProjectionExec: expr=[column1@0 as #3]
              DataSourceExec: partitions=1, partition_sizes=[1]
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[min(s.#3)@0 as #4]
      AggregateExec: mode=Single, gby=[], aggr=[min(s.#3)]
        ProjectionExec: expr=[column1@0 as #3]
          DataSourceExec: partitions=1, partition_sizes=[1]
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Broadcast)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#6@0 as v, #7@1 as n]
      ScalarSubqueryExec: subqueries=1
        SortExec: expr=[#6@0 ASC], preserve_partitioning=[false]
          ProjectionExec: expr=[#1@0 as #6, count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING@1 as #7]
            WindowAggExec: wdw=[count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING: Ok(Field { name: "count(Int64(1)) PARTITION BY [CASE WHEN #4 = Int32(1) THEN t.#1 % CASE WHEN Int32(2) = Int32(0) THEN raise_error(Utf8(/"Remainder by zero/")) ELSE Int32(2) END WHEN Boolean(true) THEN t.#1 END] ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING", data_type: Int64 }), frame: WindowFrame { units: Rows, start_bound: Preceding(UInt64(NULL)), end_bound: Following(UInt64(NULL)), is_causal: false }]
              SortExec: expr=[CASE WHEN scalar_subquery(<pending>) = 1 THEN #1@0 % 2 ELSE #1@0 END ASC NULLS LAST], preserve_partitioning=[false]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]
        StageInputExec: input=0, partitioning=UnknownPartitioning(1)
