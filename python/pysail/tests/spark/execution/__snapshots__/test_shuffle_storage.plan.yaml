# serializer version: 1
---
name: "test_query_execution_with_storage_shuffle"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.
  
    == Plan Steps ==
    initial_logical_plan:
    Sort: #13 ASC NULLS FIRST
      Projection: #12 AS #13, count(Int64(1)) AS count AS #14, sum(#10) AS s1 AS #15, sum(#11) AS s2 AS #16
        Aggregate: groupBy=[[#12]], aggr=[[count(Int64(1)), sum(#10), sum(#11)]]
          Projection: #8 AS #9, #2 AS #10, #5 AS #11, #8 % CASE WHEN Int32(4) = Int32(0) THEN raise_error(Utf8("Remainder by zero")) ELSE Int32(4) END AS #12
            Projection: #8, #2, #5
              Projection: coalesce(#1, #4) AS #8, #1 AS #6, #2, #4 AS #7, #5
                Inner Join: #1 = #4
                  ExplicitRepartition: kind=hash, n=Some(8), expr=[#1]
                    Projection: #0 AS k AS #1, #0 * Int32(2) AS v1 AS #2
                      Range: start=0, end=64, step=1, num_partitions=4
                  ExplicitRepartition: kind=hash, n=Some(8), expr=[#4]
                    Projection: #3 AS k AS #4, #3 + Int32(1) AS v2 AS #5
                      Range: start=0, end=64, step=1, num_partitions=4
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE
  
    logical_plan after type_coercion:
    Sort: #13 ASC NULLS FIRST
      Projection: #12 AS #13, count(Int64(1)) AS count AS #14, sum(#10) AS s1 AS #15, sum(#11) AS s2 AS #16
        Aggregate: groupBy=[[#12]], aggr=[[count(Int64(1)), sum(#10), sum(#11)]]
          Projection: #8 AS #9, #2 AS #10, #5 AS #11, #8 % CAST(CASE WHEN Int32(4) = Int32(0) THEN CAST(raise_error(Utf8("Remainder by zero")) AS Int32) ELSE Int32(4) END AS Int64) AS #12
            Projection: #8, #2, #5
              Projection: coalesce(#1, #4) AS #8, #1 AS #6, #2, #4 AS #7, #5
                Inner Join: #1 = #4
                  ExplicitRepartition: kind=hash, n=Some(8), expr=[#1]
                    Projection: #0 AS k AS #1, #0 * CAST(Int32(2) AS Int64) AS v1 AS #2
                      Range: start=0, end=64, step=1, num_partitions=4
                  ExplicitRepartition: kind=hash, n=Some(8), expr=[#4]
                    Projection: #3 AS k AS #4, #3 + CAST(Int32(1) AS Int64) AS v2 AS #5
                      Range: start=0, end=64, step=1, num_partitions=4
  
    analyzed_logical_plan:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    Sort: #13 ASC NULLS FIRST
      Projection: #12 AS #13, count(Int64(1)) AS count AS #14, sum(#10) AS s1 AS #15, sum(#11) AS s2 AS #16
        Aggregate: groupBy=[[#12]], aggr=[[count(Int64(1)), sum(#10), sum(#11)]]
          Projection: #8 AS #9, #2 AS #10, #5 AS #11, #8 % Int64(4) AS #12
            Projection: #8, #2, #5
              Projection: #1 AS #8, #1 AS #6, #2, #4 AS #7, #5
                Inner Join: #1 = #4
                  ExplicitRepartition: kind=hash, n=Some(8), expr=[#1]
                    Projection: #0 AS k AS #1, #0 * Int64(2) AS v1 AS #2
                      Range: start=0, end=64, step=1, num_partitions=4
                  ExplicitRepartition: kind=hash, n=Some(8), expr=[#4]
                    Projection: #3 AS k AS #4, #3 + Int64(1) AS v2 AS #5
                      Range: start=0, end=64, step=1, num_partitions=4
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #13 ASC NULLS FIRST
      Projection: #12 AS #13, count(Int64(1)) AS count AS #14, sum(#10) AS s1 AS #15, sum(#11) AS s2 AS #16
        Aggregate: groupBy=[[#12]], aggr=[[count(Int64(1)), sum(#10), sum(#11)]]
          Projection: #2 AS #10, #5 AS #11, #8 % Int64(4) AS #12
            Projection: #1 AS #8, #2, #5
              Inner Join: #1 = #4
                ExplicitRepartition: kind=hash, n=Some(8), expr=[#1]
                  Projection: #0 AS k AS #1, #0 * Int64(2) AS v1 AS #2
                    Range: start=0, end=64, step=1, num_partitions=4
                ExplicitRepartition: kind=hash, n=Some(8), expr=[#4]
                  Projection: #3 AS k AS #4, #3 + Int64(1) AS v2 AS #5
                    Range: start=0, end=64, step=1, num_partitions=4
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    Sort: #13 ASC NULLS FIRST
      Projection: #12 AS #13, count(Int64(1)) AS count AS #14, sum(#10) AS s1 AS #15, sum(#11) AS s2 AS #16
        Aggregate: groupBy=[[#12]], aggr=[[count(Int64(1)), sum(#10), sum(#11)]]
          Projection: #2 AS #10, #5 AS #11, #1 % Int64(4) AS #12
            Inner Join: #1 = #4
              ExplicitRepartition: kind=hash, n=Some(8), expr=[#1]
                Projection: #0 AS k AS #1, #0 * Int64(2) AS v1 AS #2
                  Range: start=0, end=64, step=1, num_partitions=4
              ExplicitRepartition: kind=hash, n=Some(8), expr=[#4]
                Projection: #3 AS k AS #4, #3 + Int64(1) AS v2 AS #5
                  Range: start=0, end=64, step=1, num_partitions=4
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE
  
    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE
  
    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE
  
    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE
  
    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE
  
    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE
  
    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE
  
    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE
  
    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE
  
    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE
  
    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE
  
    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE
  
    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE
  
    logical_plan:
    Sort: #13 ASC NULLS FIRST
      Projection: #12 AS #13, count(Int64(1)) AS count AS #14, sum(#10) AS s1 AS #15, sum(#11) AS s2 AS #16
        Aggregate: groupBy=[[#12]], aggr=[[count(Int64(1)), sum(#10), sum(#11)]]
          Projection: #2 AS #10, #5 AS #11, #1 % Int64(4) AS #12
            Inner Join: #1 = #4
              ExplicitRepartition: kind=hash, n=Some(8), expr=[#1]
                Projection: #0 AS k AS #1, #0 * Int64(2) AS v1 AS #2
                  Range: start=0, end=64, step=1, num_partitions=4
              ExplicitRepartition: kind=hash, n=Some(8), expr=[#4]
                Projection: #3 AS k AS #4, #3 + Int64(1) AS v2 AS #5
                  Range: start=0, end=64, step=1, num_partitions=4
  
    initial_physical_plan:
    SortExec: expr=[#13@0 ASC], preserve_partitioning=[false]
      ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
        AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
          AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
            ProjectionExec: expr=[#2@1 as #10, #5@3 as #11, #1@0 % 4 as #12]
              HashJoinExec: mode=Auto, join_type=Inner, on=[(#1@0, #4@0)]
                ExplicitRepartitionExec
                  ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                    RangeExec
                ExplicitRepartitionExec
                  ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                    RangeExec
  
  
    initial_physical_plan_with_stats:
    SortExec: expr=[#13@0 ASC], preserve_partitioning=[false], statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:)]]
      ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16], statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:)]]
        AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)], statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:)]]
          AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)], statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:)]]
            ProjectionExec: expr=[#2@1 as #10, #5@3 as #11, #1@0 % 4 as #12], statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:)]]
              HashJoinExec: mode=Auto, join_type=Inner, on=[(#1@0, #4@0)], statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:),(Col[2]:),(Col[3]:)]]
                ExplicitRepartitionExec, statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:)]]
                  ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2], statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:)]]
                    RangeExec, statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:)]]
                ExplicitRepartitionExec, statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:)]]
                  ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5], statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:),(Col[1]:)]]
                    RangeExec, statistics=[Rows=Absent, Bytes=Absent, [(Col[0]:)]]
  
  
    initial_physical_plan_with_schema:
    SortExec: expr=[#13@0 ASC], preserve_partitioning=[false], schema=[#13:Int64, #14:Int64, #15:Int64;N, #16:Int64;N]
      ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16], schema=[#13:Int64, #14:Int64, #15:Int64;N, #16:Int64;N]
        AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)], schema=[#12:Int64, count(Int64(1)):Int64, sum(#10):Int64;N, sum(#11):Int64;N]
          AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)], schema=[#12:Int64, count(Int64(1))[count]:Int64, sum(#10)[sum]:Int64;N, sum(#11)[sum]:Int64;N]
            ProjectionExec: expr=[#2@1 as #10, #5@3 as #11, #1@0 % 4 as #12], schema=[#10:Int64, #11:Int64, #12:Int64]
              HashJoinExec: mode=Auto, join_type=Inner, on=[(#1@0, #4@0)], schema=[#1:Int64, #2:Int64, #4:Int64, #5:Int64]
                ExplicitRepartitionExec, schema=[#1:Int64, #2:Int64]
                  ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2], schema=[#1:Int64, #2:Int64]
                    RangeExec, schema=[#0:Int64]
                ExplicitRepartitionExec, schema=[#4:Int64, #5:Int64]
                  ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5], schema=[#4:Int64, #5:Int64]
                    RangeExec, schema=[#3:Int64]
  
  
    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[(#13@0, asc)], dist_by=SinglePartition
      SortExec: expr=[#13@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
          AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
            AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
              ProjectionExec: expr=[#2@1 as #10, #5@3 as #11, #1@0 % 4 as #12]
                HashJoinExec: mode=Auto, join_type=Inner, on=[(#1@0, #4@0)]
                  ExplicitRepartitionExec
                    ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                      RangeExec
                  ExplicitRepartitionExec
                    ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                      RangeExec
  
  
    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE
  
    physical_plan after join_selection:
    OutputRequirementExec: order_by=[(#13@0, asc)], dist_by=SinglePartition
      SortExec: expr=[#13@0 ASC], preserve_partitioning=[false]
        ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
          AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
            AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
              ProjectionExec: expr=[#2@1 as #10, #5@3 as #11, #1@0 % 4 as #12]
                HashJoinExec: mode=Partitioned, join_type=Inner, on=[(#1@0, #4@0)]
                  ExplicitRepartitionExec
                    ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                      RangeExec
                  ExplicitRepartitionExec
                    ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                      RangeExec
  
  
    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceDistribution:
    OutputRequirementExec: order_by=[(#13@0, asc)], dist_by=SinglePartition
      SortExec: expr=[#13@0 ASC], preserve_partitioning=[false]
        CoalescePartitionsExec
          ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
            AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
              RepartitionExec: partitioning=Hash([#12@0], 4), input_partitions=8
                AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
                  ProjectionExec: expr=[#2@1 as #10, #5@3 as #11, #1@0 % 4 as #12]
                    HashJoinExec: mode=Partitioned, join_type=Inner, on=[(#1@0, #4@0)]
                      ExplicitRepartitionExec
                        ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                          RangeExec
                      ExplicitRepartitionExec
                        ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                          RangeExec
  
  
    physical_plan after CombinePartialFinalAggregate:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceSorting:
    OutputRequirementExec: order_by=[(#13@0, asc)], dist_by=SinglePartition
      SortPreservingMergeExec: [#13@0 ASC]
        SortExec: expr=[#13@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
            AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
              RepartitionExec: partitioning=Hash([#12@0], 4), input_partitions=8
                AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
                  ProjectionExec: expr=[#2@1 as #10, #5@3 as #11, #1@0 % 4 as #12]
                    HashJoinExec: mode=Partitioned, join_type=Inner, on=[(#1@0, #4@0)]
                      ExplicitRepartitionExec
                        ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                          RangeExec
                      ExplicitRepartitionExec
                        ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                          RangeExec
  
  
    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE
  
    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[(#13@0, asc)], dist_by=SinglePartition
      SortPreservingMergeExec: [#13@0 ASC]
        SortExec: expr=[#13@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
            AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
              RepartitionExec: partitioning=Hash([#12@0], 4), input_partitions=8
                AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
                  ProjectionExec: expr=[#2@0 as #10, #5@1 as #11, #1@2 % 4 as #12]
                    HashJoinExec: mode=Partitioned, join_type=Inner, on=[(#1@0, #4@0)], projection=[#2@1, #5@3, #1@0]
                      ExplicitRepartitionExec
                        ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                          RangeExec
                      ExplicitRepartitionExec
                        ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                          RangeExec
  
  
    physical_plan after OutputRequirements:
    SortPreservingMergeExec: [#13@0 ASC]
      SortExec: expr=[#13@0 ASC], preserve_partitioning=[true]
        ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
          AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
            RepartitionExec: partitioning=Hash([#12@0], 4), input_partitions=8
              AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
                ProjectionExec: expr=[#2@0 as #10, #5@1 as #11, #1@2 % 4 as #12]
                  HashJoinExec: mode=Partitioned, join_type=Inner, on=[(#1@0, #4@0)], projection=[#2@1, #5@3, #1@0]
                    ExplicitRepartitionExec
                      ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                        RangeExec
                    ExplicitRepartitionExec
                      ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                        RangeExec
  
  
    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE
  
    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE
  
    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE
  
    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE
  
    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE
  
    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE
  
    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE
  
    physical_plan after RewriteExplicitRepartition:
    SortPreservingMergeExec: [#13@0 ASC]
      SortExec: expr=[#13@0 ASC], preserve_partitioning=[true]
        ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
          AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
            RepartitionExec: partitioning=Hash([#12@0], 4), input_partitions=8
              AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
                ProjectionExec: expr=[#2@0 as #10, #5@1 as #11, #1@2 % 4 as #12]
                  HashJoinExec: mode=Partitioned, join_type=Inner, on=[(#1@0, #4@0)], projection=[#2@1, #5@3, #1@0]
                    RepartitionExec: partitioning=Hash([#1@0], 8), input_partitions=4
                      ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                        RangeExec
                    RepartitionExec: partitioning=Hash([#4@0], 8), input_partitions=4
                      ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                        RangeExec
  
  
    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE
  
    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE
  
    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE
  
    physical_plan:
    ProjectionExec: expr=[#13@0 as g, #14@1 as count, #15@2 as s1, #16@3 as s2]
      SortPreservingMergeExec: [#13@0 ASC]
        SortExec: expr=[#13@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
            AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
              RepartitionExec: partitioning=Hash([#12@0], 4), input_partitions=8
                AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
                  ProjectionExec: expr=[#2@0 as #10, #5@1 as #11, #1@2 % 4 as #12]
                    HashJoinExec: mode=Partitioned, join_type=Inner, on=[(#1@0, #4@0)], projection=[#2@1, #5@3, #1@0]
                      RepartitionExec: partitioning=Hash([#1@0], 8), input_partitions=4
                        ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                          RangeExec
                      RepartitionExec: partitioning=Hash([#4@0], 8), input_partitions=4
                        ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                          RangeExec
  
  
    == Physical Plan ==
    ProjectionExec: expr=[#13@0 as g, #14@1 as count, #15@2 as s1, #16@3 as s2]
      SortPreservingMergeExec: [#13@0 ASC]
        SortExec: expr=[#13@0 ASC], preserve_partitioning=[true]
          ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
            AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
              RepartitionExec: partitioning=Hash([#12@0], 4), input_partitions=8
                AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
                  ProjectionExec: expr=[#2@0 as #10, #5@1 as #11, #1@2 % 4 as #12]
                    HashJoinExec: mode=Partitioned, join_type=Inner, on=[(#1@0, #4@0)], projection=[#2@1, #5@3, #1@0]
                      RepartitionExec: partitioning=Hash([#1@0], 8), input_partitions=4
                        ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
                          RangeExec
                      RepartitionExec: partitioning=Hash([#4@0], 8), input_partitions=4
                        ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
                          RangeExec
  
  
    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=4
    distribution=Hash(keys=[#1@0], channels=8)
    placement=Worker
    ProjectionExec: expr=[#0@0 as #1, #0@0 * 2 as #2]
      RangeExec
  
    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Shuffle)]
    mode=Blocking
    partitions=8
    distribution=RoundRobin(channels=1)
    placement=Worker
    StageInputExec: input=0, partitioning=Hash([#1@0], 8)
  
    === stage 2 ===
    inputs=[]
    mode=Pipelined
    partitions=4
    distribution=Hash(keys=[#4@0], channels=8)
    placement=Worker
    ProjectionExec: expr=[#3@0 as #4, #3@0 + 1 as #5]
      RangeExec
  
    === stage 3 ===
    inputs=[StageInput(stage=2, mode=Shuffle)]
    mode=Blocking
    partitions=8
    distribution=RoundRobin(channels=1)
    placement=Worker
    StageInputExec: input=0, partitioning=Hash([#4@0], 8)
  
    === stage 4 ===
    inputs=[StageInput(stage=1, mode=Forward), StageInput(stage=3, mode=Forward)]
    mode=Pipelined
    partitions=8
    distribution=Hash(keys=[#12@0], channels=4)
    placement=Worker
    AggregateExec: mode=Partial, gby=[#12@2 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
      ProjectionExec: expr=[#2@0 as #10, #5@1 as #11, #1@2 % 4 as #12]
        HashJoinExec: mode=Partitioned, join_type=Inner, on=[(#1@0, #4@0)], projection=[#2@1, #5@3, #1@0]
          StageInputExec: input=0, partitioning=Hash([#1@0], 8)
          StageInputExec: input=1, partitioning=Hash([#4@0], 8)
  
    === stage 5 ===
    inputs=[StageInput(stage=4, mode=Shuffle)]
    mode=Blocking
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    StageInputExec: input=0, partitioning=Hash([#12@0], 4)
  
    === stage 6 ===
    inputs=[StageInput(stage=5, mode=Forward)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobin(channels=1)
    placement=Worker
    SortExec: expr=[#13@0 ASC], preserve_partitioning=[true]
      ProjectionExec: expr=[#12@0 as #13, count(Int64(1))@1 as #14, sum(#10)@2 as #15, sum(#11)@3 as #16]
        AggregateExec: mode=FinalPartitioned, gby=[#12@0 as #12], aggr=[count(Int64(1)), sum(#10), sum(#11)]
          StageInputExec: input=0, partitioning=Hash([#12@0], 4)
  
    === stage 7 ===
    inputs=[StageInput(stage=6, mode=Merge)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobin(channels=1)
    placement=Worker
    ProjectionExec: expr=[#13@0 as g, #14@1 as count, #15@2 as s1, #16@3 as s2]
      SortPreservingMergeExec: [#13@0 ASC]
        StageInputExec: input=0, partitioning=Hash([#13@0], 4)
