# serializer version: 1
---
name: "test_binary_distinct_uses_largebinary_grouping"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.

    == Plan Steps ==
    initial_logical_plan:
    Distinct:
      Projection: t.#1 AS #2
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Binary("1")), (Binary("1"))

    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE

    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE

    logical_plan after type_coercion:
    SAME TEXT AS ABOVE

    analyzed_logical_plan:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE

    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE

    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE

    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE

    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE

    logical_plan after replace_distinct_aggregate:
    Aggregate: groupBy=[[#2]], aggr=[[]]
      Projection: t.#1 AS #2
        SubqueryAlias: t
          Projection: #0 AS #1
            Projection: column1 AS #0
              Values: (Binary("1")), (Binary("1"))

    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE

    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE

    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE

    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE

    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE

    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE

    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE

    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE

    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE

    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE

    logical_plan after optimize_projections:
    Aggregate: groupBy=[[#2]], aggr=[[]]
      Projection: t.#1 AS #2
        SubqueryAlias: t
          Projection: column1 AS #1
            Values: (Binary("1")), (Binary("1"))

    logical_plan after rewrite_binary_grouping:
    Projection: CAST(CAST(#2 || LargeBinary("") AS BinaryView) AS Binary) AS #2
      Aggregate: groupBy=[[CAST(#2 AS LargeBinary) AS #2]], aggr=[[]]
        Projection: t.#1 AS #2
          SubqueryAlias: t
            Projection: column1 AS #1
              Values: (Binary("1")), (Binary("1"))

    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE

    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE

    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE

    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE

    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE

    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE

    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE

    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE

    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE

    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE

    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE

    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE

    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE

    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE

    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE

    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE

    logical_plan after rewrite_binary_grouping:
    SAME TEXT AS ABOVE

    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE

    logical_plan:
    Projection: CAST(CAST(#2 || LargeBinary("") AS BinaryView) AS Binary) AS #2
      Aggregate: groupBy=[[CAST(#2 AS LargeBinary) AS #2]], aggr=[[]]
        Projection: t.#1 AS #2
          SubqueryAlias: t
            Projection: column1 AS #1
              Values: (Binary("1")), (Binary("1"))

    initial_physical_plan:
    ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2]
      AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[]
        AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[]
          ProjectionExec: expr=[#1@0 as #2]
            ProjectionExec: expr=[column1@0 as #1]
              DataSourceExec: partitions=1, partition_sizes=[1]


    initial_physical_plan_with_stats:
    ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
      AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          ProjectionExec: expr=[#1@0 as #2], statistics=[Rows=Exact(2), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0))]]
            ProjectionExec: expr=[column1@0 as #1], statistics=[Rows=Exact(2), Bytes=Inexact(<bytes>), [(Col[0]: Null=Exact(0))]]
              DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(2), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]


    initial_physical_plan_with_schema:
    ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2], schema=[#2:Binary]
      AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[], schema=[#2:LargeBinary]
        AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[], schema=[#2:LargeBinary]
          ProjectionExec: expr=[#1@0 as #2], schema=[#2:Binary]
            ProjectionExec: expr=[column1@0 as #1], schema=[#1:Binary]
              DataSourceExec: partitions=1, partition_sizes=[1], schema=[column1:Binary]


    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[]
          AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[]
            ProjectionExec: expr=[#1@0 as #2]
              ProjectionExec: expr=[column1@0 as #1]
                DataSourceExec: partitions=1, partition_sizes=[1]


    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE

    physical_plan after JoinReorder:
    SAME TEXT AS ABOVE

    physical_plan after join_selection:
    SAME TEXT AS ABOVE

    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE

    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE

    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE

    physical_plan after EnsureRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[]
          RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
            AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[]
              ProjectionExec: expr=[#1@0 as #2]
                ProjectionExec: expr=[column1@0 as #1]
                  DataSourceExec: partitions=1, partition_sizes=[1]


    physical_plan after CombinePartialFinalAggregate:
    SAME TEXT AS ABOVE

    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE

    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[]
          RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
            AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[]
              ProjectionExec: expr=[column1@0 as #2]
                DataSourceExec: partitions=1, partition_sizes=[1]


    physical_plan after OutputRequirements:
    ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2]
      AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[]
        RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
          AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[]
            ProjectionExec: expr=[column1@0 as #2]
              DataSourceExec: partitions=1, partition_sizes=[1]


    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE

    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE

    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE

    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE

    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE

    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE

    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE

    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE

    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE

    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE

    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE

    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE

    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE

    physical_plan:
    ProjectionExec: expr=[#2@0 as k]
      ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[]
          RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
            AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[]
              ProjectionExec: expr=[column1@0 as #2]
                DataSourceExec: partitions=1, partition_sizes=[1]


    == Physical Plan ==
    ProjectionExec: expr=[#2@0 as k]
      ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[]
          RepartitionExec: partitioning=Hash([#2@0], 4), input_partitions=1
            AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[]
              ProjectionExec: expr=[column1@0 as #2]
                DataSourceExec: partitions=1, partition_sizes=[1]


    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=Hash(keys=[#2@0], channels=4)
    placement=Worker
    AggregateExec: mode=Partial, gby=[CAST(#2@0 AS LargeBinary) as #2], aggr=[]
      ProjectionExec: expr=[column1@0 as #2]
        DataSourceExec: partitions=1, partition_sizes=[1]

    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobinBatch(channels=1)
    placement=Worker
    ProjectionExec: expr=[#2@0 as k]
      ProjectionExec: expr=[CAST(CAST(#2@0 ||  AS BinaryView) AS Binary) as #2]
        AggregateExec: mode=FinalPartitioned, gby=[#2@0 as #2], aggr=[]
          StageInputExec: input=0, partitioning=Hash([#2@0], 4)
