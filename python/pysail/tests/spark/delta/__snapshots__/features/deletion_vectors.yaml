# serializer version: 1
---
name: "test_delta_log_after_dv_delete_contains_correct_commit_actions"
data:
  clientVersion: sail-delta-lake.x.x.x
  engineInfo: sail-delta-lake:x.x.x
  isBlindAppend: false
  operation: DELETE
  operationMetrics:
    executionTimeMs: <time_ms>
    numAddedChangeFiles: 0
    numAddedFiles: 1
    numCopiedRows: 0
    numDeletedRows: 1
    numDeletionVectorsAdded: 1
    numDeletionVectorsRemoved: 0
    numDeletionVectorsUpdated: 0
    numRemovedFiles: 1
    scanTimeMs: <time_ms>
  operationParameters:
    predicate: 'id = 2 '
  timestamp: <timestamp>
---
name: "test_explain_codegen_filtered_dv_scan_retains_estimated_statistics"
data:
  |-
    == Codegen ==
    Whole-stage codegen is not supported; showing physical plan instead.

    == Plan Steps ==
    initial_logical_plan:
    Projection: count(Int64(1)) AS cnt AS #3
      Aggregate: groupBy=[[]], aggr=[[count(Int64(1))]]
        Filter: delta_dv_explain.#2 > Int32(100)
          Projection: delta_dv_explain.id AS #0, delta_dv_explain.name AS #1, delta_dv_explain.value AS #2
            TableScan: delta_dv_explain

    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE

    logical_plan after resolve_grouping_function:
    SAME TEXT AS ABOVE

    logical_plan after type_coercion:
    SAME TEXT AS ABOVE

    analyzed_logical_plan:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE

    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE

    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE

    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE

    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE

    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE

    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE

    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE

    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE

    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE

    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE

    logical_plan after push_down_filter:
    Projection: count(Int64(1)) AS cnt AS #3
      Aggregate: groupBy=[[]], aggr=[[count(Int64(1))]]
        Projection: delta_dv_explain.id AS #0, delta_dv_explain.name AS #1, delta_dv_explain.value AS #2
          Filter: delta_dv_explain.value > Int32(100)
            TableScan: delta_dv_explain, partial_filters=[delta_dv_explain.value > Int32(100)]

    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE

    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE

    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE

    logical_plan after optimize_projections:
    Projection: count(Int64(1)) AS cnt AS #3
      Aggregate: groupBy=[[]], aggr=[[count(Int64(1))]]
        Projection:
          Filter: delta_dv_explain.value > Int32(100)
            TableScan: delta_dv_explain projection=[value], partial_filters=[delta_dv_explain.value > Int32(100)]

    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_lateral_projection:
    SAME TEXT AS ABOVE

    logical_plan after rewrite_set_comparison:
    SAME TEXT AS ABOVE

    logical_plan after optimize_unions:
    SAME TEXT AS ABOVE

    logical_plan after unions_to_filter:
    SAME TEXT AS ABOVE

    logical_plan after simplify_expressions:
    SAME TEXT AS ABOVE

    logical_plan after replace_distinct_aggregate:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_join:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_predicate_subquery:
    SAME TEXT AS ABOVE

    logical_plan after scalar_subquery_to_join:
    SAME TEXT AS ABOVE

    logical_plan after decorrelate_lateral_join:
    SAME TEXT AS ABOVE

    logical_plan after extract_equijoin_predicate:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_duplicated_expr:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_filter:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_cross_join:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_limit:
    SAME TEXT AS ABOVE

    logical_plan after propagate_empty_relation:
    SAME TEXT AS ABOVE

    logical_plan after filter_null_join_keys:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_outer_join:
    SAME TEXT AS ABOVE

    logical_plan after push_down_limit:
    SAME TEXT AS ABOVE

    logical_plan after push_down_filter:
    SAME TEXT AS ABOVE

    logical_plan after single_distinct_aggregation_to_group_by:
    SAME TEXT AS ABOVE

    logical_plan after eliminate_group_by_constant:
    SAME TEXT AS ABOVE

    logical_plan after common_sub_expression_eliminate:
    SAME TEXT AS ABOVE

    logical_plan after extract_leaf_expressions:
    SAME TEXT AS ABOVE

    logical_plan after optimize_projections:
    SAME TEXT AS ABOVE

    logical_plan after resolve_lambda_variables:
    SAME TEXT AS ABOVE

    logical_plan:
    Projection: count(Int64(1)) AS cnt AS #3
      Aggregate: groupBy=[[]], aggr=[[count(Int64(1))]]
        Projection:
          Filter: delta_dv_explain.value > Int32(100)
            TableScan: delta_dv_explain projection=[value], partial_filters=[delta_dv_explain.value > Int32(100)]

    initial_physical_plan:
    ProjectionExec: expr=[count(Int64(1))@0 as #3]
      AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))]
        AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))]
          ProjectionExec: expr=[]
            FilterExec: value@0 > 100
              ProjectionExec: expr=[value@0 as value]
                DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true)
                  DataSourceExec: partitions=1, partition_sizes=[1]


    initial_physical_plan_with_stats:
    ProjectionExec: expr=[count(Int64(1))@0 as #3], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]:)]]
      AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
        AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))], statistics=[Rows=Exact(1), Bytes=Inexact(<bytes>), [(Col[0]:)]]
          ProjectionExec: expr=[], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), []]
            FilterExec: value@0 > 100, statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]: Min=Inexact(Int32(101)) Max=Inexact(Int32(300)) Null=Exact(0))]]
              ProjectionExec: expr=[value@0 as value], statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]: Min=Inexact(Int32(100)) Max=Inexact(Int32(300)) Null=Inexact(0))]]
                DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true), statistics=[Rows=Inexact(2), Bytes=Inexact(<bytes>), [(Col[0]: Min=Inexact(Int32(100)) Max=Inexact(Int32(300)) Null=Inexact(0))]]
                  DataSourceExec: partitions=1, partition_sizes=[1], statistics=[Rows=Exact(1), Bytes=Exact(<bytes>), [(Col[0]: Null=Exact(0))]]


    initial_physical_plan_with_schema:
    ProjectionExec: expr=[count(Int64(1))@0 as #3], schema=[#3:Int64]
      AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))], schema=[count(Int64(1)):Int64]
        AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))], schema=[count(Int64(1))[count]:Int64]
          ProjectionExec: expr=[], schema=[]
            FilterExec: value@0 > 100, schema=[value:Int32]
              ProjectionExec: expr=[value@0 as value], schema=[value:Int32]
                DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true), schema=[value:Int32]
                  DataSourceExec: partitions=1, partition_sizes=[1], schema=[action:Union(Dense, 0: ("add": non-null Struct("path": non-null Utf8, "partitionValues": non-null Map("key_value": non-null Struct("key": non-null Utf8, "value": Utf8), unsorted), "size": non-null Int64, "modificationTime": non-null Int64, "dataChange": non-null Boolean, "stats": Utf8, "tags": Map("key_value": non-null Struct("key": non-null Utf8, "value": Utf8), unsorted), "deletionVector": Struct("storageType": non-null Utf8, "pathOrInlineDv": non-null Utf8, "offset": Int32, "sizeInBytes": non-null Int32, "cardinality": non-null Int64), "baseRowId": Int64, "defaultRowCommitVersion": Int64, "clusteringProvider": Utf8)), 1: ("remove": non-null Struct("path": non-null Utf8, "dataChange": non-null Boolean, "deletionTimestamp": Int64, "extendedFileMetadata": Boolean, "partitionValues": Map("key_value": non-null Struct("key": non-null Utf8, "value": Utf8), unsorted), "size": Int64, "stats": Utf8, "tags": Map("key_value": non-null Struct("key": non-null Utf8, "value": Utf8), unsorted), "deletionVector": Struct("storageType": non-null Utf8, "pathOrInlineDv": non-null Utf8, "offset": Int32, "sizeInBytes": non-null Int32, "cardinality": non-null Int64), "baseRowId": Int64, "defaultRowCommitVersion": Int64)), 2: ("protocol": non-null Struct("minReaderVersion": non-null Int32, "minWriterVersion": non-null Int32, "readerFeatures": List(Utf8, field: 'element'), "writerFeatures": List(Utf8, field: 'element'))), 3: ("metadata": non-null Struct("id": non-null Utf8, "name": Utf8, "description": Utf8, "format": non-null Struct("provider": non-null Utf8, "options": non-null Map("key_value": non-null Struct("key": non-null Utf8, "value": Utf8), unsorted)), "schemaString": non-null Utf8, "partitionColumns": non-null List(Utf8, field: 'element'), "createdTime": Int64, "configuration": non-null Map("key_value": non-null Struct("key": non-null Utf8, "value": Utf8), unsorted))), 4: ("commit_meta": non-null Struct("commit_row_count": non-null UInt64, "operation_json": Utf8, "operation_metrics_json": Utf8)))]


    physical_plan after OutputRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ProjectionExec: expr=[count(Int64(1))@0 as #3]
        AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))]
          AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))]
            ProjectionExec: expr=[]
              FilterExec: value@0 > 100
                ProjectionExec: expr=[value@0 as value]
                  DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true)
                    DataSourceExec: partitions=1, partition_sizes=[1]


    physical_plan after aggregate_statistics:
    SAME TEXT AS ABOVE

    physical_plan after join_selection:
    SAME TEXT AS ABOVE

    physical_plan after LimitedDistinctAggregation:
    SAME TEXT AS ABOVE

    physical_plan after FilterPushdown:
    SAME TEXT AS ABOVE

    physical_plan after WindowTopN:
    SAME TEXT AS ABOVE

    physical_plan after EnsureRequirements:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ProjectionExec: expr=[count(Int64(1))@0 as #3]
        AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))]
          CoalescePartitionsExec
            AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))]
              ProjectionExec: expr=[]
                FilterExec: value@0 > 100
                  RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                    ProjectionExec: expr=[value@0 as value]
                      DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true)
                        DataSourceExec: partitions=1, partition_sizes=[1]


    physical_plan after CombinePartialFinalAggregate:
    SAME TEXT AS ABOVE

    physical_plan after OptimizeAggregateOrder:
    SAME TEXT AS ABOVE

    physical_plan after ProjectionPushdown:
    OutputRequirementExec: order_by=[], dist_by=Unspecified
      ProjectionExec: expr=[count(Int64(1))@0 as #3]
        AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))]
          CoalescePartitionsExec
            AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))]
              FilterExec: value@0 > 100, projection=[]
                RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                  DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true)
                    DataSourceExec: partitions=1, partition_sizes=[1]


    physical_plan after OutputRequirements:
    ProjectionExec: expr=[count(Int64(1))@0 as #3]
      AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))]
        CoalescePartitionsExec
          AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))]
            FilterExec: value@0 > 100, projection=[]
              RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true)
                  DataSourceExec: partitions=1, partition_sizes=[1]


    physical_plan after LimitAggregation:
    SAME TEXT AS ABOVE

    physical_plan after LimitPushPastWindows:
    SAME TEXT AS ABOVE

    physical_plan after HashJoinBuffering:
    SAME TEXT AS ABOVE

    physical_plan after LimitPushdown:
    SAME TEXT AS ABOVE

    physical_plan after TopKRepartition:
    SAME TEXT AS ABOVE

    physical_plan after ProjectionPushdown:
    SAME TEXT AS ABOVE

    physical_plan after PushdownSort:
    SAME TEXT AS ABOVE

    physical_plan after EnsureCooperative:
    SAME TEXT AS ABOVE

    physical_plan after FilterPushdown(Post):
    SAME TEXT AS ABOVE

    physical_plan after RewriteExplicitRepartition:
    SAME TEXT AS ABOVE

    physical_plan after RewriteCollectLeftHashJoin:
    SAME TEXT AS ABOVE

    physical_plan after EnforceBarrierPartitioning:
    SAME TEXT AS ABOVE

    physical_plan after SanityCheckPlan:
    SAME TEXT AS ABOVE

    physical_plan:
    ProjectionExec: expr=[#3@0 as cnt]
      ProjectionExec: expr=[count(Int64(1))@0 as #3]
        AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))]
          CoalescePartitionsExec
            AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))]
              FilterExec: value@0 > 100, projection=[]
                RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                  DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true)
                    DataSourceExec: partitions=1, partition_sizes=[1]


    == Physical Plan ==
    ProjectionExec: expr=[#3@0 as cnt]
      ProjectionExec: expr=[count(Int64(1))@0 as #3]
        AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))]
          CoalescePartitionsExec
            AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))]
              FilterExec: value@0 > 100, projection=[]
                RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                  DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true)
                    DataSourceExec: partitions=1, partition_sizes=[1]


    == Distributed Plan ==
    === stage 0 ===
    inputs=[]
    mode=Pipelined
    partitions=1
    distribution=RoundRobinBatch(channels=4)
    placement=Worker
    DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([2]), limit=None, pushdown=true)
      DataSourceExec: partitions=1, partition_sizes=[1]

    === stage 1 ===
    inputs=[StageInput(stage=0, mode=Shuffle)]
    mode=Pipelined
    partitions=4
    distribution=RoundRobinBatch(channels=1)
    placement=Worker
    AggregateExec: mode=Partial, gby=[], aggr=[count(Int64(1))]
      FilterExec: value@0 > 100, projection=[]
        StageInputExec: input=0, partitioning=RoundRobinBatch(4)

    === stage 2 ===
    inputs=[StageInput(stage=1, mode=Shuffle)]
    mode=Pipelined
    partitions=1
    distribution=RoundRobinBatch(channels=1)
    placement=Worker
    ProjectionExec: expr=[#3@0 as cnt]
      ProjectionExec: expr=[count(Int64(1))@0 as #3]
        AggregateExec: mode=Final, gby=[], aggr=[count(Int64(1))]
          StageInputExec: input=0, partitioning=UnknownPartitioning(1)
---
name: "test_explain_count_literal_after_delete_uses_logical_dv_statistics"
data:
  |-
    == Physical Plan ==
    ProjectionExec: expr=[#3@0 as cnt]
      ProjectionExec: expr=[2 as #3]
        PlaceholderRowExec
---
name: "test_explain_delete_on_dv_table_shows_deletionvectorwriterexec"
data:
  |-
    == Physical Plan ==
    DeltaCommitExec(table_path=file://<tmp>/dv_explain/)
      CoalescePartitionsExec
        DeletionVectorWriterExec: condition=id@0 = 2
          DeltaDiscoveryExec(table_path=file://<tmp>/dv_explain/)
            FilterExec: get_field(stats_parsed@7, minValues, id) IS NULL OR get_field(stats_parsed@7, maxValues, id) IS NULL OR get_field(stats_parsed@7, minValues, id) <= 2 AND get_field(stats_parsed@7, maxValues, id) >= 2
              DeltaMetadataStatsExec(output=stats_parsed, columns=[id])
                DeltaLogReplayExec(table_path=file://<tmp>/dv_explain/, version=0, mode=sort)
                  SortExec: expr=[__sail_delta_replay_path@7 ASC NULLS LAST, __sail_delta_log_version@9 DESC NULLS LAST, __sail_delta_is_remove@8 ASC NULLS LAST], preserve_partitioning=[true]
                    ProjectionExec: expr=[CASE WHEN add@0 IS NOT NULL THEN get_field(add@0, path) END as __sail_file_path, CASE WHEN CASE WHEN add@0 IS NOT NULL THEN get_field(add@0, size) END IS NOT NULL THEN CASE WHEN add@0 IS NOT NULL THEN get_field(add@0, size) END ELSE 0 END as size_bytes, CASE WHEN CASE WHEN add@0 IS NOT NULL THEN get_field(add@0, modificationTime) END IS NOT NULL THEN CASE WHEN add@0 IS NOT NULL THEN get_field(add@0, modificationTime) END ELSE 0 END as modification_time, NULL as _commit_version, NULL as _commit_timestamp, CASE WHEN add@0 IS NOT NULL THEN get_field(add@0, stats) END as stats_json, CASE WHEN add@0 IS NOT NULL THEN get_field(add@0, deletionVector) END as deletionVector, CASE WHEN CASE WHEN add@0 IS NOT NULL THEN concat(CAST(octet_length(get_field(add@0, path)) AS Utf8), :, get_field(add@0, path), :, CASE WHEN add@0 IS NOT NULL AND get_field(add@0, deletionVector) IS NOT NULL THEN concat(1:, concat(get_field(add@0, deletionVector, storageType), get_field(add@0, deletionVector, pathOrInlineDv), CASE WHEN get_field(add@0, deletionVector, offset) IS NOT NULL THEN concat(@, CAST(get_field(add@0, deletionVector, offset) AS Utf8)) ELSE  END)) ELSE 0 END) END IS NOT NULL THEN CASE WHEN add@0 IS NOT NULL THEN concat(CAST(octet_length(get_field(add@0, path)) AS Utf8), :, get_field(add@0, path), :, CASE WHEN add@0 IS NOT NULL AND get_field(add@0, deletionVector) IS NOT NULL THEN concat(1:, concat(get_field(add@0, deletionVector, storageType), get_field(add@0, deletionVector, pathOrInlineDv), CASE WHEN get_field(add@0, deletionVector, offset) IS NOT NULL THEN concat(@, CAST(get_field(add@0, deletionVector, offset) AS Utf8)) ELSE  END)) ELSE 0 END) END ELSE CASE WHEN remove@1 IS NOT NULL THEN concat(CAST(octet_length(get_field(remove@1, path)) AS Utf8), :, get_field(remove@1, path), :, CASE WHEN remove@1 IS NOT NULL AND get_field(remove@1, deletionVector) IS NOT NULL THEN concat(1:, concat(get_field(remove@1, deletionVector, storageType), get_field(remove@1, deletionVector, pathOrInlineDv), CASE WHEN get_field(remove@1, deletionVector, offset) IS NOT NULL THEN concat(@, CAST(get_field(remove@1, deletionVector, offset) AS Utf8)) ELSE  END)) ELSE 0 END) END END as __sail_delta_replay_path, remove@1 IS NOT NULL AND add@0 IS NULL as __sail_delta_is_remove, __sail_delta_log_version@2 as __sail_delta_log_version]
                      RepartitionExec: partitioning=Hash([CASE WHEN CASE WHEN add@0 IS NOT NULL THEN concat(CAST(octet_length(get_field(add@0, path)) AS Utf8), :, get_field(add@0, path), :, CASE WHEN add@0 IS NOT NULL AND get_field(add@0, deletionVector) IS NOT NULL THEN concat(1:, concat(get_field(add@0, deletionVector, storageType), get_field(add@0, deletionVector, pathOrInlineDv), CASE WHEN get_field(add@0, deletionVector, offset) IS NOT NULL THEN concat(@, CAST(get_field(add@0, deletionVector, offset) AS Utf8)) ELSE  END)) ELSE 0 END) END IS NOT NULL THEN CASE WHEN add@0 IS NOT NULL THEN concat(CAST(octet_length(get_field(add@0, path)) AS Utf8), :, get_field(add@0, path), :, CASE WHEN add@0 IS NOT NULL AND get_field(add@0, deletionVector) IS NOT NULL THEN concat(1:, concat(get_field(add@0, deletionVector, storageType), get_field(add@0, deletionVector, pathOrInlineDv), CASE WHEN get_field(add@0, deletionVector, offset) IS NOT NULL THEN concat(@, CAST(get_field(add@0, deletionVector, offset) AS Utf8)) ELSE  END)) ELSE 0 END) END ELSE CASE WHEN remove@1 IS NOT NULL THEN concat(CAST(octet_length(get_field(remove@1, path)) AS Utf8), :, get_field(remove@1, path), :, CASE WHEN remove@1 IS NOT NULL AND get_field(remove@1, deletionVector) IS NOT NULL THEN concat(1:, concat(get_field(remove@1, deletionVector, storageType), get_field(remove@1, deletionVector, pathOrInlineDv), CASE WHEN get_field(remove@1, deletionVector, offset) IS NOT NULL THEN concat(@, CAST(get_field(remove@1, deletionVector, offset) AS Utf8)) ELSE  END)) ELSE 0 END) END END], 4), input_partitions=1
                        DataSourceExec: file_groups={1 group: [[<tmp>/dv_explain/_delta_log/00000000000000000000.json]]}, projection=[add, remove, __sail_delta_log_version], file_type=json
---
name: "test_explain_select_on_dv_table_after_delete_uses_eager_add_input"
data:
  |-
    == Physical Plan ==
    ProjectionExec: expr=[#3@0 as id, #4@1 as name, #5@2 as value]
      ProjectionExec: expr=[id@0 as #3, name@1 as #4, value@2 as #5]
        SortExec: expr=[id@0 ASC], preserve_partitioning=[false]
          DeltaScanByAddsExec(table_path=file://<tmp>/dv_explain/, version=1, projection=Some([0, 1, 2]), limit=None, pushdown=false)
            DataSourceExec: partitions=1, partition_sizes=[1]
